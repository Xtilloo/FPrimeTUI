# F´ (F Prime) AI Training Dataset

This document contains a synthetic Q&A dataset derived from the NASA F´ (F Prime) framework documentation. It is designed to train or fine-tune AI models on F´ architecture, modeling, framework services, testing, and implementation.

| ID | Category | Question (Prompt) | Comprehensive Answer | Technical Reference (File/Folder) |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Architecture | What are the fundamental differences between Active, Passive, and Queued components in F´? | **Active components** possess an internal execution thread and a message queue; they dispatch messages asynchronously on their own thread. **Queued components** have a message queue but no thread; they require an external synchronous invocation (e.g., from a RateGroup) to call `doDispatch()` and process their queue. **Passive components** have neither a thread nor a queue; they execute logic synchronously in the context of the calling component's thread. | `Fw/Comp` |
| 2 | Modeling | How is a State Machine defined in FPP and integrated into a component? | A state machine is defined using the `state machine` keyword in FPP, specifying `initial`, `state`, `signal`, and `transition` (using `on ... enter`). To integrate it, a component must be `active` or `queued` and declare a `state machine instance`. The autocoder generates a base class (e.g., `MStateMachineBase`) and pure virtual action/guard handlers (e.g., `action_doReset`) that the developer must implement in the component's C++ class. | `docs/how-to/define-state-machines.md` |
| 3 | Framework Services | Describe the protocol for command dispatching and response in F´. | The `Svc::CmdDispatcher` receives a serialized `Fw::Com` packet, extracts the opcode, and identifies the target component instance. It invokes the component's `CmdDisp` input port. Upon completion, the handling component must call `cmdResponse_out(opcode, cmdSeq, status)` to return an `Fw::CmdResponse` (e.g., `OK`, `VALIDATION_ERROR`, `EXECUTION_ERROR`) back to the dispatcher or sequencer. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 4 | Testing | How do you verify that a specific event was emitted with correct arguments in a GTest unit test? | In the `Tester` class (derived from `GTestBase`), use the generated macros: `ASSERT_EVENTS_SIZE(n)` to check the total count, and `ASSERT_EVENTS_EventName(index, arg1, arg2)` to verify the specific instance and its parameters. The tester harness automatically captures these in a history using the `from_Log` port. | `docs/user-manual/overview/unit-testing.md` |
| 5 | Implementation | What is the purpose of `Fw::Buffer` and how is it managed via `Svc::BufferManager`? | `Fw::Buffer` is a lightweight wrapper containing a pointer to a memory region and its size. Unlike typed ports, it handles raw byte streams (e.g., for file uplink). `Svc::BufferManager` provides a pool of these buffers, organized into "bins" of specific sizes. Components request a buffer via an `Fw::BufferGet` port and must return it via an `Fw::BufferSend` port to prevent memory leaks in embedded contexts. | `docs/user-manual/framework/dynamic-memory.md` |
| 6 | Architecture | Explain the "Application-Manager-Driver" design pattern. | This pattern promotes modularity by layering software: the **Application** layer implements mission logic (e.g., a robotic arm); the **Manager** layer translates high-level requests into hardware-specific protocols (e.g., a Servo Manager); and the **Driver** layer handles raw I/O with hardware (e.g., `Drv::LinuxI2cDriver`). This allows the same manager logic to be reused across different hardware platforms by simply swapping the driver component. | `docs/user-manual/design-patterns/app-man-drv.md` |
| 7 | Modeling | What is the difference between an FPP `struct` and an FPP `serializable`? | In modern FPP, `struct` is the keyword used to define a serializable data structure. A `serializable` (or `struct`) generates a C++ class that inherits from `Fw::Serializable`, providing `serialize()` and `deserialize()` methods. These allow the type to be passed through ports, stored as parameters, or downlinked as telemetry while maintaining architecture-independent byte ordering (typically big-endian). | `docs/user-manual/overview/05-enum-arr-ser.md` |
| 8 | Framework Services | How does the `Svc::Health` component monitor system responsiveness? | Active components implement a `pingIn` (async input) and `pingOut` (output) port pair of type `Svc::Ping`. `Svc::Health` periodically sends a "ping" signal to these components. If a component fails to respond within a configured number of rate group ticks, `Svc::Health` emits a `WARNING_HI` event, and eventually a `FATAL` event if the component remains unresponsive, typically triggering a system reset. | `docs/user-manual/design-patterns/health-checking.md` |
| 9 | Implementation | When should a developer override the `preMsgHook` in an implementation class? | `preMsgHook` is an optional virtual function generated for asynchronous ports and commands. It is called on the thread of the **caller** before the message is actually placed on the component's queue. Developers use it to perform lightweight, immediate actions or sanity checks before the message is processed asynchronously by the component's own thread. | `docs/user-manual/framework/autocoded-functions.md` |
| 10 | Modeling | Describe the use of "Base IDs" in an F´ Topology. | Every component instance in a topology is assigned a `base id`. All relative IDs for commands (opcodes), telemetry channels, events, and parameters defined within that component are added to this base ID. This ensures that every data item in the system has a globally unique ID in the generated F´ dictionary, preventing collisions between different instances of the same component type. | `docs/user-manual/overview/proj-dep.md` |
| 11 | Testing | What is the "fprime_test_api" and how is it used for integration testing? | It is a Python-based API used with `pytest` to perform system-level verification. It connects to a running GDS and provides methods like `send_and_assert_command`, `await_telemetry`, and `assert_event`. It uses "predicates" to filter and search through histories of data received from the flight software, allowing testers to verify complex end-to-end behaviors. | `docs/user-manual/gds/gds-test-api-guide.md` |
| 12 | Architecture | How do Rate Groups ensure deterministic execution in F´? | A `Svc::RateGroupDriver` receives a master "tick" from a hardware interrupt or clock driver. It divides this clock into sub-frequencies (e.g., 10Hz, 1Hz) and invokes `Svc::ActiveRateGroup` components. These rate groups then synchronously call the `schedIn` ports of attached components in a specific, prioritized order. Determinism is maintained by ensuring all work within a cycle completes before the next tick ("rate group slip" detection). | `docs/user-manual/design-patterns/rate-group.md` |
| 13 | Implementation | What is `Fw::PolyType` and when is it useful? | `Fw::PolyType` is a polymorphic type that can represent any F´ primitive (U32, I8, F32, bool, etc.) using a fixed-size internal buffer. It is used in generic components like `Svc::PolyDb` to store and retrieve various data types without the database component needing to know the specific underlying type at compile time. | `Fw/Types` |
| 14 | Modeling | How do you define a port that returns a value in FPP? | Use the arrow syntax in the port definition: `port GetData(arg1: U32) -> F32`. Ports with return values must be instantiated as `sync` or `guarded` on the input side because they execute in the caller's context to return the value immediately. They cannot be `async` because the message queue mechanism does not support immediate functional return. | `docs/user-manual/overview/03-port-comp-top.md` |
| 15 | Framework Services | Explain the difference between `set` and `save` operations for F´ Parameters. | The `set` command (auto-generated for each parameter) updates the parameter's value in the component's local RAM. The `save` command triggers the component to send its current local value to a parameter database (like `Svc::PrmDb`), which then writes it to non-volatile storage (e.g., a file) so the value persists across system reboots. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 16 | Troubleshooting | What causes a "Rate Group Slip" and how is it mitigated? | A slip occurs when the synchronous execution of all components attached to a rate group takes longer than the rate group's period (e.g., 100ms of work in a 10Hz group). Mitigation involves optimizing component logic, moving non-critical or long-running tasks to a slower rate group, or using the "Manager/Worker" pattern to offload work to a low-priority background thread. | `docs/user-manual/design-patterns/rate-group.md` |
| 17 | Implementation | How does F´ handle string data safely in embedded systems? | F´ uses the `Fw::StringBase` and `Fw::FixedLengthString` classes. These enforce a maximum size (defined in FPP or `FpConfig.h`) to prevent buffer overflows. Strings are serialized with their length preceding the characters, and the framework avoids standard C-string functions in favor of size-aware bounds-checked methods. | `Fw/Types` |
| 18 | Architecture | Describe the "Hub Pattern" for multi-processor communication. | The Hub pattern uses a specialized component (`Svc::GenericHub`) with "serialize ports." It captures port invocations, serializes them into a buffer, and transmits them over a physical link (UART, Ethernet) to a remote node. The remote hub receives the buffer, deserializes it, and invokes the corresponding local port. This allows components on different CPUs to communicate as if they were in the same address space. | `docs/user-manual/design-patterns/hub-pattern.md` |
| 19 | Testing | What is the purpose of `TesterBase::doDispatch()` in a unit test? | For `active` or `queued` components, messages sent to input ports are queued. In a unit test environment, there is no background thread to process the queue. The tester must manually call `doDispatch()` to pull the next message from the component's queue and execute its handler logic within the test's execution context. | `docs/user-manual/overview/unit-testing.md` |
| 20 | Framework Services | What is the role of `Svc::TlmChan` in the telemetry system? | `Svc::TlmChan` acts as a centralized database for telemetry. Components write their channel updates to it via port calls. `Svc::TlmChan` stores the latest value for every channel ID. When triggered by a RateGroup, it collects all updated values, packages them into a `Fw::Com` packet, and sends them to the downlink stack for transmission to the ground. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 21 | Modeling | How are "Internal Interfaces" used in FPP? | Internal interfaces define a mechanism for a component to send a message to itself asynchronously. Calling `internalInterface_internalInterfaceInvoke(...)` places a message on the component's own queue. When dispatched, the `internalInterface_handler(...)` is executed. This is useful for breaking up long-running tasks into smaller chunks that don't block the queue for extended periods. | `docs/user-manual/framework/autocoded-functions.md` |
| 22 | Implementation | Describe the use of `FW_ASSERT` and its configuration levels. | `FW_ASSERT` is a macro for runtime error checking. If the condition is false, it calls an assertion hook (usually printing an error and halting). Levels include: `FW_NO_ASSERT` (compiled out), `FW_FILEID_ASSERT` (uses a numeric hash instead of a filename string to save space), and `FW_FILENAME_ASSERT` (standard behavior with full file and line info). | `Fw/Types/Assert.hpp` |
| 23 | Architecture | What is a "Subtopology" and what are its benefits? | A subtopology is a reusable grouping of component instances and their internal connections (e.g., a standard "Comm Stack"). It is defined in FPP and can be imported into multiple different deployment topologies. This promotes architectural reuse and reduces the manual wiring required when setting up new projects or deployments. | `docs/user-manual/design-patterns/subtopologies.md` |
| 24 | Testing | How do you perform "Test-Driven Development" (TDD) for an F´ component? | 1. Design the interface in FPP (ports, commands). 2. Generate the implementation stubs with `fprime-util impl`. 3. Write a failing unit test in the `test/ut` folder using `fprime-util impl --ut`. 4. Implement the component logic in C++ until `fprime-util check` passes. 5. Refine and add edge-case tests. | `docs/how-to/test-driven-development.md` |
| 25 | Framework Services | Explain the "Framer Status Protocol" in the communication stack. | This protocol regulates data flow between `Svc::ComQueue` and the radio driver. The `Svc::Framer` receives a packet, frames it, and sends it to the driver. The driver returns a `comStatus` (SUCCESS/FAILURE). The Framer forwards this status back to the `ComQueue`. Only upon receiving a `SUCCESS` status will the `ComQueue` release the next packet, preventing the driver's hardware buffers from being overwhelmed. | `docs/reference/communication-adapter-interface.md` |
| 26 | Build System | What is the role of `settings.ini` in an F´ deployment? | `settings.ini` defines the environment configuration for a specific deployment. Key fields include `project_root` (path to the project), `framework_path` (path to F´ core), `library_locations` (external modules), and `default_toolchain`. It ensures that `fprime-util` can correctly locate dependencies and generate the appropriate build cache for the target platform. | `docs/user-manual/build-system/settings.md` |
| 27 | GDS | How do you create a custom GDS Dashboard? | Custom dashboards are defined using an XML specification. Users can layout functional components like `command-input`, `event-list`, and `channel-table` within structural tags like `dashboard-box` and `dashboard-row`. Properties like `filter-text`, `fields`, and `items-shown` allow developers to create mission-specific views that are uploaded via the GDS "Dashboard" tab. | `docs/user-manual/gds/gds-custom-dashboards.md` |
| 28 | Modeling | What are "Array Records" in F´ Data Products? | A product record can contain a single value of type *T* or an array of values of type *T* with a statically unknown size (using the `array` keyword). Unlike standard FPP arrays, these records store their dynamic size in the container, allowing for flexible science data collection (e.g., a variable-length sensor burst). | `docs/user-manual/framework/data-products.md` |
| 29 | Implementation | Explain the memory safety implications of NASA's "No Dynamic Memory" rule in F´. | F´ adheres to the rule that no dynamic memory allocation (`new`, `malloc`) should occur after system initialization. This prevents unpredictable runtime failures due to heap exhaustion or fragmentation. Instead, all components are instantiated at startup, and dynamic data handling is managed via pre-allocated buffer pools like `Svc::BufferManager`. | `docs/user-manual/overview/fprime_conventions.md` |
| 30 | Architecture | What is a "Driver Component" in the context of hardware abstraction? | A Driver component is the lowest software layer that interacts directly with hardware registers or OS-level device drivers (e.g., `/dev/i2c-1`). It implements the "Byte Stream Driver Model" or specific bus interfaces (I2C, SPI, UART). Its role is to provide a hardware-independent interface to "Manager" components, facilitating portability. | `docs/user-manual/design-patterns/app-man-drv.md` |
| 31 | GDS | Describe the purpose of the `fprime-cli` tool. | `fprime-cli` provides a headless, command-line interface to the GDS. It allows operators to monitor events (`fprime-cli events`), check telemetry (`fprime-cli channels`), and send commands (`fprime-cli command-send`) from a terminal. This is essential for automation, remote server operation, and integration with shell-based testing scripts. | `docs/user-manual/gds/gds-cli.md` |
| 32 | Framework Services | How is "Time" distributed across an F´ system? | A central time source component (e.g., `Svc::LinuxTime`) implements the `Svc::Time` port. Every component requiring a timestamp (for events or telemetry) calls its `timeGetOut` port. The framework automatically injects these timestamps into packets, ensuring all data is correlated to a consistent system clock or external epoch. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 33 | OSAL | What does the `Os::Task` class abstract? | `Os::Task` provides an abstraction for operating system threads (or tasks). It wraps platform-specific calls (e.g., `pthread_create` on Linux, `taskSpawn` on VxWorks) to provide a consistent interface for starting, joining, and managing the execution context of `active` components. | `Os/Task` |
| 34 | Build System | Explain the difference between `fprime-util generate` and `fprime-util build`. | `generate` invokes CMake to create the build cache (Makefile or Ninja files) based on the specified toolchain and platform files. `build` executes the actual compilation and linking process using the files created during the generate step. `generate` must be re-run if any `CMakeLists.txt` or `settings.ini` files are modified. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 35 | Modeling | How do you define a "Guarded" command in FPP? | A command is defined as `guarded command mnemonic(...)` in FPP. The autocoder generates a handler that is executed within a mutex-protected region. This ensures that if multiple sources (e.g., a sequencer and a ground link) send commands simultaneously, the component's internal state remains thread-safe during execution. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 36 | Implementation | What are the constraints on "Passive" components regarding threading? | Passive components execute entirely within the thread of the component that invokes their ports. Consequently, they cannot define `async` input ports or `async` commands, as they lack a message queue and thread to process them. They are ideal for low-overhead, synchronous data transformations. | `docs/user-manual/overview/03-port-comp-top.md` |
| 37 | Testing | How can you simulate a hardware failure in a unit test? | By using a "Mock" or "Stub" implementation of a Driver component. The `Tester` class can be configured to return error codes (e.g., `Drv::I2cStatus::I2C_OPEN_ERR`) from its "to_Port" handlers, allowing the developer to verify that the component under test correctly handles and reports the hardware anomaly via events. | `docs/user-manual/overview/unit-testing.md` |
| 38 | GDS | What is an F´ "Sequence" file (.seq)? | A sequence file is a human-readable text file containing a list of commands with either absolute (`A`) or relative (`R`) timestamps. The `fprime-seqgen` tool compiles these into a compact binary format (`.bin`) that is uploaded to the flight software and executed by `Svc::CmdSequencer` to perform automated tasks. | `docs/user-manual/gds/seqgen.md` |
| 39 | Architecture | What is "Thread Virtualization" and when is it used? | Used primarily on baremetal or single-threaded systems, it unrolls the loops of `active` components into "run-once" functions. A central scheduler (e.g., `Os::Baremetal::Scheduler`) calls these in a round-robin fashion within the main program loop, simulating parallel execution without requiring a multi-threaded OS kernel. | `docs/user-manual/framework/baremetal-multicore.md` |
| 40 | Modeling | Define "Alias Types" in FPP. | Alias types (using the `type` keyword) provide alternative names for existing data types (e.g., `type MyId = U32`). They improve code readability and allow for system-wide type changes (e.g., upgrading an ID from 16-bit to 32-bit) by modifying a single definition in an FPP file. | `docs/user-manual/overview/05-enum-arr-ser.md` |
| 41 | Implementation | How does F´ handle "Endianness" during serialization? | By default, F´ serializes all data in **big-endian** (network byte order) to ensure compatibility between different processor architectures. The `Fw::Serializable` interface provides `serializeTo` and `deserializeFrom` methods that can optionally accept an `Fw::Endianness` flag if a specific little-endian format is required for hardware interfaces. | `docs/user-manual/overview/05-enum-arr-ser.md` |
| 42 | Framework Services | Describe the role of `Svc::PrmDb`. | `Svc::PrmDb` is the Parameter Database. It loads a non-volatile file containing parameter values at startup and serves them to components via `ParamGet` ports. It also receives updated values via `ParamSet` ports and can be commanded to `SAVE` the current database back to the non-volatile file. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 43 | Build System | What is a CMake "Platform" file in F´? | A Platform file (e.g., `Linux.cmake`, `Darwin.cmake`) defines platform-specific configurations, including basic types (`PlatformTypes.h`) and chooses default implementations for OSAL services (e.g., choosing `Os::File::Posix` for Linux). It acts as the bridge between the generic F´ framework and the target OS. | `docs/user-manual/build-system/cmake-platforms.md` |
| 44 | Coding Standards | Why does NASA rule #4 limit function length to ~60 lines? | Limiting functions to what fits on a single printed page ensures that the logic is simple, readable, and easy to verify. It forces developers to decompose complex logic into smaller, testable units, reducing the likelihood of "spaghetti code" and logical errors in safety-critical systems. | `docs/user-manual/overview/fprime_conventions.md` |
| 45 | GDS | How are "Ground-Derived Channels" implemented? | They are implemented as GDS Python plugins using the `DataHandlerPlugin` base class. The plugin subscribes to specific telemetry descriptors, performs calculations on the ground (e.g., converting volts to percentage), and publishes the result back into the GDS as a new channel, appearing as if it came from the flight software. | `docs/how-to/derive-channels-on-ground.md` |
| 46 | Architecture | Explain "Subtopologies" and how they use "Phases". | Subtopologies group component instances and their connections for reuse. They often use "Phases" (C++ configuration functions tied to `init` specifiers in FPP) to allow instance-specific initialization logic (like setting a unique device path) while keeping the main topology's C++ code clean and modular. | `docs/how-to/develop-subtopologies.md` |
| 47 | Troubleshooting | How do you debug an `FW_ASSERT` failure in a production build? | If `FW_FILEID_ASSERT` is used, the error will report a numeric hash. Developers can use `fprime-util hash-to-file <hash>` to recover the filename where the assertion occurred. Examining the surrounding code and the provided argument values helps identify the software state that violated the safety check. | `docs/user-manual/framework/configuring-fprime.md` |
| 48 | Implementation | What is the `Fw::Buffer` "context" field used for? | The context field in `Fw::Buffer` is a user-defined integer value. It is often used to track the source or intended destination of a buffer as it passed through asynchronous port chains, helping components correlate responses with original requests without inspecting the payload. | `docs/user-manual/framework/dynamic-memory.md` |
| 49 | Modeling | How do you specify a telemetry channel that only updates "on change"? | In the FPP telemetry definition, add the `update on change` specifier. The `Svc::TlmChan` component will then only include this channel in its downlink packets if the value has been updated with a *different* value since the last downlink cycle, saving bandwidth on low-activity channels. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 50 | Testing | What is the purpose of "Reciprocal Rank Fusion" (RRF) in the fprime-tui RAG system? | RRF is used to merge the results of **dense retrieval** (vector-based semantic search) and **sparse retrieval** (keyword-based BM25 search). It assigns a unified score to documents based on their rank in both lists, ensuring that chunks relevant by both meaning and specific terms are prioritized for the LLM's context. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 51 | Framework Services | List and define the 7 F´ Event Severities. | 1. **DIAGNOSTIC**: Debug data, rarely downlinked. 2. **COMMAND**: Traces command execution. 3. **ACTIVITY_LO**: Info from background tasks. 4. **ACTIVITY_HI**: Info from foreground/ground actions. 5. **WARNING_LO**: Non-critical anomalies. 6. **WARNING_HI**: Critical anomalies, system remains functional. 7. **FATAL**: Unrecoverable error requiring a system reset. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 52 | GDS | What are the three default ports used by the F´ GDS? | 1. **5000**: The HTML frontend (browser access). 2. **50000**: The communication interface port (where the flight software connects). 3. **50050**: The internal server port (ThreadedTCP) used for transporting GDS data between layers. | `docs/user-manual/overview/gds-introduction.md` |
| 53 | Implementation | How do you enable "Port Tracing" in F´? | Set the `FW_PORT_TRACING` macro to `1` in `FpConfig.h`. When enabled, calling `setTrace(true)` on a port instance will cause the framework to log a message every time that port is invoked, allowing developers to visualize the data flow through the topology. | `docs/user-manual/framework/configuring-fprime.md` |
| 54 | Architecture | Explain the "Manager/Worker" pattern. | This pattern uses an **Active Worker** component (low priority) to perform long-running or blocking tasks (e.g., file I/O, heavy math) and a **Manager** component (high priority) to orchestrate these tasks. The Manager sends a request to the Worker and remains responsive to ground commands; the Worker sends a callback port invocation when the task is complete. | `docs/user-manual/design-patterns/manager-worker.md` |
| 55 | Modeling | What is the `Fw.Signal` port type? | `Fw.Signal` is a standard framework port type that carries no arguments. It is commonly used for "tick" signals, simple notifications, or triggering actions that do not require external data, such as a RateGroup heartbeat or a simple completion callback. | `Fw/Types` |
| 56 | Build System | How do you cross-compile F´ for an ARM architecture? | 1. Install the appropriate toolchain (e.g., `gcc-arm-linux-gnueabihf`). 2. Set the `ARM_TOOLS_PATH` environment variable to the toolchain location. 3. Run `fprime-util generate <platform-name>` (e.g., `aarch64-linux`). 4. Run `fprime-util build <platform-name>`. | `docs/tutorials/cross-compilation.md` |
| 57 | OSAL | What is the purpose of `Os::Mutex`? | `Os::Mutex` provides a platform-independent abstraction for mutual exclusion. It is used to protect shared data structures from simultaneous access by multiple threads. In F´, it is the underlying mechanism for "guarded" ports and internal critical sections in `active` components. | `Os/Mutex` |
| 58 | GDS | How does the `fprime-seqgen` tool handle absolute vs. relative time? | `fprime-seqgen` parses sequence files where absolute times start with `A` (ISO-8601 format) and relative times start with `R` (HH:MM:SS format). Relative times are calculated from the execution of the *previous* command in the sequence, allowing for complex time-based automation scripts. | `docs/user-manual/gds/seqgen.md` |
| 59 | Modeling | What are "Internal Interfaces" in an F´ component? | They are asynchronous messages that a component sends to **itself**. Calling an internal interface puts a message on the component's own queue. This allows a component to defer work, break up long tasks, or change its own state in a thread-safe manner without blocking its current execution context. | `docs/user-manual/framework/autocoded-functions.md` |
| 60 | Implementation | Describe the `FW_OBJECT_NAMES` configuration setting. | When `FW_OBJECT_NAMES` is set to `1` in `FpConfig.h`, every component and port instance stores its string name in RAM. This is invaluable for debugging and tracing, as it allows logs and error messages to report the specific instance name (e.g., `imuManager`) rather than just a pointer address. | `docs/user-manual/framework/configuring-fprime.md` |
| 61 | Testing | What is a "Predicate" in the `fprime_test_api`? | A Predicate is a callable Python class or function that evaluates if a data object (Event, Channel, etc.) meets a specific criteria (e.g., `val > 10`). The API uses them in functions like `await_telemetry(predicate)` to find matching data in the GDS history during integration tests. | `docs/user-manual/gds/gds-test-api-guide.md` |
| 62 | Architecture | How does F´ support "Baremetal" (No-OS) systems? | Through **Thread Virtualization** and a specialized OSAL. The system uses a `BAREMETAL_SCHEDULER` that calls each component's "run-once" logic in a sequential loop. This removes the need for an OS context switcher, allowing F´ to run on microcontrollers like the MSP430 or Pi Pico. | `docs/user-manual/framework/baremetal-multicore.md` |
| 63 | Modeling | What is the role of `initial enter` in a State Machine? | The `initial enter` statement defines the starting state of the machine. Upon component initialization, the state machine automatically transitions into this state. Any entry actions defined for that state will be executed, but they must not call output ports since components are not yet connected at this phase. | `docs/how-to/define-state-machines.md` |
| 64 | Framework Services | What is the `Svc::RateGroupDriver` configuration "DividerSet"? | It is a table of integers passed to the driver's constructor. Each integer defines how many master clock "ticks" must occur before a specific `CycleOut` port is invoked. For example, if the master clock is 100Hz, a divider of 10 results in a 10Hz rate group. | `docs/user-manual/design-patterns/rate-group.md` |
| 65 | Implementation | How do you implement a custom serializable class? | Create a C++ class that inherits from `Fw::Serializable` and implement the `serializeTo()` and `deserializeFrom()` methods. These methods must use the provided `Fw::SerializeBufferBase` to write/read member variables in a specific order, ensuring data can be passed across F´ ports or to the GDS. | `docs/user-manual/overview/05-enum-arr-ser.md` |
| 66 | GDS | What is the purpose of the "Orb" in the GDS GUI? | The "Orb" is a visual connection status indicator in the top-right of the GDS browser. A **Green** circle indicates active data flow from the flight software; a **Red X** indicates a timeout or disconnection. It provides immediate feedback to operators on the health of the ground-to-space link. | `docs/user-manual/overview/gds-introduction.md` |
| 67 | Build System | What is a Software Bill of Materials (SBOM) in F´? | F´ can automatically generate an SBOM in `spdx-json` format using the `syft` tool. It records every software tool, library, and dependency used to build the flight software, facilitating vulnerability scanning (e.g., using `grype`) and supply chain security compliance. | `docs/user-manual/security/software-bill-of-materials.md` |
| 68 | Coding Standards | Define NASA Rule #3: "No dynamic memory allocation after initialization." | This rule mandates that all memory needed for flight operations must be allocated during the startup/init phase. Once the "forever loop" begins, `malloc`, `free`, `new`, and `delete` are strictly forbidden. This eliminates the risk of heap fragmentation or out-of-memory crashes during critical mission phases. | `docs/user-manual/overview/fprime_conventions.md` |
| 69 | Troubleshooting | What is a "Buffer Manager ID Collision"? | This occurs when two different `Svc::BufferManager` instances are assigned the same ID in a topology. When an `Fw::Buffer` is returned via `dataReturnOut`, the manager checks the ID to ensure it owns that memory. If IDs collide, a buffer might be returned to the wrong pool, leading to memory corruption or crashes. | `docs/user-manual/framework/dynamic-memory.md` |
| 70 | Architecture | What is the "CdhCore" subtopology? | `CdhCore` is a standard, reusable FPP subtopology provided by the framework. It includes the core Command and Data Handling components: `CmdDispatcher`, `EventManager`, `TlmChan`, `PrmDb`, and `Health`. Most F´ deployments import this to establish basic ground-link capabilities instantly. | `docs/user-manual/design-patterns/subtopologies.md` |
| 71 | Modeling | How do you define a port array in FPP? | Use the square bracket notation: `sync input port myPorts: [10] MyPortType`. This creates 10 instances of the port on the component. The handler function in C++ will receive a `portNum` argument (0-9) to identify which specific port in the array was invoked. | `docs/user-manual/overview/03-port-comp-top.md` |
| 72 | Implementation | What is the `Fw::ExternalSerializeBuffer` used for? | It is a wrapper that allows an external, pre-existing memory region (like a raw `U8` array) to be treated as an F´ serialization buffer. This avoids extra memory copies when serializing data directly into hardware-managed buffers or DMA regions. | `Fw/Types` |
| 73 | Testing | How do you check code coverage for F´ unit tests? | Run `fprime-util check --coverage`. This compiles the tests with coverage instrumentation (GCOV), executes them, and generates `.gcov` files. Developers can then inspect these to see exactly which lines of C++ code were (and were not) executed by their test suite. | `docs/user-manual/overview/unit-testing.md` |
| 74 | Framework Services | Describe the "Time Base" field in an `Fw::Time` object. | The Time Base identifies the clock source used for the timestamp. Examples include `TB_PROC_TIME` (processor uptime), `TB_SC_TIME` (synchronized spacecraft time), and `TB_WORKSTATION_TIME` (ground system time). It ensures that data from different sources is only compared if it shares a common epoch. | `docs/user-manual/framework/configuring-fprime.md` |
| 75 | GDS | How do you send a command with a string argument containing spaces via `fprime-cli`? | Wrap the argument in double quotes: `fprime-cli command-send myComp.LogMessage --arguments "This is a message with spaces"`. The CLI parser will treat the quoted string as a single argument rather than multiple space-separated tokens. | `docs/user-manual/gds/gds-cli.md` |
| 76 | Framework Services | What is the difference between binary and text log ports? | A **binary log port** (`Log`) emits event data in a compact, serialized format optimized for transmission over bandwidth-limited links to the GDS. A **text log port** (`TextLog`) emits a fully formatted human-readable string (e.g., "IMU reset successful") intended for local console output or real-time debugging. | `docs/user-manual/framework/autocoded-functions.md` |
| 77 | GDS | How can you list all available commands for a deployment using the CLI? | Use the command `fprime-cli command-send -l` (or `--list`). This reads the F´ dictionary and prints a complete list of all command mnemonics, their numeric opcodes, and the arguments they require, which is useful for rapid shell-based testing. | `docs/user-manual/gds/gds-cli.md` |
| 78 | GDS | What does the `fprime-gds -n` flag do? | The `-n` (or `--no-app`) flag prevents the GDS from automatically launching the native flight software executable. This is used when the flight software is already running on embedded hardware or in a separate terminal, allowing the GDS to act only as a communication server and GUI. | `docs/user-manual/overview/gds-introduction.md` |
| 79 | Implementation | Explain the `FW_SERIALIZABLE_TO_STRING` macro. | When set to `1`, the autocoder generates a `toString()` method for every `serializable` class. This allows the data structure to be easily converted into a human-readable string for logging or UI display, though it increases the final binary size. | `docs/user-manual/framework/configuring-fprime.md` |
| 80 | Architecture | Describe the "Generic Hub" component. | `Svc::GenericHub` is a standard implementation of the Hub pattern. It provides a set of serialized input and output ports that can be used to tunnel any F´ port call across a physical communication link (like a socket or UART), enabling transparent inter-processor communication. | `docs/user-manual/design-patterns/hub-pattern.md` |
| 81 | Framework Services | How does `Svc::TlmChan` handle incoming telemetry during a downlink cycle? | `Svc::TlmChan` uses **double-buffering**. While one buffer is being collected and packaged for downlink, incoming telemetry updates are written to the second "active" buffer. This prevents race conditions and ensures telemetry data is never lost during the transmission process. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 82 | Implementation | What is the `Svc::StaticMemory` component? | `Svc::StaticMemory` is a simplified memory manager that provides fixed-size regions of stack-allocated memory. Each client component is assigned a unique port index. It is designed for deterministic, high-reliability systems where asynchronous heap-like allocation is not required. | `docs/user-manual/framework/dynamic-memory.md` |
| 83 | GDS | How do you filter events by severity in the `fprime-cli`? | Use the `--search` flag with the severity name, for example: `fprime-cli events --search FATAL`. This will filter the real-time event stream to only show events that contain the string "FATAL" in their severity or description fields. | `docs/user-manual/gds/gds-cli.md` |
| 84 | OSAL | What does `Os::File` abstract? | `Os::File` provides a platform-independent API for file system operations, such as `open()`, `read()`, `write()`, and `seek()`. It allows F´ components (like the FileUplink manager) to perform disk I/O without knowing the underlying OS (Linux, RTOS, or baremetal). | `Os/File` |
| 85 | Modeling | Can a State Machine have "Guards" on its transitions? | Yes. A guard is a boolean function (e.g., `guard_isReady`) that is evaluated before a transition occurs. In FPP, this is defined using the `if` syntax on a transition. The developer must implement the guard logic in C++, and the transition will only execute if the guard returns `true`. | `docs/user-manual/framework/state-machines.md` |
| 86 | Coding Standards | Define NASA Rule #1: "Simple Control Flow." | Rule #1 forbids the use of complex flow structures like `goto`, `setjmp`, `longjmp`, and both direct and indirect recursion. This ensures that the code's execution path is predictable, making it easier to perform static analysis and formal verification. | `docs/user-manual/overview/fprime_conventions.md` |
| 87 | Build System | What is the purpose of the `fprime-util impl` command? | `impl` (Implementation) generates C++ skeleton files (`.cpp-template` and `.hpp-template`) based on an FPP component definition. These templates contain all the necessary handler function stubs and boilerplate, ensuring the hand-coded implementation matches the autogenerated base class. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 88 | Troubleshooting | How do you detect a "Rate Group Slip"? | `Svc::ActiveRateGroup` tracks its execution time. If the synchronous port calls take longer than the cycle period, it emits a `WARNING_HI` event (e.g., `RateGroupSlip`). Operators can monitor this event to identify CPU over-utilization or non-deterministic component behavior. | `docs/user-manual/design-patterns/rate-group.md` |
| 89 | Implementation | Explain the `FW_COM_BUFFER_MAX_SIZE` setting. | This setting in `FpConfig.h` defines the maximum size of the generic communication buffer used for ground links. It must be large enough to hold the largest possible serialized command, event, or telemetry channel in the entire system, plus its framework header. | `docs/user-manual/framework/configuring-fprime.md` |
| 90 | GDS | What are GDS "Functional Components"? | They are the core UI widgets used in custom dashboards, including `command-input` (sending commands), `command-history` (viewing sent commands), `event-list` (real-time logs), and `channel-table` (latest telemetry values). | `docs/user-manual/gds/gds-dashboard-reference.md` |
| 91 | Architecture | Describe the "CdhCore" base ID strategy. | In a subtopology like `CdhCore`, a `BASE_ID` constant is defined (e.g., `0xA0000000`). All components within the subtopology are then instantiated with an offset (e.g., `events base id BASE_ID + 0x1000`). This allows the entire subtopology to be relocated within the global ID space by changing one constant. | `docs/how-to/develop-subtopologies.md` |
| 92 | Modeling | How do you handle "Optional" port connections? | Use the `isConnected_<PortName>_OutputPort(portNum)` method in the component C++ code. This allows a component to safely check if a port is wired in the topology before invoking it, preventing an assertion failure if the connection is intended to be optional for certain deployments. | `docs/user-manual/framework/autocoded-functions.md` |
| 93 | Implementation | What is the purpose of `Fw::PolyType` `isXXX()` methods? | Since `PolyType` can store multiple primitives, `isU32()`, `isF32()`, etc., allow a component to dynamically inspect the type of data currently held in the container before casting or using it, preventing type-mismatch assertions at runtime. | `docs/user-manual/overview/05-enum-arr-ser.md` |
| 94 | Testing | How do you send a command with arguments using the `fprime_test_api`? | Use the `send_command` method with a list of strings: `fprime_test_api.send_command("MY_CMD", ["123", "3.14"])`. Note that all arguments must be passed as strings, as the API handles the conversion to the appropriate F´ types based on the dictionary. | `docs/user-manual/gds/gds-test-api-guide.md` |
| 95 | Coding Standards | Define NASA Rule #9: "Restricted Pointers." | Rule #9 restricts pointer usage to a maximum of one level of dereference (e.g., `int* p`). It forbids function pointers and discourages hiding dereference operations inside macros or complex typedefs, making data access patterns transparent and easier to analyze for memory safety. | `docs/user-manual/overview/fprime_conventions.md` |
| 96 | Build System | What is the `library_locations` field in `settings.ini`? | It is a colon-separated list of paths where `fprime-util` will search for external F´ libraries or components. This allows projects to share and reuse code that lives outside the main F´ framework or project repository. | `docs/user-manual/build-system/settings.md` |
| 97 | Troubleshooting | What is a "Circular Port Dependency"? | This occurs when a synchronous port chain eventually calls back into the originating component (e.g., A -> B -> A). This can lead to deadlocks (especially with `guarded` ports) or infinite recursion. Developers should use `async` ports or internal interfaces to break such cycles. | `docs/user-manual/overview/03-port-comp-top.md` |
| 98 | Implementation | How do you configure the stack size of an `active` component? | The stack size is passed as an argument to the `start()` method of the component, usually within the `Topology.cpp` file. It must be carefully sized based on the component's maximum call depth and local variable usage to prevent stack overflows in embedded environments. | `docs/user-manual/framework/building-topology.md` |
| 99 | GDS | How do you clear the event table in the GDS GUI? | Click the "Clear" button at the top of the "Events" tab. This empties the current browser-side display of events, allowing operators to focus on new incoming data without having to scroll through historical logs. | `docs/user-manual/overview/gds-introduction.md` |
| 100 | Modeling | What is the `FwEnumStoreType` used for in State Machines? | It is the underlying integer type (typically `I32`) used to store the current state ID. The autocoder uses this generic type in the base class to maintain state while allowing the specific `State` enum to remain type-safe in the developer's implementation. | `docs/user-manual/framework/state-machines.md` |
| 101 | Coding Standards | Define NASA Rule #2: "Fixed Loop Bounds." | All loops in flight software must have a trivially provable upper bound. This prevents infinite loops and allows static analysis tools to determine the maximum execution time of any function, which is critical for real-time determinism. | `docs/user-manual/overview/fprime_conventions.md` |
| 102 | OSAL | How does F´ abstract thread creation? | F´ uses the `Os::Task` class. On Linux, it wraps `pthread_create`, while on RTOSs like VxWorks, it might wrap `taskSpawn`. This allows the framework to start the execution thread of an `active` component using a single, portable API. | `Os/Task` |
| 103 | Implementation | What is the `SimpleObjRegistry`? | It is an optional framework utility (enabled by `FW_OBJECT_REGISTRATION`) that keeps a list of all instantiated F´ objects. It allows developers to dump a list of all components and ports in the system to the console, which is helpful for verifying that the topology was constructed correctly. | `docs/user-manual/framework/configuring-fprime.md` |
| 104 | Architecture | What are the constraints on State Machine initial transitions? | The initial transition (including entry actions) occurs during component initialization (`init()`), before ports are connected. Therefore, actions in the initial state **cannot** emit telemetry, events, or invoke output ports. Developers should transition to an "IDLE" state and wait for a signal from a RateGroup before performing such actions. | `docs/user-manual/framework/state-machines.md` |
| 105 | Build System | How do you run all unit tests in a project? | Navigate to the project root and run `fprime-util check --all`. This will recursively traverse all component directories, compile their unit tests, execute them, and report a summary of successes and failures. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 106 | GDS | Describe the `fprime-gds -g none` option. | This flag launches the GDS backend (ThreadedTCP server and data handlers) but suppresses the automatic opening of the browser-based GUI. It is commonly used when interacting with the system purely through the CLI or automated integration test scripts. | `docs/user-manual/overview/gds-introduction.md` |
| 107 | Modeling | What is a "Choice" state in an FPP State Machine? | A choice state acts as a branch point. When entered, it evaluates one or more guards. Based on the result of these guards, it immediately transitions to a specific destination state. It allows for complex conditional logic within the state machine's flow. | `docs/user-manual/framework/state-machines.md` |
| 108 | Implementation | Why does Mission Control use `aiofiles` for file I/O? | Mission Control is built on the Textual TUI framework, which runs an asynchronous event loop. Using standard blocking `open().read()` would freeze the UI while the disk is being accessed. `aiofiles` provides non-blocking I/O, ensuring the terminal remains responsive even when reading large F´ source files. | `docs/implementation/choices.md` |
| 109 | Architecture | How are Subtopologies registered in CMake? | Use the `register_fprime_module` function with the `INTERFACE` directive in the subtopology's `CMakeLists.txt`. This tells the build system that the module provides FPP files and headers for other topologies to import, but does not compile into a standalone binary itself. | `docs/how-to/develop-subtopologies.md` |
| 110 | Modeling | What does the `ref` keyword do in an FPP port definition? | The `ref` keyword indicates that an argument should be passed by **reference** in the generated C++ code (e.g., `MyType& arg`). This is more efficient than passing by value for large structs or serializables, as it avoids unnecessary memory copies. | `docs/user-manual/overview/03-port-comp-top.md` |
| 111 | Framework Services | Explain "Command Sequencing" in F´. | Command sequencing allows a predefined series of commands to be executed automatically. The `Svc::CmdSequencer` component reads a binary sequence file, sends each command to the `Svc::CmdDispatcher`, and waits for a completion status before proceeding to the next command, enabling autonomous operations. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 112 | Implementation | What is the `FW_OPTIONAL_NAME` macro? | It is a utility macro that returns the object's name if `FW_OBJECT_NAMES` is enabled, or an empty string if it is disabled. This allows developers to write code that supports naming for debugging while maintaining a minimal RAM footprint in production builds. | `docs/user-manual/framework/configuring-fprime.md` |
| 113 | GDS | How do you use the `fprime-cli` to view telemetry? | Run `fprime-cli channels`. This command will connect to the GDS and stream every telemetry update received from the flight software to the terminal in real-time. You can filter the output by component using the `-c` flag. | `docs/user-manual/gds/gds-cli.md` |
| 114 | Coding Standards | Define NASA Rule #5: "Assertion Density." | Rule #5 recommends an average of at least two assertions per function. Assertions should be used to verify preconditions, postconditions, and internal invariants. This practice helps catch software bugs early in the development cycle and provides documentation of expected behavior. | `docs/user-manual/overview/fprime_conventions.md` |
| 115 | Build System | What is the purpose of `fprime-util purge`? | The `purge` command completely removes the build cache directories (e.g., `build-fprime-automatic-native`). This is useful for "starting from scratch" when encountering strange build errors or after making significant changes to the toolchain or CMake configuration. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 116 | OSAL | Describe the `Os::Queue` class. | `Os::Queue` provides an abstraction for inter-thread message queues. It allows one thread to send data to another asynchronously. In F´, it is the fundamental mechanism used by `active` and `queued` components to receive port invocations and commands. | `Os/Queue` |
| 117 | Modeling | What is the "mnemonic" of a command? | The mnemonic is the human-readable string identifier for a command (e.g., `CMD_NO_OP`). In the GDS, commands are referenced by their mnemonic, which is prepended by the component instance name (e.g., `cmdDisp.CMD_NO_OP`) to ensure uniqueness. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 118 | Implementation | What is "Exact-Match" file replacement in Mission Control? | It is a safety feature of the `replace_in_file` tool. The AI must provide the *exact* literal string it wants to replace. If the string is not found exactly once in the target file, the tool fails. This prevents the AI from making accidental or ambiguous edits to the flight software codebase. | `docs/implementation/choices.md` |
| 119 | Architecture | How do Rate Groups handle "Slips"? | When a rate group exceeds its cycle time, it emits a `WARNING_HI` event. Internally, the rate group will attempt to catch up by skipping cycles or executing immediately if it is behind, but persistent slips indicate that the system's task loading needs to be re-evaluated. | `docs/user-manual/design-patterns/rate-group.md` |
| 120 | GDS | What is the `ThreadedTCP.log` file? | This log file is produced by the GDS and contains the output of the internal server that manages communication between the flight software and the GDS data handlers. It is the primary source for debugging connection issues or low-level protocol errors. | `docs/user-manual/overview/gds-introduction.md` |
| 121 | Modeling | Describe "Matched Ports" in FPP. | Matched ports (using the `match` keyword) are a model-level constraint ensuring that two port arrays are connected in parallel. For example, matching `pingIn` with `pingOut` ensures that if `pingIn[3]` is connected to a specific component, `pingOut[3]` must also be connected to that same component. | `docs/user-manual/design-patterns/common-port-patterns.md` |
| 122 | Implementation | What is the default LLM used by Mission Control? | Mission Control defaults to using `qwen3:8b` via a local Ollama instance. This model was chosen for its strong reasoning capabilities and performance on consumer-grade hardware, providing a good balance for F´ engineering tasks. | `docs/other/Ollama_install.md` |
| 123 | Testing | How do you verify that a unit test handler data didn't go out of scope? | Avoid allocating `Fw::Buffer` data on the stack within a unit test handler. Instead, use **persistent member variables** in the component implementation or the tester class. This prevents "stack-use-after-return" errors when the buffer is accessed later by the framework's asynchronous dispatch logic. | `docs/user-manual/overview/fprime_conventions.md` |
| 124 | Framework Services | What is the role of `Svc::PassiveConsoleTextLogger`? | This component receives text-formatted events via its `TextLog` port and prints them directly to the standard output (stdout). It is a simple way to provide a real-time log of flight software activities to a developer's terminal window. | `docs/user-manual/overview/source-tree.md` |
| 125 | Coding Standards | Define NASA Rule #7: "Check Returns & Parameters." | This rule requires that the return value of every non-void function be checked by the caller, and that every function must verify the validity of its input parameters before using them. This prevents errors from propagating silently through the system and ensures that functions fail fast when given invalid data. | `docs/user-manual/overview/fprime_conventions.md` |
| 126 | Coding Standards | Define NASA Rule #6: "Minimal Scope." | Rule #6 states that all data objects should be declared at the smallest possible level of scope. This reduces the risk of unintended data modification and makes the data flow of the program easier to track and reason about during code reviews. | `docs/user-manual/overview/fprime_conventions.md` |
| 127 | Coding Standards | Define NASA Rule #8: "Limited Preprocessor." | Rule #8 limits the use of the C preprocessor to simple header inclusions and basic macro definitions. It forbids complex macros, token pasting, and recursive macro calls, which can make code behavior opaque and difficult for static analysis tools to interpret. | `docs/user-manual/overview/fprime_conventions.md` |
| 128 | Coding Standards | Define NASA Rule #10: "Maximum levels of pointer dereference." | Rule #10 limits pointers to a single level of dereference (e.g., `*p`) and forbids the use of function pointers. This simplifies memory access patterns and ensures that pointer behavior remains predictable and verifiable by automated tools. | `docs/user-manual/overview/fprime_conventions.md` |
| 129 | Implementation | What is the "Three Strikes" rule in Mission Control? | To prevent infinite reasoning loops or repeated failures, Mission Control tracks consecutive tool execution errors. If the AI fails to execute a valid tool three times in a row (e.g., due to hallucinations or bad JSON), the agent aborts the current task and alerts the user. | `docs/planning/v0.01/07-Design-Error-Handling.md` |
| 130 | Architecture | What is the `library.cmake` file? | Every F´ library must have a `library.cmake` file at its root. It uses `add_fprime_subdirectory` calls to include the library's components, ports, and subtopologies in the project's build system, serving as the central integration point for reusable code. | `docs/how-to/develop-fprime-libraries.md` |
| 131 | GDS | Explain the difference between SELECTION and FEATURE plugins in the GDS. | **SELECTION** plugins allow users to choose one implementation from a set of alternatives (e.g., choosing a specific framing protocol). **FEATURE** plugins provide additive functionality that runs automatically or in parallel with the main GDS (e.g., a data logger or custom dashboard process). | `docs/how-to/develop-gds-plugins.md` |
| 132 | Implementation | How does Mission Control truncate large files for the AI? | The `execute_read_file` tool includes truncation logic. If a file is too large for the LLM's context window, it returns only the first portion of the file followed by a warning message, preventing the agent from being overwhelmed by massive source files or logs. | `docs/implementation/sdd.md` |
| 133 | Modeling | What is a "Frame Detector" in the communication stack? | A Frame Detector is a C++ helper class (not a component) used by `Svc::FrameAccumulator` to identify frame boundaries in a continuous stream of bytes (e.g., from a UART). It searches for sync patterns or validates headers to extract individual packets from the raw stream. | `docs/how-to/custom-framing.md` |
| 134 | Build System | What does the `fprime-util new --port` command do? | This command launches an interactive wizard that guides the developer through creating a new F´ port. It generates the `.fpp` definition, creates the directory structure, and automatically adds the new port to the deployment's build system. | `docs/user-manual/overview/development-practice.md` |
| 135 | OSAL | How does `Os::Task` handle task deletion? | `Os::Task` provides a virtual destructor and an `isStarted()` method. To delete a task, the framework typically signals the thread to exit (e.g., via a flag), waits for it to finish using `join()`, and then the OSAL wrapper handles the low-level cleanup of OS resources. | `Os/Task` |
| 136 | Framework Services | How do you configure the "Ping" timeout for a component? | Ping timeouts are defined in the topology's `PingEntries` namespace. For each component instance, you define `WARN` and `FATAL` constants representing the number of RateGroup ticks `Svc::Health` should wait before emitting a warning or fatal event. | `docs/user-manual/design-patterns/health-checking.md` |
| 137 | Implementation | What is the `Fw::MallocAllocator` used for? | It is a standard framework implementation of `Fw::MemAllocator` that wraps the system's `malloc()` and `free()` calls. It is typically used during system initialization to allocate memory for buffer pools or component-internal data structures. | `docs/user-manual/framework/dynamic-memory.md` |
| 138 | GDS | Describe the `fprime-gds --ip-address` flag. | This flag specifies the network address where the GDS communication server will listen for incoming connections from the flight software. By default, it listens on all interfaces (`0.0.0.0`), but it can be restricted to a specific IP for security or routing reasons. | `docs/user-manual/overview/gds-introduction.md` |
| 139 | Architecture | What is the `Svc::ComStub` component? | `Svc::ComStub` is a minimal component that implements the `Svc::Com` interface. It is used as a placeholder in topologies where a real radio or socket driver is not yet integrated, allowing the rest of the communication stack to be tested in isolation. | `docs/reference/communication-adapter-interface.md` |
| 140 | Modeling | How do you carry data with a State Machine signal? | In FPP, define the signal with a type: `signal mySignal(data: U32)`. When sending the signal from C++ using `mySm_sendSignal_mySignal(123)`, the data is serialized onto the component's queue and then passed to any action or guard handlers triggered by that signal. | `docs/user-manual/framework/state-machines.md` |
| 141 | Implementation | What is the purpose of `nomic-embed-text` in the RAG system? | It is the embedding model used by Mission Control's knowledge layer. It transforms F´ documentation and source code chunks into high-dimensional vectors (embeddings) that allow for semantic search based on mathematical similarity rather than just keyword matching. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 142 | GDS | How do you enable tab completion for the `fprime-cli`? | Tab completion is provided by the `argcomplete` Python package. Users must follow a one-time setup process (usually involving sourcing a bash script provided by the package) to enable the shell to dynamically query the GDS tools for available commands and arguments. | `docs/user-manual/gds/gds-dev-guide.md` |
| 143 | Build System | What is the `fprime-util hash-to-file` command used for? | When a flight software build uses `FW_FILEID_ASSERT`, the reported error only contains a numeric hash. This command takes that hash and looks it up in the project's assert database to return the original source file path, aiding in debugging production builds. | `docs/user-manual/framework/configuring-fprime.md` |
| 144 | OSAL | Describe the `Os::Mutex` "LockGuard" pattern. | Similar to `std::lock_guard`, F´ uses a RAII (Resource Acquisition Is Initialization) pattern for mutexes. A local object locks the mutex in its constructor and automatically unlocks it in its destructor when it goes out of scope, ensuring the mutex is always released even if an error occurs. | `Os/Mutex` |
| 145 | Framework Services | What is the "Time Context" used for in `Fw::Time`? | The Time Context is an 8-bit integer used to provide additional metadata about a timestamp (e.g., "polled from hardware" vs "estimated"). By default, it is not used in time comparisons but can be leveraged by project-specific logic to prioritize different time sources. | `docs/user-manual/framework/configuring-fprime.md` |
| 146 | Implementation | Why does Mission Control use a bash entry script? | The `fprime-tui` script ensures that the tool always launches in the correct directory. It saves the user's current path, locates the tool's own virtual environment, and then `cd`s back to the user's project directory before running the Python application, ensuring local environment discovery works correctly. | `docs/implementation/choices.md` |
| 147 | Architecture | What is "Reciprocal Rank Fusion" (RRF)? | RRF is a rank-merging algorithm that combines the results of multiple search methods (like keyword and semantic search). It calculates a score for each document based on its reciprocal rank in the input lists, effectively balancing the strengths of both retrieval techniques for better RAG accuracy. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 148 | Modeling | How do you define a constant in FPP? | Use the `constant` keyword: `constant MY_MAX_SIZE = 1024`. These constants can be used throughout the FPP model for sizing port arrays, queues, and serializable fields, ensuring that architectural limits are managed centrally and consistently. | `docs/reference/fpp-json-dict.md` |
| 149 | GDS | What is the `gds.ini` file? | The `gds.ini` file contains configuration settings for the GDS Flask web server, including the bind address and port. It allows users to customize the ground system's network interface without modifying the Python source code. | `docs/user-manual/overview/gds-introduction.md` |
| 150 | Implementation | Describe the "Semantic Chunker" in the RAG system. | The semantic chunker splits source documents into small, meaningful pieces (chunks) based on structural boundaries like Markdown headers (##) or FPP component blocks. This ensures that each chunk sent to the LLM contains a complete, coherent unit of information. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 151 | GDS | How do you filter available commands in the GDS "Mnemonic" dropdown? | The dropdown box is searchable. Operators can type any part of the component instance name or command mnemonic into the text field, and the list will dynamically filter to show only matching commands, enabling rapid selection in complex systems. | `docs/user-manual/overview/gds-introduction.md` |
| 152 | Framework Services | Describe the `TB_DONT_CARE` time base. | `TB_DONT_CARE` is a special time base value (typically `0xFFFF`) used in command sequences. It instructs the `Svc::CmdSequencer` to execute a command regardless of the flight software's current time synchronization status, which is useful for boot-up or emergency recovery tasks. | `docs/user-manual/gds/seqgen.md` |
| 153 | Implementation | What is the `FW_OBJ_SIMPLE_REG_ENTRIES` macro? | This macro defines the maximum number of objects (components and ports) that the `SimpleObjRegistry` can track. The default value is `500`. In very large topologies, this value may need to be increased to ensure all system objects are included in the registry's dump. | `docs/user-manual/framework/configuring-fprime.md` |
| 154 | Architecture | What are the core methods of a custom "Framing Protocol"? | A custom protocol must implement `frame()` (wrapping payload data with headers/trailers) and `deframe()` (extracting payloads from raw byte streams using a circular buffer). These methods are called by the `Svc::Framer` and `Svc::Deframer` components respectively. | `docs/user-manual/framework/ground-interface.md` |
| 155 | GDS | What does the `fprime-gds --tts-port` flag configure? | It sets the port for the ThreadedTCP server (default `50050`). This server acts as the internal data backbone of the GDS, and changing the port allows multiple GDS instances to run on the same host without network collisions. | `docs/user-manual/overview/gds-introduction.md` |
| 156 | Modeling | Describe the `Fw.CompletionStatus` port type. | `Fw.CompletionStatus` is a framework port type used to signal the end of an asynchronous operation. It typically carries an enum value (e.g., `COMPLETED`, `CANCELED`, `ERROR`) to inform the requesting component of the final outcome of a background task. | `docs/user-manual/design-patterns/manager-worker.md` |
| 157 | Implementation | How does the RAG retriever handle "Top K" candidates? | The retriever first fetches `TOP_K=8` candidates using vector search and another 8 using keyword search. These 16 candidates are then merged using Reciprocal Rank Fusion, and only the top `FINAL_K=3` most relevant chunks are ultimately injected into the LLM's prompt. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 158 | Build System | What is the purpose of `fprime-util generate --ut`? | This flag instructs the build system to generate a cache specifically for unit tests. It enables test-specific configurations, links against the GTest framework, and includes any source files or stubs defined in `UT_SOURCE_FILES` within the module's `CMakeLists.txt`. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 159 | OSAL | What are the possible return statuses of `Os::File::read()`? | `Os::File` methods return an `Os::File::Status` enum. Common values include `OP_OK` (success), `DOES_NOT_EXIST`, `NO_SPACE`, `PERMISSION_DENIED`, and `OTHER_ERROR`. Developers must check these statuses to ensure robust file I/O handling. | `Os/File` |
| 160 | GDS | How do you "Stage" a file for uplink in the GDS? | In the "Uplink" tab, use the file browser to select files. This adds them to a staging list where you can review their destination paths and order. Staging allows you to curate a set of files before clicking "Submit Uplink" to begin the actual transfer. | `docs/user-manual/overview/gds-introduction.md` |
| 161 | Modeling | What is a State Machine "Signal" without data? | It is a simple event trigger defined as `signal mySignal`. In C++, it is invoked using a no-argument method like `mySm_sendSignal_mySignal()`. It is used to drive transitions or actions that depend only on the occurrence of an event, such as a "startup complete" notification. | `docs/how-to/define-state-machines.md` |
| 162 | Implementation | Describe the SHA-256 deduplication in the RAG indexer. | During indexing, the script calculates a SHA-256 hash of each text chunk's content. If multiple documents (e.g., a README appearing in two different repos) produce the same chunk, only one instance is stored in ChromaDB, preventing redundant information from wasting the LLM's context. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 163 | Architecture | What is the "Byte Stream Driver Model" interface? | It is a standard set of ports (`ready`, `send`, `recv`, `poll`) that all F´ hardware drivers should implement. It provides a uniform way for the communication stack to interact with any serial-like hardware (UART, TCP, Radio), ensuring that the `Svc::Framer` and `Svc::Deframer` remain hardware-agnostic. | `docs/user-manual/framework/ground-interface.md` |
| 164 | Framework Services | Explain "Double-Buffering" in `Svc::TlmChan`. | `Svc::TlmChan` maintains two internal arrays of telemetry data. One array is "active" and receives real-time updates from components. When a downlink cycle occurs, the manager swaps the arrays, allowing the ground system to read from one stable "snapshot" while updates continue seamlessly in the other. | `docs/user-manual/overview/source-tree.md` |
| 165 | Implementation | How does Mission Control discover the project root? | It uses the `find_fprime_venv()` helper, which starts at the current working directory and traverses upwards through parent folders until it identifies a directory containing the `fprime-venv` folder, which serves as the marker for an F´ project root. | `docs/implementation/sdd.md` |
| 166 | GDS | What is a GDS "SELECTION" plugin? | A Selection plugin allows the user to choose one specific implementation from multiple choices for a core GDS function. For example, selecting the "CCSDS" protocol for framing or the "TCP" adapter for communication. Only one selection plugin per category can be active at a time. | `docs/how-to/develop-gds-plugins.md` |
| 167 | Modeling | How do you specify an "Absolute" command opcode? | By default, opcodes are relative to the component base ID. However, if a project requires a fixed, non-relative opcode, it can be defined in the FPP, and the developer must ensure no collisions occur manually across the entire deployment topology. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 168 | Troubleshooting | What is a "Rate Group Overrun"? | Also known as a "Slip," this occurs when the work scheduled in a rate group takes longer than the allotted time slice. It is often caused by blocking I/O, heavy computation, or too many components being attached to a single high-frequency rate group. | `docs/user-manual/design-patterns/rate-group.md` |
| 169 | Implementation | What is the `Fw::Buffer` "context" for in Mission Control? | In the `execute_read_file` tool, if a file is truncated, the tool might store truncation metadata in the buffer's context (or return it in the result string) so the AI knows it is only seeing a partial view of the file. | `docs/implementation/tools.py` |
| 170 | Build System | What does the `fprime-util check --all` flag do? | It recursively runs the `fprime-util check` command in every module and component directory within the project. This is the standard way to verify that all unit tests across the entire codebase are passing before a major release or commit. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 171 | OSAL | How does `Os::Task` handle task priority? | Task priority is an integer value (0-255) passed to the `start()` method. The OSAL translates this into platform-specific values (e.g., 0-99 for Linux SCHED_FIFO). F´ assumes that higher numbers represent higher priority, though this is normalized by the underlying OSAL implementation. | `Os/Task` |
| 172 | GDS | Describe the `fprime-gds --ip-port` flag. | This flag sets the port number (default `50000`) where the GDS will listen for incoming socket connections from the flight software. It must match the port configured in the flight software's `SocketIpDriver` or `TcpClient` component. | `docs/user-manual/overview/gds-introduction.md` |
| 173 | Modeling | What is the difference between a "sync" and a "guarded" port? | Both are synchronous and run on the caller's thread. However, a **guarded** port automatically locks a component-wide mutex before executing the handler, while a **sync** port does not. Use "guarded" when the handler modifies shared internal state that must be protected from other threads. | `docs/user-manual/overview/03-port-comp-top.md` |
| 174 | Implementation | What is the `RRF_K` constant in Reciprocal Rank Fusion? | `RRF_K` is a smoothing constant (defaulting to `60` in the Mission Control retriever) that prevents high-ranking documents from completely dominating the score, ensuring a more balanced combination of dense and sparse search results. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 175 | Troubleshooting | How do you recover the original source path from a hashed `FW_ASSERT` message? | By running `fprime-util hash-to-file <HASH_VALUE>` in the deployment terminal. The build system maintains a mapping of source file hashes to their full paths to save binary space while still allowing developers to locate assertion failures. | `docs/user-manual/framework/configuring-fprime.md` |
| 176 | Architecture | What is the `Svc::BufferManager` "Bin" configuration? | In the `setup()` method, developers provide a table of bins. Each bin specifies a buffer size and the number of buffers of that size to allocate. The bins must be ordered by increasing size, allowing the manager to efficiently find the smallest available buffer that satisfies a client's request. | `docs/user-manual/framework/dynamic-memory.md` |
| 177 | Modeling | How do you define a `string` type with a maximum size in FPP? | Use the `string` keyword with the `size` specifier: `type MyString = string size 80`. This creates a fixed-capacity string container in C++ that prevents buffer overflows by enforcing the 80-character limit during assignment and serialization. | `docs/reference/fpp-json-dict.md` |
| 178 | GDS | Describe the "Data Products" dictionary format. | Data products are defined in the FPP JSON dictionary under `records` and `containers`. Each entry includes a unique `id`, a `type` descriptor, and metadata like `defaultPriority`, allowing the GDS to correctly parse and prioritize science data packets downlinked from the system. | `docs/reference/fpp-json-dict.md` |
| 179 | Build System | How do you add an external non-F´ library to a component? | In the component's `CMakeLists.txt`, add the library to the `DEPENDS` list of the `register_fprime_module` call. You may also need to use `target_include_directories` to point to the external library's headers, ensuring the compiler can resolve non-F´ dependencies. | `docs/how-to/integrate-external-libraries.md` |
| 180 | Implementation | What is the `FW_ASSERT_LEVEL` macro? | It controls the verbosity of assertion failures. `FW_NO_ASSERT` removes all check code; `FW_FILEID_ASSERT` reports a numeric hash of the filename; and `FW_FILENAME_ASSERT` reports the full `__FILE__` string. This allows projects to balance debuggability with code space constraints. | `docs/user-manual/framework/configuring-fprime.md` |
| 181 | GDS | How does the `fprime-gds` find its dictionary by default? | The GDS automatically searches the current working directory for any file ending in `Dictionary.json`. It uses the first one it finds to populate the UI with the commands, events, and telemetry channels specific to that deployment. | `docs/user-manual/overview/gds-introduction.md` |
| 182 | OSAL | What is the difference between `Os::Task::join()` and `Os::Task::suspend()`? | `join()` blocks the calling thread until the target task has completely finished execution. `suspend()` (where supported by the OS) pauses the execution of the task without terminating it. In F´, `join()` is the standard way to ensure clean task shutdown. | `Os/Task` |
| 183 | Modeling | What is an FPP "Module"? | A module is a namespacing construct used to organize FPP definitions (components, ports, types). In C++, it translates into a `namespace` block, preventing naming collisions between different subsystems or libraries (e.g., `Svc` vs `Drv`). | `docs/user-manual/overview/source-tree.md` |
| 184 | Implementation | Describe the `Fw::Time` math operations. | The `Fw::Time` class supports overloaded operators for addition, subtraction, and comparison. You can add or subtract durations (represented as floating-point seconds) to a timestamp, and compare two timestamps to determine which event occurred first, provided they share the same Time Base. | `docs/user-manual/gds/gds-test-api-guide.md` |
| 185 | Architecture | What is the "Passive Rate Group" component? | Unlike the active version, a `PassiveRateGroup` does not have its own thread. It executes its attached components synchronously within the execution context of the component that triggers its `run` port, offering lower latency and overhead for simple periodic tasks. | `docs/user-manual/overview/source-tree.md` |
| 186 | Testing | How do you check for a "Command Response" in an integration test? | Use the `fprime_test_api.send_and_assert_command()` method. This will send the command and then automatically wait for the corresponding `CommandResponse` event from the dispatcher, raising an assertion error if the command fails or times out. | `docs/user-manual/gds/gds-test-api-guide.md` |
| 187 | Framework Services | What is the `Svc::TlmPacketizer`? | It is an alternative to `Svc::TlmChan`. Instead of sending individual channels, it groups telemetry items into fixed-format packets based on a "Packet Specification" file. This reduces header overhead and is more efficient for high-frequency data transmission over narrow-band links. | `docs/user-manual/design-patterns/subtopologies.md` |
| 188 | Implementation | How does the Mission Control "Orchestrator" handle AI tokens? | The orchestrator (in `app.py`) captures chunks of text as they are streamed from Ollama. It looks for specific patterns (like JSON blocks) and can suspend the stream to execute a tool or wait for user approval before allowing the AI to continue generating. | `docs/implementation/sdd.md` |
| 189 | GDS | What is the `config.js` file in the GDS? | It is a JavaScript configuration file for the GDS web frontend. It allows users to customize UI behaviors, such as the timeout for the "Orb" connection indicator, the default logo, and the refresh rate of telemetry tables. | `docs/user-manual/overview/gds-introduction.md` |
| 190 | Modeling | How do you define a `bool` type in a serializable struct? | Simply use the `bool` keyword: `struct MyStatus { isReady: bool }`. The autocoder will generate a C++ struct where the boolean is stored as a single byte and serialized using the framework's architecture-independent boolean format. | `docs/reference/fpp-json-dict.md` |
| 191 | Build System | What is the `project.cmake` file? | This file is the entry point for an F´ project's CMake build. It typically includes the F´ framework's CMake logic (`include(fprime/project.cmake)`) and uses `add_fprime_subdirectory` to include all the deployments and libraries that make up the project. | `docs/user-manual/build-system/01-cmake-intro.md` |
| 192 | OSAL | What does `Os::Queue::receive()` status `RECV_EMPTY` mean? | This status is returned when a non-blocking read is attempted on an empty queue. In F´ thread virtualization, this is used to allow a component to "poll" its queue for work without blocking the single execution thread of the baremetal system. | `docs/user-manual/framework/baremetal-multicore.md` |
| 193 | Architecture | Explain the "Passive Component" stack usage. | Since passive components run on the caller's thread, their stack usage is added to the caller's stack. Developers must ensure that the thread starting the invocation chain (e.g., an `ActiveRateGroup`) has a large enough stack to accommodate the deep call trees of all its passive children. | `docs/user-manual/overview/03-port-comp-top.md` |
| 194 | Implementation | Describe the `TARGET_TOKENS` constant in the RAG chunker. | In `TUI/rag/chunker.py`, `TARGET_TOKENS=300` defines the ideal size for a text chunk. Sections are truncated or grouped to stay near this limit, ensuring that retrieved context is long enough to be useful but short enough to fit multiple chunks into the LLM's prompt. | `docs/planning/v0.02/2026-03-13-fprime-tui-rag-implementation-plan.md` |
| 195 | GDS | How do you use the GDS "Logs" tab? | The Logs tab provides a real-time view of files produced by the GDS server, such as `event.log` and `command.log`. Operators can select a log from the sidebar to monitor the raw data stream entering and leaving the ground system without checking the filesystem. | `docs/user-manual/overview/gds-introduction.md` |
| 196 | Modeling | What is the difference between an `async` and `guarded` command? | An **async** command is placed on the component's queue and handled later by its own thread. A **guarded** command is handled immediately by the thread of the command dispatcher, but it is protected by a mutex to ensure thread-safe access to the component's state. | `docs/user-manual/overview/04-cmd-evt-chn-prm.md` |
| 197 | Build System | How do you specify a custom toolchain in `fprime-util`? | Pass the toolchain name as an argument: `fprime-util generate my-custom-toolchain`. This will cause CMake to load the corresponding `.cmake` file from the project's or framework's toolchain directory, configuring the build for a specific compiler and target OS. | `docs/user-manual/build-system/cmake-toolchains.md` |
| 198 | Troubleshooting | What is a "Port Serialization Error"? | This occurs when data sent to a port cannot be correctly packed into a buffer, often due to a size mismatch (e.g., a string exceeding its defined capacity) or a failure in a custom `serializeTo()` method. It usually results in a runtime assertion. | `docs/user-manual/overview/03-port-comp-top.md` |
| 199 | Implementation | What is the `FPrimeAIClient` "System Prompt" for? | It is a strict instruction set passed to Ollama that defines the agent's persona (Senior Principal Engineer), its technical rules (JPL standards), and the JSON schema for every tool it is allowed to use, serving as the "laws" of the autonomous agent. | `docs/implementation/sdd.md` |
| 200 | Architecture | Describe the "Hub Pattern" bridge. | The Hub pattern bridge is the physical medium (TCP, Serial, Spacecraft Bus) that connects two hubs. The hubs serialize port calls into "Com packets" that are agnostic to the bridge type, allowing F´ components to communicate across any hardware layer without modification. | `docs/user-manual/design-patterns/hub-pattern.md` |
| 201 | Architecture | What are the `Fpp::ToCpp::Phases`? | They are an enumeration defining the stages of system-wide initialization and teardown, including `configConstants`, `initComponents`, `configComponents`, `regCommands`, `readParameters`, `loadParameters`, `startTasks`, and `tearDownComponents`. | `fprime/Fpp/ToCpp.fpp` |
| 202 | Architecture | Describe the purpose of the `Drv::AsyncByteStreamBufferAdapter`. | This passive component mediates between the `AsyncByteStreamDriver` interface and the `PassiveBufferDriver` interface. It allows components designed for simple buffer handling to interact with asynchronous drivers that use completion callbacks. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.fpp` |
| 203 | Framework Services | What is the buffer ownership rule for `Drv::ByteStreamStatus::SEND_RETRY`? | When a send operation returns `SEND_RETRY`, the caller **retains ownership** of the `Fw::Buffer`. The data was not transmitted, and the caller is responsible for attempting the send again or handling the failure. | `fprime/Drv/ByteStreamDriverModel/docs/sdd.md` |
| 204 | Implementation | How do you configure an `Drv::IpSocket`? | Use the `configure()` method to set the hostname (IP address), port number, and send timeouts. For UDP, separate `configureSend()` and `configureRecv()` calls are required to set up unidirectional or bidirectional communication. | `fprime/Drv/Ip/IpSocket.hpp` |
| 205 | Implementation | Explain the difference between `startup()` and `openProtocol()` in `Drv::TcpServerSocket`. | `startup()` creates the listening socket and binds it to a local port. `openProtocol()` (which implements the `IpSocket::open()` interface) calls `accept()` to wait for and establish a connection with a single incoming client. | `fprime/Drv/Ip/TcpServerSocket.cpp` |
| 206 | Implementation | How are ephemeral ports supported in `Drv::UdpSocket`? | By passing `0` as the port number to `configureRecv()` or `configureSend()`. The OS will assign an available port during `bind()`, which can then be retrieved via `getRecvPort()` after the socket is opened. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 207 | Architecture | What is the `Drv::SocketComponentHelper` used for? | It is a base class that provides a standardized receive loop (`readTask`) and an automatic reconnection loop (`reconnectTask`) for components wrapping IP sockets, ensuring robust background communication handling. | `fprime/Drv/Ip/docs/sdd.md` |
| 208 | Implementation | What is the output format of the autogenerated `toString()` for FPP arrays? | It produces a string representation of the array elements wrapped in square brackets, for example: `[ val1, val2, val3 ]`. Each element uses its own type-specific `toString()` implementation. | `fprime/FppTestProject/FppTest/array/ArrayToStringTest.cpp` |
| 209 | Implementation | How does the FPP `enum::isValid()` handle intervals? | The autocoder generates a series of range checks for enums with explicitly defined values (e.g., `A=0, B=10, C=11`). `isValid()` returns `true` only if the underlying numeric value falls within one of these defined intervals. | `fprime/FppTestProject/FppTest/enum/EnumToStringTest.cpp` |
| 210 | Modeling | Describe a "Choice-to-Choice" transition in a State Machine. | It is a transition where the target of a branch in a `choice` block is another `choice` block. The state machine evaluates the guards of the first choice, transitions, and then immediately evaluates the guards of the second choice before landing in a final state. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/ChoiceToChoice.cpp` |
| 211 | Implementation | What is `Fw::InternalInterfaceString`? | It is a safe string type used for arguments in "Internal Interfaces." Like other F´ strings, it has a fixed capacity (default `40` characters) to ensure deterministic memory usage during asynchronous self-dispatching. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.hpp` |
| 212 | Implementation | How is the binary packet size calculated for a Data Product container? | Using `DpContainer::getPacketSizeForDataSize(dataSize)`. This calculates the total buffer requirement by adding the constant framework header size (`Fw::DpContainer::MIN_PACKET_SIZE`) to the requested user data size. | `fprime/FppTestProject/FppTest/dp/DpTest.hpp` |
| 213 | Implementation | What does `Fw::FW_DESERIALIZE_BUFFER_EMPTY` indicate? | This status is returned during deserialization when an attempt is made to read data from a buffer that has no bytes remaining. In FPP tests, it is used to verify that serial port handlers correctly detect truncated or malformed messages. | `fprime/FppTestProject/FppTest/component/tests/TesterHelpers.cpp` |
| 214 | Implementation | List the supported modes for `Drv::LinuxGpioDriver`. | Supported modes include `GPIO_OUTPUT` (direct writing), `GPIO_INPUT` (direct reading), and three interrupt modes: `GPIO_INTERRUPT_RISING_EDGE`, `GPIO_INTERRUPT_FALLING_EDGE`, and `GPIO_INTERRUPT_BOTH_RISING_AND_FALLING_EDGES`. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.hpp` |
| 215 | Implementation | How does `Drv::LinuxSpiDriver` handle configuration mismatches? | After writing configuration values (like `MODE` or `BITS_PER_WORD`) via `ioctl()`, the driver reads the values back from the kernel. If they do not match the requested values, it emits an `SPI_ConfigMismatch` warning event. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 216 | Implementation | What is the baud rate range for `Drv::LinuxUartDriver`? | On Linux, it supports standard rates from `BAUD_9600` up to high-speed rates like `BAUD_4000K` (4,000,000 baud), depending on the specific hardware capabilities and kernel support. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.hpp` |
| 217 | Implementation | Describe the `Drv::DataBuffer` class in `Drv/Ports/DataTypes`. | It is a fixed-capacity serialization buffer (default `256` bytes) that inherits from `Fw::SerializeBufferBase`. It is used as a generic container for passing raw byte data across ports when `Fw::Buffer` is not required. | `fprime/Drv/Ports/DataTypes/DataBuffer.hpp` |
| 218 | Implementation | What is the difference between `Fw::LinearBufferBase` and `Fw::SerialBufferBase`? | `Fw::LinearBufferBase` provides an interface for memory regions that can be accessed as linear arrays. `Fw::SerialBufferBase` extends this by providing stateful `serialize()` and `deserialize()` operations, tracking the current position within the buffer. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.cpp` |
| 219 | Testing | What are `productGetHistory` and `productRequestHistory`? | They are history data structures in `TesterBase` that record every allocation request made by a producer component via "product get" or "product request" ports during a unit test. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.hpp` |
| 220 | Testing | Describe the `ASSERT_PRODUCT_SEND` GTest macro. | It is used to verify a Data Product transmission. It checks the container ID, priority, time tag, processor type, and user data, then deserializes the header and provides the remaining payload buffer for further verification. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.hpp` |
| 221 | Implementation | What is the `__FPRIME_AC_INITIAL_TRANSITION` signal? | It is a special internal signal used by the state machine autocoder to trigger the initial transition when the component is first initialized. It allows the machine to enter its `initial` state without an external signal. | `fprime/FppTestProject/FppTest/state_machine/internal/initial/Basic.cpp` |
| 222 | Build System | What is the purpose of `register_fprime_config` in `fprime/Fpp`? | It registers the `FppToCppSettings` module, which provides the FPP definitions (like the `Phases` enum) used by the autocoder to generate system-wide configuration code. | `fprime/Fpp/CMakeLists.txt` |
| 223 | Implementation | What fields are in the `Drv::SocketDescriptor` struct? | It contains `fd` (the primary communication file descriptor) and `serverFd` (the listening file descriptor used only by server-type sockets). | `fprime/Drv/Ip/IpSocket.hpp` |
| 224 | Implementation | How does `Drv::UdpSocket` handle zero-length datagrams? | It explicitly checks if the size is 0 and calls `sendProtocol()`/`recvProtocol()` accordingly. In UDP, a zero-length datagram is a valid, successful transmission, not an error or disconnection. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 225 | Modeling | How are FPP format strings used in array definitions? | They allow developers to customize the `toString()` output. Using specifiers like `{d}` (decimal), `{o}` (octal), `{x}` (hex), or `{.2f}` (fixed-point float) in the `format` attribute dictates how each array element is rendered. | `fprime/FppTestProject/FppTest/array/format.fpp` |
| 226 | Modeling | Describe "Behavioral Polymorphism" in FPP State Machines. | It allows a child state to override a transition defined in its parent state for the same signal. If the state machine is in the child state and receives that signal, the child's specific transition is executed instead of the parent's generic one. | `fprime/FppTestProject/FppTest/state_machine/internal/state/Polymorphism.fpp` |
| 227 | Implementation | What is the difference between a self-transition and an internal transition? | A **self-transition** (`on s enter S`) causes the state to exit and then re-enter, executing both `exit` and `entry` actions. An **internal transition** (`on s do { a }`) executes an action without leaving the state, thus preserving the current state and skipping `exit`/`entry` logic. | `fprime/FppTestProject/FppTest/state_machine/internal/state/BasicSelf.fpp` |
| 228 | Framework Services | How do you implement "Event Throttling" in FPP? | Use the `throttle` keyword in the event definition (e.g., `throttle 10`). This limits the number of times an event is emitted within a specific window. The count can be reset manually using the autogenerated `log_SEVERITY_EventName_ThrottleClear()` method. | `fprime/FppTestProject/FppTest/component/include/events.fppi` |
| 229 | Implementation | What is an "External" state machine in F´? | It is a state machine defined using external tools (like PlantUML or STARS) rather than FPP syntax. The FPP model references the external definition, and the STARS autocoder generates the C++ implementation, which is then instantiated within an F´ component. | `fprime/FppTestProject/FppTest/state_machine/external_instance/SmTest.fpp` |
| 230 | Architecture | What is the role of the `Drv::ByteStreamReady` port? | It is a signal emitted by a byte stream driver (like `TcpClient`) to inform its clients that the hardware interface is successfully opened and ready to transmit or receive data. Clients often use this to enable their own communication logic. | `fprime/Drv/ByteStreamDriverModel/ByteStreamDriverModel.fpp` |
| 231 | Implementation | How do you handle asynchronous self-dispatching in a component? | By defining an `internal port`. Calling the `internalInterfaceInvoke()` method places a message on the component's own queue. When dispatched, it triggers a local handler, allowing the component to perform work on its own thread at a later time. | `fprime/FppTestProject/FppTest/component/include/internal_ports.fppi` |
| 232 | Implementation | Describe the `Fw::ParamExternalDelegate` class. | It is an interface used to manage parameters stored outside the component's base class (e.g., in a specialized hardware register). Developers implement `serializeParam()` and `deserializeParam()` to bridge the F´ parameter system with external storage. | `fprime/FppTestProject/FppTest/component/active/test/ut/ActiveTestTester.hpp` |
| 233 | Testing | How can you find a free TCP port for a unit test? | By calling `socket()` and then `bind()` with port `0`. The OS will assign a random available ephemeral port. The test can then use `getsockname()` to retrieve the assigned port number before closing the socket and starting the actual test server. | `fprime/FppTestProject/FppTest/utils/Utils.cpp` |
| 234 | Architecture | What is the "PassiveAsyncByteStreamDriverClient" interface? | It is an interface for components that act as passive clients to an *asynchronous* driver. It defines output ports for sending data and input ports for receiving completion callbacks and status from the driver. | `fprime/Drv/Interfaces/PassiveAsyncByteStreamDriverClient.fpp` |
| 235 | Implementation | How does `Drv::LinuxGpioDriver` handle interrupts? | It uses a dedicated thread (`pollLoop`) that calls the Linux `poll()` system call on the GPIO line's file descriptor. When an edge transition is detected, the thread emits a signal via the `gpioInterrupt` output port with a high-resolution timestamp. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 236 | Implementation | What is the `SPI_RDWR` ioctl used for in `LinuxSpiDriver`? | It is used to perform a full-duplex synchronous SPI transaction. It sends a buffer of data and simultaneously reads a buffer of the same size in a single kernel-level operation, ensuring the highest possible timing accuracy. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 237 | GDS | How do you define a "Choice" branch in a GDS sequence? | Choices are not directly supported in the `.seq` file format (which is linear). However, the `Svc::CmdSequencer` can execute different sequence files based on the status of previous commands or ground-sent branch commands. | `docs/user-manual/gds/seqgen.md` |
| 238 | Modeling | What does the `assert` keyword mean when used on an FPP queue? | It specifies the "Queue Full" behavior. If an attempt is made to place a message on a full queue, the software will trigger a runtime assertion (halting execution) instead of dropping the message or blocking the caller. | `fprime/FppTestProject/FppTest/interfaces/serial_ports_async.fpp` |
| 239 | Modeling | Can an FPP `struct` have members that are also structs? | Yes. FPP supports nested data structures. A `struct` (serializable) can include fields of any other defined `struct`, `array`, or `enum` type, allowing for complex, hierarchical data modeling. | `fprime/FppTestProject/FppTest/struct/struct.fpp` |
| 240 | Implementation | What is the purpose of `Drv::SocketDescriptor::serverFd`? | It stores the file descriptor of the *listening* socket in a TCP server. This is distinct from the `fd` field, which stores the descriptor of the active *connection* with a specific client. | `fprime/Drv/Ip/IpSocket.hpp` |
| 241 | Implementation | Describe the "Ordered Shutdown" behavior in `TcpServerSocket`. | When `recv()` returns `0`, it indicates that the client has performed a clean, orderly shutdown of the TCP connection. The server handles this by transitioning to a `SOCK_DISCONNECTED` state and releasing the client-specific file descriptor. | `fprime/Drv/Ip/IpSocket.cpp` |
| 242 | Testing | What is the `SmHarness::History` template used for? | It is a utility class used in state machine unit tests to record a sequence of events (signals or actions). It provides methods to `push()` items and check `getSize()`, allowing the test to verify the exact order of state transitions. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/History.hpp` |
| 243 | Modeling | How do you define a "Choice" state that evaluates a U32 value? | In FPP, define a choice with a typed guard: `choice C { if g enter S1 else enter S2 }` where `guard g: U32`. The state machine will pass the signal's U32 data to the `guard_g` implementation to decide the branch. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/BasicU32.hpp` |
| 244 | Implementation | What is `Fw::LinearBufferBase`? | It is a base class representing a continuous region of memory. It provides basic pointers and size information but lacks the high-level serialization logic of `Fw::SerialBuffer`, making it more suitable for low-level DMA or hardware buffer operations. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.cpp` |
| 245 | Architecture | Explain the "PassiveBufferDriver" interface. | It is a simplified driver interface that uses `Fw::Buffer` objects for both input and output. It allows components to exchange raw byte chunks synchronously or asynchronously using standard buffer allocation and return patterns. | `fprime/Drv/Interfaces/PassiveBufferDriver.fpp` |
| 246 | Implementation | How does `Drv::UdpSocket` support "Ephemeral Send"? | By setting the destination port to `0` in `configureSend()`. The socket will then dynamically set its send destination to the IP and port of the *first* remote packet it receives, enabling "reply-to" behavior without pre-configuring the client's port. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 247 | Testing | Describe the `ASSERT_TLM_ChannelName_SIZE` macro. | It is a GTest macro generated for every telemetry channel. It verifies the total number of updates received for that specific channel during a unit test, ensuring the component is measurement-reporting at the expected frequency. | `docs/user-manual/overview/unit-testing.md` |
| 248 | Modeling | What is the `every` keyword in an FPP event definition? | It specifies a periodic re-transmission of a throttled event (e.g., `every {seconds = 5}`). After the throttle limit is reached, the event will only be emitted once every 5 seconds, preventing "log spam" while still providing periodic updates. | `fprime/FppTestProject/FppTest/component/include/events.fppi` |
| 249 | Implementation | What does `FW_ASSERT_NO_OVERFLOW(val, type)` do? | It is a safety macro that checks if a numeric value `val` can be safely cast to the target `type` without loss of data or overflow. It is extensively used in drivers to validate that user-provided sizes fit within kernel-level integer limits. | `fprime/Drv/LinuxI2cDriver/LinuxI2cDriver.cpp` |
| 250 | Architecture | What is the "Subtopology Autocoder"? | It is part of the FPP toolchain that parses subtopology definitions (`.fpp` files containing instances and connections) and generates C++ headers and source files that allow these groupings to be easily integrated into a larger project topology. | `docs/how-to/develop-subtopologies.md` |
| 251 | Architecture | Describe the `initComponents` and `configComponents` phases in `Fpp::ToCpp::Phases`. | `initComponents` is the phase where component base classes are initialized (setting up queues and internal state). `configComponents` is the subsequent phase where developers perform instance-specific configuration (like opening device paths or setting constants) before the tasks are started. | `fprime/Fpp/ToCpp.fpp` |
| 252 | Implementation | What is the purpose of the `O_NOCTTY` flag in the `LinuxUartDriver`? | This flag prevents the UART device from becoming the "controlling terminal" for the process. This ensures that keyboard signals (like `Ctrl+C`) or other terminal-specific input from the serial line do not accidentally affect the flight software process. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 253 | Implementation | How does `LinuxGpioDriver` translate Linux `errno` to F´ statuses? | It uses helper functions like `errno_to_gpio_status()`. For example, `EBADF` is translated to `NOT_OPENED`, and `ENXIO` to `INVALID_MODE`. This mapping allows the low-level OS errors to be reported as type-safe F´ enums across component boundaries. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 254 | Modeling | What are "Hierarchical State Machines" in FPP? | They are state machines where a state can contain nested sub-states. Transitions can happen between sub-states, or from a sub-state back to a parent state. This allows for complex behavior modeling where child states inherit the properties and transitions of their parents. | `fprime/FppTestProject/FppTest/state_machine/internal/state/Internal.fpp` |
| 255 | Implementation | Describe the `Fw::ParamBuffer` used in parameter handlers. | `Fw::ParamBuffer` is a fixed-capacity buffer used to store a serialized parameter value. During a `prmGet` call, the component serializes its local parameter into this buffer; during a `prmSet` call, it deserializes the new value from the buffer into its internal state. | `fprime/FppTestProject/FppTest/component/active/test/ut/ActiveTestTester.cpp` |
| 256 | Architecture | What is the difference between `AsyncByteStreamBufferAdapter` and `ByteStreamBufferAdapter`? | The **Async** version is designed to work with drivers that return status asynchronously via a callback (`toByteStreamDriverReturn`), while the **standard** version is for synchronous drivers that return status immediately from the `send` port invocation. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.fpp` |
| 257 | Implementation | How is the "Ordered List" of array elements formatted in `toString()`? | The autogenerated code iterates through the array, calling the `toString()` method of each element. Elements are comma-separated and the entire list is wrapped in `[ ]` (e.g., `[ 1, 2, 3 ]`), ensuring a consistent and readable log format. | `fprime/FppTestProject/FppTest/array/ArrayToStringTest.cpp` |
| 258 | Modeling | What is an "Internal Transition" in an FPP State Machine? | Defined using `on signal do { action }`, it executes the specified action without triggering an exit from or entry into the current state. This allows a state to handle a signal (like an update or heartbeat) while maintaining its current operational context. | `fprime/FppTestProject/FppTest/state_machine/internal/state/BasicInternal.fppi` |
| 259 | Implementation | Explain the use of `tcflush()` in the `LinuxUartDriver`. | `tcflush(fd, TCIFLUSH)` is called during the UART `open()` sequence to discard any old data currently sitting in the kernel's receive buffer. This ensures the driver starts with a "clean slate" and doesn't process stale bytes from a previous session. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 260 | Testing | What is the purpose of the `PortSelector` utility in IP socket tests? | It provides a `get_free_port()` function that binds to port `0` to let the OS assign an available port, retrieves that port number, and then immediately closes the socket. This allows tests to dynamically find unused ports for TCP/UDP servers, reducing race conditions in CI environments. | `fprime/Drv/Ip/test/ut/PortSelector.cpp` |
| 261 | Implementation | How does `LinuxSpiDriver` detect "SPI Configuration Mismatch"? | After calling `ioctl()` to set parameters like `SPI_IOC_WR_MODE`, the driver immediately performs a read-back using `SPI_IOC_RD_MODE`. If the returned value differs from what was written, it emits a `SPI_ConfigMismatch` event to alert the operator. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 262 | Modeling | Describe a "Nested Initial Transition". | In a hierarchical state machine, both the parent state and the child state can define an `initial` transition. When the parent is entered, its initial transition executes, followed immediately by the initial transition of its chosen child state, until a leaf state is reached. | `fprime/FppTestProject/FppTest/state_machine/internal/initial/include/Nested.fppi` |
| 263 | Implementation | What is the `Fw::SmSignalBuffer` used for? | It is a specialized serialization buffer used to carry data associated with a state machine signal. It ensures that any data defined in the FPP signal (e.g., `signal s(data: U32)`) can be safely passed through the component queue to the state machine handler. | `fprime/FppTestProject/FppTest/state_machine/external_instance/DeviceSm.hpp` |
| 264 | Architecture | What is the role of `SocketComponentHelper::readLoop()`? | It is the main execution body of the receive thread. It handles the stateful logic of checking if the socket is opened, requesting a reconnect if disconnected, allocating an F´ buffer, performing the low-level `recv()`, and finally sending the filled buffer to the component. | `fprime/Drv/Ip/SocketComponentHelper.cpp` |
| 265 | Implementation | How do you define an "Explicit" enum in FPP? | By assigning constant integer values to each member: `enum E { A = 10, B = 20 }`. The autocoder generates a C++ enum with these specific underlying values, and `isValid()` will only return true for these exact integers. | `fprime/FppTestProject/FppTest/enum/explicit.fpp` |
| 266 | Testing | Describe the `ASSERT_PRODUCT_REQUEST` macro. | It is used in producer component unit tests to verify that the component correctly requested a Data Product buffer from the manager. It checks the target container ID and the requested size against expected values stored in the `productRequestHistory`. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.cpp` |
| 267 | Modeling | What is a "Choice-to-State" transition? | A branch in a `choice` block that leads directly to a target state. After evaluating the choice's guards, the state machine transitions to the destination state, executing any intervening `exit` and `entry` actions along the path. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/ChoiceToState.cpp` |
| 268 | Implementation | What is the `FW_TASK_NAME_BUFFER_SIZE` macro? | It defines the maximum length of the string used to name an OS task/thread. If `FW_OBJECT_NAMES` is enabled, this name is used to identify the thread in system logs and debuggers, aiding in the analysis of multi-threaded F´ applications. | `docs/user-manual/framework/configuring-fprime.md` |
| 269 | Implementation | How does `LinuxGpioDriver` setup a "Line Event"? | It uses the `GPIO_GET_LINEEVENT_IOCTL`. This kernel call configures a GPIO line to generate an event file descriptor that can be monitored for transitions, providing a low-latency way to handle hardware interrupts on Linux. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 270 | Architecture | Describe the `PassiveBufferDriver` ports. | The interface consists of two primary imports: `Fw.PassiveBufferIn` (for receiving buffers to send) and `Fw.PassiveBufferOut` (for providing received buffers to clients). This standardized interface allows for flexible routing of raw data streams between components. | `fprime/Drv/Interfaces/PassiveBufferDriver.fpp` |
| 271 | Modeling | What is the "Representation Type" of an FPP enum? | It is the underlying integer type used to store and serialize the enum values, defined as `enum MyEnum : U8 { ... }`. Choosing a smaller type (like `U8` instead of the default `I32`) helps minimize the telemetry and parameter footprint. | `fprime/FppTestProject/FppTest/enum/serialize_type.fpp` |
| 272 | Implementation | What is the `handleZeroReturn()` method in `IpSocket`? | It is a virtual method called when a socket `recv()` returns 0 bytes. For TCP, the default implementation treats this as a disconnection (`SOCK_DISCONNECTED`), whereas for UDP, it is overridden to return `SOCK_SUCCESS` (a 0-byte datagram). | `fprime/Drv/Ip/IpSocket.cpp` |
| 273 | Testing | How is `STest::Random::seed()` used in F´ tests? | It initializes the random number generator used by the STest library. By logging the seed value, developers can reproduce non-deterministic test failures by re-running the test suite with the exact same seed. | `fprime/FppTestProject/FppTest/main.cpp` |
| 274 | Implementation | Describe the `MultiString` struct in FPP tests. | It is a serializable struct used to test the handling of multiple strings with different maximum sizes (e.g., 50, 60, and 80 characters) within a single data structure, ensuring the autocoder correctly manages various buffer capacities. | `fprime/FppTestProject/FppTest/struct/struct.fpp` |
| 275 | Architecture | What are the `stopTasks` and `freeThreads` phases in `Fpp::ToCpp::Phases`? | `stopTasks` is the phase where component execution threads are signaled to exit. `freeThreads` follows, where the memory and OS resources associated with those threads are reclaimed, ensuring a clean system shutdown. | `fprime/Fpp/ToCpp.fpp` |
| 276 | Implementation | Describe the `VMIN` and `VTIME` configuration in `LinuxUartDriver`. | In the `termios` setup, `VMIN=0` and `VTIME=10` are used to configure a 1-second timeout for blocking reads. This ensures that the UART read thread periodically wakes up even if no data is present, allowing it to check for shutdown signals. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 277 | Implementation | What is the purpose of the `m_driverIsReady` flag in buffer adapters? | This flag is updated via the `byteStreamDriverReady` port. The adapter only routes incoming buffers to the driver if this flag is `true`; otherwise, it returns the buffer to the sender or logs an error, preventing transitions before the link is stable. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.hpp` |
| 278 | Framework Services | Define `Drv::ByteStreamStatus::RECV_NO_DATA`. | This status indicates that a receive operation was successful at the OS level, but no bytes were actually available to read. It allows the driver to wake up the receive thread without passing empty buffers into the flight software stack. | `fprime/Drv/ByteStreamDriverModel/ByteStreamDriverModel.fpp` |
| 279 | Implementation | How does `TcpServerSocket` limit concurrent clients? | In the `startup()` method, it calls `listen()` with a backlog of `1`. This ensures that only one client connection is queued by the OS at a time, fitting the single-client architecture of most F´ ground-link deployments. | `fprime/Drv/Ip/TcpServerSocket.cpp` |
| 280 | Implementation | What is the `SocketComponentHelper::reconnectLoop()`? | It is a dedicated thread that monitors the `m_reconnectState`. If a reconnect is requested (e.g., after a disconnect), it periodically attempts to call `reopen()` on the socket handler until a successful connection is re-established. | `fprime/Drv/Ip/SocketComponentHelper.cpp` |
| 281 | Modeling | What is an "Implicit" enum in FPP? | An enum where member values are not explicitly assigned (e.g., `enum E { A, B }`). The autocoder automatically assigns sequential integers starting from 0, and the string representation in `toString()` reflects these default IDs. | `fprime/FppTestProject/FppTest/enum/implicit.fpp` |
| 282 | Implementation | Describe the "Multi-Element" constructor for FPP arrays. | It is a C++ constructor that allows an array to be initialized using an initializer list of its elements: `MyArray a({val1, val2, val3})`. This is extensively used in FPP tests to verify correct data assignment during construction. | `fprime/FppTestProject/FppTest/array/main.cpp` |
| 283 | Modeling | What is a "Choice Sequence" in a State Machine? | A pattern where multiple `choice` blocks are linked together (e.g., `choice C1 -> choice C2`). This allows the state machine to evaluate a series of complex conditional branches in a single logical step before landing in a final destination state. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/Sequence.fpp` |
| 284 | Implementation | How does `LinuxGpioDriver` translate configuration to ioctl flags? | It uses helper functions like `configuration_to_handler_flags()`. For example, `GPIO_OUTPUT` is mapped to `GPIOHANDLE_REQUEST_OUTPUT`, while input/interrupt modes are mapped to `GPIOHANDLE_REQUEST_INPUT`. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 285 | Implementation | What is the `SPI_IOC_MESSAGE(1)` ioctl? | It is the primary Linux kernel call for performing an SPI transfer. It takes a pointer to a `spi_ioc_transfer` struct, which contains pointers to the transmit and receive buffers, effectively executing a single SPI transaction. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 286 | Implementation | Describe the `Drv::DataBuffer` serialized type ID. | In `Drv/Ports/DataTypes`, `DataBuffer` defines an internal ID of `1010`. This ID can be used by generic dispatchers or decoders to identify the buffer type when it is passed through a serialization port. | `fprime/Drv/Ports/DataTypes/DataBuffer.hpp` |
| 287 | Implementation | What is the `Fw::InternalInterfaceString` capacity? | By default, it has a capacity of `40` characters (plus the null terminator). This fixed size ensures that internal messages can be queued without the need for dynamic memory allocation on the component thread. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.hpp` |
| 288 | Modeling | Describe "Hierarchical State Entry" behavior. | When transitioning into a nested child state, the state machine executes the `entry` action of the parent state first, followed by the `entry` actions of every intermediate state, ending with the `entry` action of the target child leaf state. | `fprime/FppTestProject/FppTest/state_machine/internal/state/StateToChild.fppi` |
| 289 | Testing | What is the `SmHarness::SignalValueHistory` template? | It is a test harness utility that records both the `Signal` received and the associated `Value` (for typed signals). It allows unit tests to verify that data was correctly passed through the state machine's transitions and actions. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/SignalValueHistory.hpp` |
| 290 | Modeling | How do you define a "Choice" with multiple input signals? | In FPP, multiple `on signal enter choice` statements can point to the same `choice` block. The choice then acts as a shared branching point for different triggers, simplifying the model by consolidating common decision logic. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/InputPairU16U32.fppi` |
| 291 | Implementation | What is the `FW_QUEUE_NAME_BUFFER_SIZE` macro? | It defines the maximum length of the string used to name an OS message queue. This name is used for registration and debugging purposes when `FW_OBJECT_NAMES` is enabled. | `docs/user-manual/framework/configuring-fprime.md` |
| 292 | Implementation | Explain the `O_RDWR` flag in `LinuxI2cDriver::open()`. | This flag opens the I2C device file for both reading and writing. This is necessary for standard I2C transactions, where a register address is written to the device before its contents are read back. | `fprime/Drv/LinuxI2cDriver/LinuxI2cDriver.cpp` |
| 293 | Implementation | How does `LinuxSpiDriver` handle "Bits Per Word"? | The driver configures the SPI interface for 8 bits per word using the `SPI_IOC_WR_BITS_PER_WORD` ioctl. It verifies the setting by reading it back with `SPI_IOC_RD_BITS_PER_WORD` to ensure hardware compatibility. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 294 | Modeling | What is the `drop` keyword used for in async commands? | It specifies the queue full behavior. If the component's message queue is full when an async command arrives, the command will be discarded (dropped) and an error event will typically be emitted, rather than blocking the caller. | `fprime/FppTestProject/FppTest/component/include/commands_async.fppi` |
| 295 | Implementation | Describe the `Fw::LinearBufferBase::copyRaw()` method. | It is a low-level method used to perform a bulk copy of data from one linear buffer to another. In FPP tests, it is used to simulate the receipt of raw serial data into a component's internal handlers. | `fprime/FppTestProject/FppTest/component/tests/TesterHandlers.cpp` |
| 296 | Architecture | What are the `readParameters` and `loadParameters` phases in `Fpp::ToCpp::Phases`? | `readParameters` is the phase where parameter values are retrieved from the database (e.g., `Svc::PrmDb`). `loadParameters` is the subsequent phase where those values are actually applied to the component's internal state variables. | `fprime/Fpp/ToCpp.fpp` |
| 297 | Implementation | What is the `wait_on_change()` helper in socket tests? | It is a polling utility that waits for a specific socket state (e.g., `isOpened() == true`) for a set number of iterations. It allows tests to synchronize with asynchronous reconnection events without hardcoded sleep times. | `fprime/Drv/TcpClient/test/ut/TcpClientTester.cpp` |
| 298 | Modeling | How do you define a "Struct" with a default value in FPP? | Use the `default` keyword followed by a field initializer list: `struct S { x: U32 } default { x = 10 }`. This ensures that any instance of the struct starts with a valid, known state if not explicitly initialized. | `fprime/FppTestProject/FppTest/struct/struct.fpp` |
| 299 | Testing | Describe the `ASSERT_PRODUCT_GET` macro. | It verifies that a producer component requested a buffer from the manager using a "product get" port. It checks the container ID and the exact requested size, ensuring memory allocation logic is correct. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.cpp` |
| 300 | Implementation | What is the `FW_SERIALIZATION_TYPE_ID` macro? | When set to `1` in `FpConfig.h`, every `serializable` type includes its unique type ID in its binary representation. This allows decoders to identify the data type without needing a pre-shared dictionary, though it increases packet size. | `docs/user-manual/framework/configuring-fprime.md` |
| 301 | Architecture | Describe the `configConstants` and `configObjects` phases in `Fpp::ToCpp::Phases`. | `configConstants` is the initial phase for defining system-wide architectural constants. `configObjects` follows, where global objects (like the component registry or memory allocators) are instantiated before component-specific initialization begins. | `fprime/Fpp/ToCpp.fpp` |
| 302 | Implementation | What is the "Consumer Label" in `LinuxGpioDriver`? | It is a string label (usually the component instance name) passed to the Linux GPIO kernel API during `setupLineHandle`. It allows OS-level tools (like `lsgpio`) to report which software component is currently controlling a specific hardware pin. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 303 | Implementation | How does `LinuxSpiDriver` define `SpiMode`? | It maps the 4 standard SPI modes using `CPOL` (Clock Polarity) and `CPHA` (Clock Phase). For example, `SPI_MODE_0` corresponds to `CPOL=0, CPHA=0`, and `SPI_MODE_3` to `CPOL=1, CPHA=1`, as defined in the `SpiMode` enumeration. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.hpp` |
| 304 | Testing | How do FPP tests verify array formatting for octal and hex? | By using the `FormatTest` suite, which checks that array elements marked with `{o}` or `{x}` in FPP are rendered correctly in the `toString()` output using `std::oct` and `std::hex` stream manipulators respectively. | `fprime/FppTestProject/FppTest/array/FormatTest.cpp` |
| 305 | Implementation | What is the string representation of an "Invalid" enum value? | If an enum object is assigned a value not defined in FPP, its `toString()` method will output the string `[invalid] (value)`. This allows developers to quickly identify corrupted data or out-of-bounds telemetry values in the GDS. | `fprime/FppTestProject/FppTest/enum/EnumToStringTest.cpp` |
| 306 | Modeling | Describe the action sequence during a "Choice-to-Choice" transition. | The state machine executes actions in a strict order: 1. `exit` actions of the source state. 2. Any actions defined on the first choice branch. 3. Any actions defined on the second choice branch. 4. `entry` actions of the target state. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/ChoiceToChoice.cpp` |
| 307 | Implementation | What is the purpose of `const_cast` in state machine guards? | In the `SmHarness::NoArgGuard`, `const_cast` is used within the `call()` method (which is `const`) to update the internal call history. This allows the guard to remain functionally `const` while still recording execution data for unit test verification. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/NoArgGuard.hpp` |
| 308 | Implementation | How does the RAG indexer handle docs site scraping? | It uses the `BeautifulSoup` library to extract the text content from docs site pages. The HTML is stripped of tags and metadata, leaving a clean "markdown-like" text stream that is then chunked and embedded for semantic retrieval. | `fprime/TUI/rag/indexer.py` |
| 309 | Architecture | What is the `SocketComponentHelper::reconnectTask`? | It is a secondary OS task that specifically handles reconnection logic. By separating it from the primary `readTask`, the driver can attempt to re-open a socket without blocking the system's ability to process other incoming signals or teardown requests. | `fprime/Drv/Ip/SocketComponentHelper.hpp` |
| 310 | Implementation | Explain the `FW_SERIALIZABLE_TO_STRING_BUFFER_SIZE` macro. | It defines the size of the temporary character buffer used by a `serializable` object to format its `toString()` output. If the formatted string (including all member values) exceeds this size, it will be truncated. | `docs/user-manual/framework/configuring-fprime.md` |
| 311 | Implementation | How does `LinuxGpioDriver` setup a "Line Handle"? | It uses the `GPIO_GET_LINEHANDLE_IOCTL`. This call informs the Linux kernel that the process wants to take control of a GPIO line for direct read/write operations, specifying the desired direction (input or output). | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 312 | Modeling | What is a "Choice" transition without an `else` branch? | FPP requires that all choices be exhaustive. If not every possibility is covered by `if` guards, an `else` branch **must** be provided to ensure the state machine always has a valid destination state, preventing undefined behavior. | `docs/user-manual/framework/state-machines.md` |
| 313 | Implementation | Describe the `Fw::ExternalString` used in FPP tests. | It is a string implementation that wraps a pre-allocated buffer (e.g., on the stack). It is used in tests to simulate incoming string telemetry without triggering heap allocations, allowing for rigorous memory-safety verification. | `fprime/FppTestProject/FppTest/array/main.cpp` |
| 314 | Architecture | What are the `startTasks` and `stopTasks` phases? | `startTasks` is the phase where the OS threads for `active` components are spawned and begin execution. `stopTasks` is the reverse phase, where threads are signaled to exit before the system is deallocated. | `fprime/Fpp/ToCpp.fpp` |
| 315 | Implementation | How does `UdpSocket` support "Ephemeral Receive"? | By configuring the receive port to `0`. The OS will assign a random available port during `bind()`. The driver then calls `getsockname()` to retrieve and log this assigned port so that remote clients know where to send data. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 316 | Testing | Describe the `ASSERT_PRODUCT_REQUEST_SIZE` macro. | It verifies the total number of Data Product allocation requests made by a component. It is a coarse-grained check used before detailed `ASSERT_PRODUCT_REQUEST` calls to ensure the expected volume of science data was generated. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.cpp` |
| 317 | Modeling | What is the `Fw::ParamValid` enum? | It describes the status of a retrieved parameter: `VALID` (successfully loaded), `DEFAULT` (failed to load, using FPP default), `INVALID` (failed to load, no default available), or `UNINIT` (retrieval never attempted). | `docs/user-manual/framework/autocoded-functions.md` |
| 318 | Implementation | What is the `PRI_F64` format specifier used for in F´? | It is the platform-agnostic way to print floating-point numbers. In F´, both `F32` and `F64` values are promoted to `double` when passed to `printf`-family functions, so `PRI_F64` is used for both types to ensure correct formatting. | `docs/reference/numerical-types.md` |
| 319 | Architecture | Explain the "Socket Reconnection" status `SOCK_ANOTHER_THREAD_OPENING`. | This status is returned by `SocketComponentHelper::open()` if another thread is already in the process of establishing a connection. It prevents redundant socket operations and potential resource leaks in multi-threaded environments. | `fprime/Drv/Ip/IpSocket.hpp` |
| 320 | Implementation | Describe the `Fw::LinearBufferBase::getDeserializeSizeLeft()` method. | It returns the number of bytes remaining in a buffer that have not yet been deserialized. It is critical for bounds-checking in serial port handlers to prevent reading past the end of a received packet. | `fprime/FppTestProject/FppTest/component/tests/TesterHandlers.cpp` |
| 321 | Testing | What is the purpose of the `drain()` helper in socket tests? | It repeatedly calls `recv()` on a socket until it returns a `SOCK_DISCONNECTED` status. This ensures that all buffered data from a previous test case is cleared and the socket is fully closed before the next test begins. | `fprime/Drv/Ip/test/ut/SocketTestHelper.cpp` |
| 322 | Modeling | How do you define a `serializable` with an array member in FPP? | Use the member syntax: `struct MyData { data: [10] U32 }`. The autocoder generates a C++ class with a fixed-size array member and automatically handles the serialization of all elements when the struct is passed through a port. | `fprime/FppTestProject/FppTest/struct/struct.fpp` |
| 323 | Implementation | What is the `FW_MIN` macro used for? | It is a framework-provided utility for finding the minimum of two values. It is used in configuration and math logic to ensure platform-independent comparisons and to avoid collisions with standard library `min` definitions. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/Pick.hpp` |
| 324 | Architecture | Describe the `tearDownComponents` phase. | This is the final phase of system execution where components are de-initialized and any allocated memory or system resources (like file descriptors or mutexes) are released before the process exits. | `fprime/Fpp/ToCpp.fpp` |
| 325 | Implementation | How does `LinuxUartDriver` handle flow control? | It supports `HW_FLOW` (hardware flow control). If enabled, the driver modifies the `termios` `c_cflag` to include `CRTSCTS`, enabling the kernel to automatically manage the RTS/CTS serial lines for congestion control. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 326 | Implementation | Describe the `toByteStreamDriverReturn_handler` in `AsyncByteStreamBufferAdapter`. | This handler is called when the driver returns a buffer previously sent for transmission. It extracts the status and forwards the buffer to the `bufferInReturn` output port, ensuring the original sender regains ownership of the memory. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.cpp` |
| 327 | Architecture | What is the difference between `ByteStreamSend` and `Fw::BufferSend`? | `ByteStreamSend` is a synchronous port type that returns a `ByteStreamStatus` immediately. `Fw::BufferSend` is used for asynchronous transfers where the status and buffer ownership are returned later via a separate callback port. | `fprime/Drv/ByteStreamDriverModel/ByteStreamDriverModel.fpp` |
| 328 | Implementation | How does `LinuxGpioDriver` setup "Event Flags"? | It uses `configuration_to_event_flags()` to map FPP configuration to Linux kernel flags like `GPIOEVENT_REQUEST_RISING_EDGE` or `GPIOEVENT_REQUEST_FALLING_EDGE`, dictating which electrical transitions trigger an interrupt. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 329 | Modeling | What is an "Alias of an Array" in FPP? | It is a type definition that creates a new name for an existing array type: `type MyAlias = MyArray`. This allows developers to use semantically meaningful names for generic array structures throughout the model. | `fprime/FppTestProject/FppTest/array/alias.fpp` |
| 330 | Implementation | What is the `SOCK_FAILED_TO_READ_BACK_PORT` error? | This error occurs in `TcpServerSocket::startup()` if the driver cannot retrieve the assigned port number from the kernel after a successful `bind()`. This usually happens when binding to port `0` for dynamic port assignment. | `fprime/Drv/Ip/TcpServerSocket.cpp` |
| 331 | Testing | How do FPP tests verify "Non-Primitive" struct default values? | The `NonPrimitiveStructTest` suite checks that structs containing strings, enums, or nested structs are correctly initialized with the values specified in the FPP `default` block, such as `mEnum = StructEnum.C`. | `fprime/FppTestProject/FppTest/struct/NonPrimitiveStructTest.cpp` |
| 332 | Modeling | Describe a "Choice" with a typed guard in a State Machine. | A choice can evaluate data associated with a signal. For example, `guard g: U32` allows the state machine's decision logic to inspect an integer value passed with the signal before choosing which branch to take. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/BasicU32.hpp` |
| 333 | Implementation | What is the `Fw::DpContainer::DATA_OFFSET` constant? | It defines the starting position of the user data payload within a Data Product buffer. Decoders use this offset to skip the framework-managed header and begin deserializing the actual science records. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.cpp` |
| 334 | Implementation | Explain the use of `::recvfrom()` in `UdpSocket`. | In `recvProtocol()`, `::recvfrom()` is used to read an incoming datagram and simultaneously capture the sender's IP address and port. This information is used to support "ephemeral send" or "reply-to" functionality. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 335 | Testing | What is the `SmHarness::SignalHistory` class? | It is a specialized history buffer used in state machine tests to record only the sequence of signals received by the machine, facilitating simple verification of the transition path taken during a test case. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/SignalValueHistory.hpp` |
| 336 | Modeling | Describe "Hierarchical State Exit" behavior. | When transitioning out of a nested child state to a state outside its parent, the machine executes the `exit` actions in reverse order: 1. `exit` action of the child leaf state. 2. `exit` actions of every intermediate parent state. | `fprime/FppTestProject/FppTest/state_machine/internal/state/StateToChoice.fppi` |
| 337 | Implementation | What is the `FW_SERIALIZABLE_TO_STRING_BUFFER_SIZE` macro used for? | It specifies the capacity of the character buffer used by `serializable::toString()`. If the resulting string (e.g., a struct with many fields) exceeds this size, it is truncated to fit, ensuring bounded memory usage. | `docs/user-manual/framework/configuring-fprime.md` |
| 338 | Implementation | How does `LinuxSpiDriver` handle "Clock Frequency"? | The driver sets the SPI clock speed using the `SPI_IOC_WR_MAX_SPEED_HZ` ioctl. It supports frequencies like `SPI_FREQUENCY_1MHZ` or `SPI_FREQUENCY_10MHZ`, which are common for embedded sensors like IMUs. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 339 | Modeling | What is the `assert` queue behavior in async ports? | If an asynchronous port is invoked and its target component's queue is full, the system will trigger a runtime assertion. This is used for critical messages that **must** be processed and where dropping data is unacceptable. | `fprime/FppTestProject/FppTest/interfaces/serial_ports_async.fpp` |
| 340 | Implementation | Describe the `Fw::LinearBufferBase::getBuffer()` method. | It returns a raw `U8*` pointer to the start of the buffer's memory. This is used by low-level drivers to pass the buffer directly to OS system calls like `read()` or `write()` without framework overhead. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.cpp` |
| 341 | Architecture | What is the `stopTasks` phase in system teardown? | During this phase, the system signals the execution threads of all `active` components to terminate. Components typically stop their message loops and prepare for deallocation in the subsequent `freeThreads` phase. | `fprime/Fpp/ToCpp.fpp` |
| 342 | Implementation | How does `UdpSocket` support "Ephemeral Port Assignment"? | By binding to port `0` in `configureRecv()`. The OS assigns an available port, which the driver then retrieves using `getsockname()` and logs, allowing the system to receive data on a dynamically assigned interface. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 343 | Testing | Describe the `ASSERT_EVENTS_EventName_SIZE` macro. | It is a GTest macro that verifies the exact number of times a specific event (e.g., `IMU_Error`) was emitted during a unit test, providing fine-grained verification of the component's logging behavior. | `docs/user-manual/overview/unit-testing.md` |
| 344 | Modeling | What is the `every` keyword in an FPP event? | It provides periodic reporting for throttled events. For example, `every {seconds = 10}` ensures that even if an event is throttled, it will still be emitted once every 10 seconds to provide heartbeat-like status updates. | `fprime/FppTestProject/FppTest/component/include/events.fppi` |
| 345 | Implementation | What is the `Fw::ParamBuffer` capacity used for? | It defines the maximum serialized size of any parameter in the system. Components use this buffer to pass parameter values to and from the `Svc::PrmDb` via the `ParamGet` and `ParamSet` ports. | `fprime/FppTestProject/FppTest/component/active/test/ut/ActiveTestTester.cpp` |
| 346 | Architecture | Explain the "Passive Buffer Driver" ports. | This interface provides `bufferIn` (input) and `bufferOut` (output) ports of type `Fw::Buffer`. It is used by components like the `ByteStreamBufferAdapter` to provide a generic, raw-data interface to clients. | `fprime/Drv/Interfaces/PassiveBufferDriver.fpp` |
| 347 | Implementation | How does `LinuxUartDriver` handle baud rates on Darwin (macOS)? | While Linux supports a wide range of rates (up to 4000K), macOS baud rate support is limited to standard POSIX rates (up to 230K). The driver uses `#ifdef` guards to ensure only supported rates are compiled for each OS. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.hpp` |
| 348 | Testing | What is the `drain()` helper used for in socket tests? | It repeatedly calls the socket `recv()` method until a disconnection status is returned. This ensures that any stale data from previous test iterations is cleared and the socket is in a known closed state. | `fprime/Drv/Ip/test/ut/SocketTestHelper.cpp` |
| 349 | Modeling | How do you define a constant array in FPP? | Using the `array` and `default` keywords: `array MyArray = [3] U32 default [ 1, 2, 3 ]`. This creates a reusable array type with a fixed set of initial values that can be used as a constant throughout the system. | `fprime/FppTestProject/FppTest/array/enum.fpp` |
| 350 | Architecture | Describe the "Subtopology Config" module. | It is an optional module within a subtopology (e.g., `CdhCoreConfig`) that defines configurable constants like `BASE_ID`, queue sizes, and stack sizes. Projects can override these constants to tune the subtopology for their specific deployment. | `docs/how-to/develop-subtopologies.md` |
| 351 | Architecture | What is the `regCommands` phase in `Fpp::ToCpp::Phases`? | It is the phase where components register their command opcodes with the `Svc::CmdDispatcher`. This is typically done during system initialization, after components are connected but before tasks are started, to enable ground control. | `fprime/Fpp/ToCpp.fpp` |
| 352 | Architecture | Explain the difference between `stopTasks` and `freeThreads` in system teardown. | `stopTasks` signals the component threads to stop their execution loops. `freeThreads` is the final cleanup phase where the actual OS thread resources are released and memory is reclaimed, ensuring a leak-free shutdown. | `fprime/Fpp/ToCpp.fpp` |
| 353 | Implementation | How does `AsyncByteStreamBufferAdapter` handle received data? | In the `fromByteStreamDriver_handler`, it checks the `Drv::ByteStreamStatus`. If the status is `OK`, it forwards the data to its `bufferOut` port; otherwise, it logs an error and returns the buffer to the driver's return port. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.cpp` |
| 354 | Implementation | Describe the reconnection tasks in `SocketComponentHelper`. | It starts two tasks: a `readTask` for normal data reception and a `reconnectTask` for establishing/restoring connections. Each task can be configured with its own priority, stack size, and CPU affinity for fine-grained resource management. | `fprime/Drv/Ip/SocketComponentHelper.cpp` |
| 355 | Implementation | How does `LinuxGpioDriver` handle "Errno to File Status" translation? | It uses a helper `errno_to_file_status()` to map Linux-specific error codes (like `ENODEV` or `EPERM`) to F´ `Os::File::Status` enums (like `DOESNT_EXIST` or `NO_PERMISSION`), maintaining OS-agnostic error reporting. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 356 | Implementation | What is the `SpiMode` mapping in `LinuxSpiDriver`? | It maps FPP `SpiMode` enums to Linux `SPI_IOC_WR_MODE` values. For instance, `SPI_MODE_CPOL_LOW_CPHA_LOW` maps to `SPI_MODE_0`, and `SPI_MODE_CPOL_HIGH_CPHA_HIGH` maps to `SPI_MODE_3`. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 357 | Modeling | What are the generated constants `SIZE` and `ELEMENT_BUFFER_SIZE` for FPP arrays? | `SIZE` is the number of elements in the array. `ELEMENT_BUFFER_SIZE` is the capacity of the temporary buffer used by each element's `toString()` method, ensuring safe string rendering during array serialization. | `fprime/FppTestProject/FppTest/array/ArrayToStringTest.cpp` |
| 358 | Implementation | Describe the `NUM_CONSTANTS` member in autogenerated FPP enums. | It is a static constant that stores the total number of members defined for the enum in FPP. It is commonly used in tests and validation logic to iterate over all possible valid enum values. | `fprime/FppTestProject/FppTest/enum/EnumToStringTest.cpp` |
| 359 | Implementation | How is the `SERIALIZED_SIZE` of a "Non-Primitive" struct calculated? | It is the sum of the static serialized sizes of all its members, including frameworks headers for strings. For nested structs and arrays, the autocoder recursively adds their serialized sizes to the total. | `fprime/FppTestProject/FppTest/struct/NonPrimitiveStructTest.cpp` |
| 360 | Implementation | What is the `Signal` enumeration in an FPP State Machine? | It is an autogenerated enum containing all signals defined in the FPP model, plus the special `__FPRIME_AC_INITIAL_TRANSITION` signal used for component startup logic. | `fprime/FppTestProject/FppTest/state_machine/internal/initial/Basic.cpp` |
| 361 | Modeling | Describe the "Data Product" container ID calculation. | A container ID is calculated as the component's `base id` plus the local ID defined in FPP (e.g., `product container Container1 id 100`). This ensures science data packets are globally unique within a mission's dictionary. | `fprime/FppTestProject/FppTest/dp/DpTest.fpp` |
| 362 | Implementation | How is `Fw::ParamBuffer` used in the `prmGetIn_handler`? | The component resets the buffer, serializes its local parameter value into it using `serializeFrom()`, and returns the buffer to the `Svc::PrmDb` or test harness, facilitating parameter retrieval. | `fprime/FppTestProject/FppTest/component/active/test/ut/ActiveTestTester.cpp` |
| 363 | Implementation | What is the capacity of the `Fw::SmSignalBuffer`? | It is a specialized buffer used for state machine signals that has a fixed capacity (sized to fit the largest typed signal in the model), ensuring that signal data can be queued without dynamic allocation. | `fprime/FppTestProject/FppTest/state_machine/external_instance/SmTest.hpp` |
| 364 | Implementation | Explain the use of `tcgetattr()` and `tcsetattr()` in `LinuxUartDriver`. | These POSIX functions are used to read and write the `termios` configuration struct for a UART device. The driver uses them to set baud rates, parity, and character sizes (`CS8`) during the `open()` call. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 365 | Implementation | Describe the `serialAsyncAssert_handler` in FPP tests. | It is a test handler for a serial asynchronous port with `assert` overflow behavior. It verifies that the component correctly dispatches the serialized message or triggers an assertion if the queue is overloaded. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.cpp` |
| 366 | Testing | What is the role of `FormalParamTypes` in FPP tests? | It is a suite of helper classes and functions used to generate random but valid values for various FPP types (Enums, Arrays, Structs), ensuring that unit tests cover a wide range of data edge cases. | `fprime/FppTestProject/FppTest/component/types/FormalParamTypes.hpp` |
| 367 | Testing | What is the "Race Condition" warning in the `PortSelector` utility? | It notes that there is a brief window between closing the dummy socket and the test server binding to the same port. While minimal, this race condition can occasionally cause port collisions in highly parallel test environments. | `fprime/Drv/Ip/test/ut/PortSelector.hpp` |
| 368 | Testing | Explain the `receive_all()` logic in `SocketTestHelper`. | It is a robust receive utility that continues calling `recv()` until the requested number of bytes have been successfully read, effectively reassembling frames that may have been fragmented by the OS network stack. | `fprime/Drv/Ip/test/ut/SocketTestHelper.cpp` |
| 369 | Architecture | What is the "Mediating" role of the `AsyncByteStreamBufferAdapter`? | It sits between a high-level component that expects a simple `Fw::Buffer` interface and a low-level asynchronous driver. It handles the mapping of send requests to completion callbacks and vice versa. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.fpp` |
| 370 | Architecture | What is the "Mediating" role of the `ByteStreamBufferAdapter`? | Like the async version, it bridges `Fw::Buffer` clients to a driver, but specifically for **synchronous** drivers that provide immediate return statuses from their port invocations. | `fprime/Drv/ByteStreamBufferAdapter/ByteStreamBufferAdapter.fpp` |
| 371 | Implementation | Describe `LinuxGpioDriver::setupLineHandle()` vs `setupLineEvent()`. | `setupLineHandle()` is used for standard input/output operations (reading/writing levels). `setupLineEvent()` is used specifically for configuring edge-triggered interrupts, returning a file descriptor that can be polled. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 372 | Implementation | How does `LinuxSpiDriver` perform a full-duplex transfer? | It uses the `spi_ioc_transfer` struct, setting both `tx_buf` (transmit) and `rx_buf` (receive) to the data pointers of F´ buffers. The kernel then executes the clock cycles to swap data bits between the master and slave. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 373 | Implementation | Explain the `VMIN` and `VTIME` settings in `LinuxUartDriver`. | `VMIN=0` and `VTIME=10` configure a non-canonical read with a 1-second timeout. If no data arrives within 1 second, the `read()` call returns 0, allowing the driver thread to remain responsive to system signals. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 374 | Build System | What is the `FppTestProject` used for? | It is a comprehensive regression suite for the FPP autocoder. It contains exhaustive tests for every FPP language feature (Arrays, Enums, Structs, Components, State Machines) to ensure the generated C++ code is correct. | `fprime/FppTestProject/README.md` |
| 375 | Build System | What is the purpose of `register_fprime_config` in the `fprime/Fpp` directory? | It registers the `FppToCppSettings` module, which contains the FPP definitions (like the `Phases` enum) that configure the behavior of the FPP-to-C++ translation process for all deployments. | `fprime/Fpp/CMakeLists.txt` |
| 376 | Architecture | Describe the `stopTasks` and `freeThreads` phases in system teardown. | `stopTasks` signals component threads to exit their execution loops. `freeThreads` is the subsequent phase where the actual OS thread resources are released and memory is reclaimed, ensuring a clean system shutdown. | `fprime/Fpp/ToCpp.fpp` |
| 377 | Implementation | What is the difference between `Drv::ByteStreamStatus::OTHER_ERROR` and `SEND_RETRY`? | `SEND_RETRY` indicates a temporary failure where retrying the operation immediately or after a short delay is likely to succeed. `OTHER_ERROR` indicates a more serious failure where the socket should likely be closed and re-opened. | `fprime/Drv/ByteStreamDriverModel/ByteStreamDriverModel.fpp` |
| 378 | Implementation | What is the role of `serverFd` in the `SocketDescriptor` struct? | `serverFd` is used by server-type sockets (like `TcpServerSocket`) to track the file descriptor of the listening socket, while `fd` tracks the descriptor of the active communication link with a client. | `fprime/Drv/Ip/IpSocket.hpp` |
| 379 | Implementation | How does `UdpSocket` assign an ephemeral receive port? | By calling `bind()` with a port number of `0`. The OS assigns an available port, which the driver then retrieves using `getsockname()` and logs so that remote senders can identify the assigned interface. | `fprime/Drv/Ip/UdpSocket.cpp` |
| 380 | Implementation | Describe the use of `setupLineHandle()` in `LinuxGpioDriver`. | It is used to configure a GPIO line for direct level-based reading or writing. It Inform the Linux kernel which line to control and in what direction (input or output) using the `GPIO_GET_LINEHANDLE_IOCTL`. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 381 | Implementation | What fields are in the `spi_ioc_transfer` struct used by `LinuxSpiDriver`? | Key fields include `tx_buf` (transmit buffer pointer), `rx_buf` (receive buffer pointer), and `len` (the number of bytes to transfer in the full-duplex SPI transaction). | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 382 | Implementation | How does `LinuxUartDriver` use `VMIN` and `VTIME` for non-blocking reads? | It sets `VMIN=0` and `VTIME=10`, which configures a 1-second timeout for the `read()` call. If no data arrives within that second, the call returns 0, allowing the driver thread to stay responsive to shutdown requests. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 383 | Implementation | Describe the "Multi-Element" constructor for FPP arrays. | It is an autogenerated C++ constructor that accepts an initializer list of all array elements (e.g., `MyArray a({1, 2, 3})`). This provides a type-safe and convenient way to initialize array instances in both flight and test code. | `fprime/FppTestProject/FppTest/array/main.cpp` |
| 384 | Implementation | What is the difference between "Implicit" and "Explicit" enums in terms of `toString()`? | Implicit enums use sequentially assigned IDs (0, 1, 2...). Explicit enums use the specific constant values defined in FPP (e.g., `A = 100`). Both include the member name and the numeric value in their string representation. | `fprime/FppTestProject/FppTest/enum/EnumToStringTest.cpp` |
| 385 | Implementation | How are "Struct" default values initialized in FPP? | The autocoder generates code that applies the `default` block values (e.g., `mU32 = 10`) during the struct's default construction, ensuring every instance starts in a well-defined state as specified in the model. | `fprime/FppTestProject/FppTest/struct/struct.fpp` |
| 386 | Implementation | What is the `__FPRIME_AC_INITIAL_TRANSITION` signal in a State Machine? | It is an internal signal used by the generated code to trigger the transition to the state machine's `initial` state during component initialization, ensuring the machine reaches a starting state automatically. | `fprime/FppTestProject/FppTest/state_machine/internal/initial/Basic.cpp` |
| 387 | Implementation | How is `getPacketSizeForDataSize` used in Data Products? | It calculates the total buffer size required for a Data Product packet by adding the framework's fixed header size (`Fw::DpContainer::MIN_PACKET_SIZE`) to the developer-specified payload data size. | `fprime/FppTestProject/FppTest/dp/DpTest.hpp` |
| 388 | Implementation | Describe the `Fw::ParamBuffer` serialization in handlers. | Components implement `from_prmGetIn_handler` by serializing their local parameter value into the provided `Fw::ParamBuffer` using `serializeFrom()`. The buffer is then returned to the `Svc::PrmDb` or test harness. | `fprime/FppTestProject/FppTest/component/active/test/ut/ActiveTestTester.cpp` |
| 389 | Implementation | Explain `requestReconnect()` vs `waitForReconnect()` in `SocketComponentHelper`. | `requestReconnect()` signals the background reconnection thread to attempt a new connection. `waitForReconnect()` is a blocking call used by the main task to pause until the reconnection thread either succeeds or times out. | `fprime/Drv/Ip/SocketComponentHelper.cpp` |
| 390 | Architecture | What is the "Mediating" role of the `AsyncByteStreamBufferAdapter`? | It sits between a high-level `PassiveBufferDriver` client and a low-level `AsyncByteStreamDriver`. It handles the mapping of asynchronous completion callbacks back to the simple buffer return ports expected by the client. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.fpp` |
| 391 | Implementation | What is the difference between `Fw::LinearBufferBase` and `Fw::SerialBufferBase`? | `Fw::LinearBufferBase` provides raw memory access pointers. `Fw::SerialBufferBase` builds on this to provide a stateful interface for serializing and deserializing data types, tracking the current read/write offset. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.cpp` |
| 392 | Testing | Describe the `productGetHistory` in `TesterBase`. | It is a history data structure that records every call made to a component's "product get" port during a unit test, capturing the container ID and the requested size for verification. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.hpp` |
| 393 | Testing | What does the `ASSERT_PRODUCT_SEND` GTest macro verify? | It verifies that a Data Product was correctly sent. It checks the global ID, priority, time tag, processor type, and user data in the packet header, then provides the payload buffer for further deserialization and check. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.hpp` |
| 394 | Build System | What is the purpose of `FppToCppSettings`? | It is a configuration module in `fprime/Fpp` that provides the FPP definitions (like the `Phases` enum) used by the autocoder to structure the generated C++ initialization and teardown code for all deployments. | `fprime/Fpp/CMakeLists.txt` |
| 395 | Testing | Describe the role of the `PortSelector` utility. | `PortSelector` provides a `get_free_port()` function that helps unit tests find unused TCP/UDP ports by binding to port `0`. This is critical for avoiding port conflicts when running multiple socket tests in parallel. | `fprime/Drv/Ip/test/ut/PortSelector.hpp` |
| 396 | Testing | Explain the `receive_all()` logic in `SocketTestHelper`. | It is a utility that calls `recv()` in a loop until it has accumulated the exact number of bytes requested. This handles cases where large frames are fragmented by the underlying OS network stack during transmission. | `fprime/Drv/Ip/test/ut/SocketTestHelper.cpp` |
| 397 | Implementation | How does `LinuxGpioDriver` translate `errno` to F´ statuses? | It uses an `errno_to_gpio_status()` helper. For example, it maps `EBADF` to `NOT_OPENED` and `ENXIO` to `INVALID_MODE`, providing type-safe error reporting that hides OS-specific error codes from the rest of the system. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 398 | Implementation | Describe the full-duplex transfer in `LinuxSpiDriver`. | It uses the `SPI_IOC_MESSAGE` ioctl to pass a `spi_ioc_transfer` struct to the kernel. This struct contains pointers to both the transmit (`tx_buf`) and receive (`rx_buf`) F´ buffers, executing the transfer in a single clock-synchronized step. | `fprime/Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 399 | Implementation | What is the difference between capacity and length in `Fw::ExternalString`? | Capacity is the total size of the memory buffer allocated to the string (set during `setBuffer()`). Length is the number of characters currently stored in that buffer, which is updated dynamically during assignment or formatting but can never exceed `capacity - 1`. | `fprime/FppTestProject/FppTest/array/main.cpp` |
| 400 | Build System | What is the overall purpose of the `FppTestProject`? | It serves as the primary regression test suite for the FPP language and autocoder. It exhaustively tests every FPP construct (Arrays, Enums, Structs, Components, State Machines) to ensure the generated C++ code is correct and follows framework standards. | `fprime/FppTestProject/README.md` |
| 401 | Implementation | Describe the `Fw::LinearBufferBase::getCapacity()` method. | It returns the total size of the memory region allocated to the linear buffer. This is used by drivers and serial port handlers to verify that incoming data will fit within the buffer before attempting a copy operation. | `fprime/Drv/Ports/DataTypes/DataBuffer.cpp` |
| 402 | Modeling | What is the `FormalParamEnum` used for in FPP tests? | It is a standardized enumeration (X, Y, Z) used across many component and port tests to verify that the autocoder correctly handles enum arguments, including their serialization, deserialization, and usage in state machine signals. | `fprime/FppTestProject/FppTest/component/active/fpp_types.fpp` |
| 403 | Architecture | When is the `SocketComponentHelper::connected()` method called? | It is a virtual notification method called by the `open()` logic immediately after a successful IPv4 socket connection is established. Derived components (like `TcpClient`) override this to signal system-level readiness via the `ready` port. | `fprime/Drv/Ip/SocketComponentHelper.cpp` |
| 404 | Architecture | How does the `AsyncByteStreamBufferAdapter` mediates between drivers? | It translates the high-level `Fw::Buffer` interface (used by components like FileUplink) to the low-level `AsyncByteStreamDriver` interface (used by radios). It manages the mapping of buffer pointers to asynchronous completion callbacks. | `fprime/Drv/AsyncByteStreamBufferAdapter/AsyncByteStreamBufferAdapter.fpp` |
| 405 | Implementation | How are high-speed baud rates like 115200 mapped in `LinuxUartDriver`? | The driver maps FPP-level constants (like `BAUD_115K`) to OS-level `termios` constants (like `B115200`). This ensures that the requested baud rate is correctly translated into the specific bit-patterns required by the Linux kernel's serial driver. | `fprime/Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 406 | Implementation | What port type is used for the `gpioInterrupt` in `LinuxGpioDriver`? | It uses the `Svc.Cycle` port type. This allows the GPIO interrupt to be connected directly to a RateGroup driver or other timing-sensitive components, providing a high-resolution signal indicating that a hardware transition occurred. | `fprime/Drv/LinuxGpioDriver/LinuxGpioDriver.fpp` |
| 407 | Implementation | List the parameters for `TcpClientComponent::configure()`. | It accepts the hostname (as a dot-notation IP string), the port number, send timeout seconds and microseconds, and the `buffer_size` to be allocated for receiving data. This last parameter determines the maximum packet size the component can handle. | `fprime/Drv/TcpClient/TcpClientComponentImpl.hpp` |
| 408 | Architecture | Describe the non-blocking nature of `TcpServerComponent::startup()`. | `startup()` only configures the listening socket and begins the "listen" phase. It does not wait for a client. The actual blocking wait for a client occurs in the `open()` call (within the read task), allowing the main thread to remain responsive during initialization. | `fprime/Drv/TcpServer/TcpServerComponentImpl.cpp` |
| 409 | Implementation | Why are `configureSend` and `configureRecv` separate in `UdpComponent`? | Unlike TCP, UDP is connectionless and can be used for purely unidirectional data flow. Providing separate configuration methods allows developers to set up "send-only" or "receive-only" ports, minimizing resource usage for simple data links. | `fprime/Drv/Udp/UdpComponentImpl.hpp` |
| 410 | Testing | What is the scope of the `TypedPortTest` suite in FPP tests? | It provides type-parameterized tests for every standard FPP data type (Enums, Arrays, Structs, Primitives). It verifies that these types can be correctly passed through `sync`, `async`, and `guarded` port connections without data loss or corruption. | `fprime/FppTestProject/FppTest/typed_tests/PortTest.hpp` |
| 411 | Architecture | How does the `ActivePhaser` component handle task contexts when the `contextType` is set to `COUNT`, and how does this differ from `SEQUENTIAL`? | When a port is registered with a specific `context` value (not `DONT_CARE`), `ActivePhaser` sets the `contextType` to `COUNT`. In this mode, the output port's context argument represents the number of phaser cycles elapsed within a user-specified time window (`m_cycle_count % entry.context`). If `context` is `DONT_CARE`, the type is `SEQUENTIAL`, and the context value increments every time the port is registered, tracking total calls since start. | `Svc/ActivePhaser/ActivePhaser.cpp` |
| 412 | Framework Services | Explain the technical implementation of cycle slip detection in `Svc::ActiveRateGroup`, specifically the interaction between `CycleIn_preMsgHook` and `CycleIn_handler`. | `ActiveRateGroup` uses a volatile boolean flag `m_cycleStarted`. The `CycleIn_preMsgHook` is called on the thread of the *caller* and sets `m_cycleStarted` to `true`. The main `CycleIn_handler` runs on the component's own thread; at the start, it resets `m_cycleStarted` to `false`. If `m_cycleStarted` is found to be `true` at the *end* of the execution loop (before the next cycle starts), it indicates a new cycle message was received before the current one finished, triggering a `RateGroupCycleSlip` event. | `Svc/ActiveRateGroup/ActiveRateGroup.cpp` |
| 413 | Implementation | In `Svc::ActiveTextLogger`, why is the `intText.format` call located in the synchronous `TextLogger_handler` rather than the asynchronous `TextQueue_internalInterfaceHandler`? | Formatting is performed in the `TextLogger_handler` so that the potentially expensive string processing occurs within the task context of the *caller*. This offloads work from the `ActiveTextLogger` component thread, ensuring that the component thread's primary responsibility is limited to the serialized I/O operations (printing to console/file) via the `TextQueue` internal interface, which maintains consistent log ordering. | `Svc/ActiveTextLogger/ActiveTextLogger.cpp` |
| 414 | Error Handling | How does `Svc::AssertFatalAdapter` handle the risk of "cascading asserts" where an assertion failure within the component itself could lead to infinite recursion? | The component maintains a member variable `m_assertCount`. Inside `reportAssert`, it increments `m_assertCount` and checks if it exceeds `FW_ASSERT_COUNT_MAX`. If the count is exceeded or if the `Log` output port is not yet connected, the component falls back to a standard `assert(0)` to terminate the process, preventing a recursive loop of FATAL events that could exhaust system resources. | `Svc/AssertFatalAdapter/AssertFatalAdapterComponentImpl.cpp` |
| 415 | Framework Services | Describe the behavior of `Svc::BufferAccumulator` when it receives a `BA_DrainBuffers` command in `NOBLOCK` mode if the queue contains fewer buffers than requested. | If `blockMode` is `NOBLOCK`, the component compares the requested `numToDrain` with the current size of the `m_bufferQueue`. If the queue size is smaller, it updates `m_numToDrain` to match the current queue size and issues a `BA_NonBlockDrain` warning (severity WARNING_LO). It then drains only the buffers currently available and completes the command response immediately after sending the last available buffer. | `Svc/BufferAccumulator/BufferAccumulator.cpp` |
| 416 | Architecture | How does `Svc::BufferManager` encode metadata into an allocated `Fw::Buffer` to ensure that returned buffers belong to the correct instance and slot? | `BufferManager` utilizes the `context` field of the `Fw::Buffer` class. During `setup`, it assigns each buffer a 32-bit context where the upper 16 bits contain the `m_mgrId` (Manager ID) and the lower 16 bits contain the index of the buffer's tracking structure (`currStruct`). Upon return in `bufferSendIn_handler`, the component validates that the `mgrId` matches its own and that the buffer ID is within the valid range of its allocated structures. | `Svc/BufferManager/BufferManagerComponentImpl.cpp` |
| 417 | Modeling | In FPP, what is the significance of the `drop` keyword when defining an `async input port`, and how does it affect the generated C++ code in `Svc::ActivePhaser`? | The `drop` keyword specifies the queue overflow behavior. It indicates that if the component's input queue is full when a message arrives, the new message should be discarded (dropped) rather than causing an assertion or blocking. In the generated C++ code for `ActivePhaser`, this configuration is passed to the underlying `Os::Queue` during component initialization. | `Svc/ActivePhaser/ActivePhaser.fpp` |
| 418 | Implementation | Explain the logic `Svc::BufferRepeater` uses to manage buffer ownership when duplicating an incoming buffer to multiple output ports. | `BufferRepeater` takes ownership of an incoming buffer via `portIn`. For every connected output port in `portOut`, it calls the `allocate` port to get a *new* `Fw::Buffer`, copies the data from the incoming buffer using `memcpy`, and sends the new buffer out. After all copies are sent, it returns the *original* incoming buffer via its `deallocate` port, ensuring no memory leaks occur. | `Svc/BufferRepeater/BufferRepeater.cpp` |
| 419 | Framework Services | What is the purpose of the `m_ticks_rollover` variable in `Svc::ActivePhaser`, and how is it calculated during port registration? | `m_ticks_rollover` prevents the cycle count (`m_cycle_count`) from growing indefinitely. It represents a common multiple of all user-configured `context` values. During `register_phased`, if a specific `context` is provided, the component multiplies `m_ticks_rollover` by the ratio of the context to the phaser cycle (`context / m_cycle`), ensuring `m_cycle_count` can be safely wrapped using modulo. | `Svc/ActivePhaser/ActivePhaser.cpp` |
| 420 | Testing | How does the `FauxPhaser` class in the `ActivePhaser` unit tests simulate component behavior, and what specific `State` transitions does it track? | `FauxPhaser` is a mock engine used in the test harness to predict the phaser's internal state. It tracks `RUNNING`, `IDLE`, `STARTING`, and `END_OF_CYCLE` states. It simulates the passing of time (ticks) and determines if the "active" child should be running based on `actual_start` and `actual_length` (which includes artificial `lethargy`), verifying if the real component detects missed deadlines correctly. | `Svc/ActivePhaser/test/ut/ActivePhaserTester.cpp` |
| 421 | Framework Services | Describe the purpose and implementation of the `writeHashFile` method in `Svc::BufferLogger`. | `writeHashFile` is called whenever a log file is closed. It uses the `Os::ValidatedFile` utility to compute a cryptographic hash of the just-closed log file and writes this hash to a companion `.hash` file. This allows ground systems to verify the integrity of the data stored on disk and detect bit-rot or unauthorized modifications. | `Svc/BufferLogger/BufferLoggerFile.cpp` |
| 422 | Architecture | Contrast the scheduling behavior of `Svc::ActiveRateGroup` vs. `Svc::ActivePhaser`. | `ActiveRateGroup` is a synchronous scheduler that, upon receiving a `CycleIn` message, immediately invokes all connected ports in a sequential loop. `ActivePhaser` is a "time-sliced" scheduler that divides a cycle into discrete ticks; it uses an internal `Tick` interface to trigger sub-cycle logic, starting and finishing individual port calls at specific tick offsets defined during configuration. | `Svc/ActiveRateGroup/docs/sdd.md` & `Svc/ActivePhaser/docs/sdd.md` |
| 423 | Implementation | In `BufferManagerComponentImpl.cpp`, what is the rationale for using "placement new" during the `setup` phase? | Placement new (`new (&this->m_buffers[currStruct].buff) Fw::Buffer(...)`) is used because the memory for the `Fw::Buffer` tracking objects is allocated as a single contiguous block via the provided `Fw::MemAllocator`. Since the allocator returns raw memory, the `Fw::Buffer` constructor must be explicitly called at those memory addresses to initialize the objects without additional heap allocation. | `Svc/BufferManager/BufferManagerComponentImpl.cpp` |
| 424 | Framework Services | How does `Svc::ActiveTextLogger` handle log file rotation or suffixes if a file with the requested name already exists? | When `set_log_file` is called, the component checks if the file exists using `Os::FileSystem::getFileSize`. If it exists, it enters a loop (up to 10 iterations) attempting to append a numeric suffix (0-9) to the filename. If all 10 suffixed names are taken, it reverts to the original filename, which will then be truncated and overwritten. | `Svc/ActiveTextLogger/LogFile.cpp` |
| 425 | Error Handling | What event and telemetry are updated when `ActiveRateGroup` detects that its execution time has exceeded the cycle period? | The component issues a `RateGroupCycleSlip` event (severity WARNING_HI) containing the current cycle count. It also increments the `m_cycleSlips` counter, which is pushed to the ground via the `RgCycleSlips` telemetry channel. To prevent event flooding, the event is throttled by `ACTIVE_RATE_GROUP_OVERRUN_THROTTLE`. | `Svc/ActiveRateGroup/ActiveRateGroup.cpp` |
| 426 | Modeling | Explain the structure of an FPP `internal port` and provide an example from the `ActivePhaser` component. | An `internal port` in FPP defines a message that a component can send to itself, acting as an asynchronous signal that triggers a specific handler on its own thread. In `ActivePhaser`, the `internal port Tick drop` is used. This allows the `CycleIn` handler to trigger the phaser's sub-cycle tick-processing logic asynchronously. | `Svc/ActivePhaser/ActivePhaser.fpp` |
| 427 | Architecture | Why does `Svc::BufferAccumulator` require a `Fw::MemAllocator` during its `allocateQueue` call instead of using standard `new` or `malloc`? | F´ avoids standard heap allocation at runtime to ensure deterministic memory usage. By requiring an `Fw::MemAllocator`, the component allows the system architect to decide if the queue memory should come from a static pool, a heap, or a specific hardware memory region, maintaining the framework's "Memory Isolation" principle. | `Svc/BufferAccumulator/BufferAccumulator.hpp` |
| 428 | Framework Services | Explain the role of the `m_waitForBuffer` flag in `Svc::BufferAccumulator`. | `m_waitForBuffer` tracks whether the component is currently waiting for a buffer to be returned from the downstream consumer. In `DRAIN` mode, or during a partial drain, the component will not send the next buffer in the queue until the previous one is returned (flag set to `false` in `bufferSendInReturn_handler`), preventing it from overwhelming a slower consumer. | `Svc/BufferAccumulator/BufferAccumulator.cpp` |
| 429 | Implementation | How does `Svc::BufferLogger` store the size of each buffer within the binary log file, and how is this configured? | `BufferLogger` stores a "size-of-size" field before each logged buffer. This is configured via the `sizeOfSize` parameter in `initLog`. The `File::writeSize` method manually serializes the `FwSizeType` into the number of bytes specified by `m_sizeOfSize` (e.g., 1, 2, 4, or 8 bytes) in big-endian format to ensure portability. | `Svc/BufferLogger/BufferLoggerFile.cpp` |
| 430 | Testing | What is the purpose of `ASSERT_DEATH` tests in the F´ framework, and how might they be used with `BufferManager`? | `ASSERT_DEATH` (a GTest macro) is used to verify that "hard" failures, such as `FW_ASSERT` violations, correctly terminate the program. For `BufferManager`, a death test might verify that returning a buffer with a pointer that does *not* originate from the component's managed memory range correctly triggers an assertion failure. | `Svc/ActivePhaser/test/ut/ActivePhaserTestMain.cpp` |
| 431 | Framework Services | In `Svc::BufferRepeater`, what happens if the `configure` method is never called? | The component initializes `m_allocation_failure_response` to `NUM_BUFFER_REPEATER_FAILURE_OPTIONS`. If `portIn_handler` is called without prior configuration, the `FW_ASSERT(this->m_allocation_failure_response < NUM_BUFFER_REPEATER_FAILURE_OPTIONS)` will fail, resulting in a system reset or termination. | `Svc/BufferRepeater/BufferRepeater.cpp` |
| 432 | Architecture | Explain the concept of "Guarded" ports in `Svc::BufferManager`. | The `bufferSendIn` and `bufferGetCallee` ports are defined as `guarded`, meaning the F´ autocoder generates a mutex lock around the handler calls. Since `BufferManager` is a passive component potentially shared by multiple threads, this guard prevents race conditions when searching for or marking buffers as allocated. | `Svc/BufferManager/BufferManager.fpp` |
| 433 | Implementation | How does `Svc::ActivePhaser` calculate the `start` time of the first registered task if `DONT_CARE` is specified? | In `register_phased`, if it's the first task (`m_state.used == 0`) and `start` is `DONT_CARE`, the component sets `start` to `0`. For subsequent tasks with `DONT_CARE`, it sets `start` to the end of the previous task (`previous.start + previous.length`), facilitating a sequential "packed" schedule. | `Svc/ActivePhaser/ActivePhaser.cpp` |
| 434 | Framework Services | Describe the severity levels used in `Svc::ActiveTextLogger` and their mapping to strings in the log file. | The component maps `Fw::LogSeverity` enums to uppercase strings: `FATAL`, `WARNING_HI`, `WARNING_LO`, `COMMAND`, `ACTIVITY_HI`, `ACTIVITY_LO`, and `DIAGNOSTIC`. Notably, `DIAGNOSTIC` events are explicitly filtered out and *never* written to the console or file by `ActiveTextLogger` to reduce log volume. | `Svc/ActiveTextLogger/ActiveTextLogger.cpp` |
| 435 | Architecture | What is the difference between a `Passive` component like `Svc::BufferManager` and an `Active` component like `Svc::ActiveRateGroup` regarding execution threads? | A `Passive` component has no thread of its own; its handlers execute on the thread of the caller. An `Active` component owns an `Os::Task` and a message queue; calls to its `async` input ports are serialized into the queue and processed one by one on its own dedicated thread. | `Svc/BufferManager/docs/sdd.md` & `Svc/ActiveRateGroup/docs/sdd.md` |
| 436 | Architecture | How does the `Svc::CmdSplitter` component facilitate the Hub Pattern in multi-deployment architectures? | `CmdSplitter` acts as a routing node between an uplink source and two separate dispatchers. It compares the opcode of an incoming `Fw::Com` packet against a configured `m_remoteBase`. Opcodes less than the base are routed to the `LocalCmd` port (local dispatcher), while those equal or greater are routed to `RemoteCmd` (hub component for remote deployment). This allows transparent command addressing across deployment boundaries using a single uplink. | `Svc/CmdSplitter/CmdSplitter.cpp` |
| 437 | Framework Services | Describe the command tracking mechanism used by `Svc::CommandDispatcher` to route execution statuses back to sequencers. | The dispatcher maintains an `m_sequenceTracker` table of `SequenceTracker` structs. When a command is received via a `seqCmdBuff` port, the dispatcher finds an empty slot, stores the opcode, assigned `m_seq`, the caller's `portNum`, and a user-defined `context`. When the component returns a status via `compCmdStat`, the dispatcher matches the `cmdSeq`, retrieves the caller's port, and invokes `seqCmdStatus_out` to inform the original source. | `Svc/CmdDispatcher/CommandDispatcherImpl.cpp` |
| 438 | Implementation | How does `Svc::Ccsds::TmFramer` ensure fixed-length frames even when user payload data is smaller than the configured `TmFrameFixedSize`? | Per the CCSDS TM Data Link protocol, `TmFramer` fills the remainder of the data field with an **Idle Packet**. It serializes the user packet first, then invokes `fill_with_idle_packet`, which inserts a Space Packet header with the `SPP_IDLE_PACKET` APID (0x07FF) and pads the remaining bytes with a fixed `IDLE_DATA_PATTERN` (0x44) until it reaches the CRC trailer offset. | `Svc/Ccsds/TmFramer/TmFramer.cpp` |
| 439 | Architecture | Explain the triple-layered deframing process in the F´ CCSDS communication stack. | Deframing occurs in stages: 1. `TcDeframer` receives a TC Transfer Frame, validates the SCID/VCID/CRC, and extracts the Frame Data Unit. 2. `SpacePacketDeframer` receives that data, parses the Space Packet Primary Header (APID, Length, Seq Count), and validates the sequence via `ApidManager`. 3. The extracted payload (typically a serialized `Fw::Com` packet) is then passed to the `CommandDispatcher`. | `Svc/Ccsds/docs/sdd.md` |
| 440 | Modeling | What is the significance of the `match` keyword in `CmdDispatcher.fpp` and how does it prevent integration errors? | The `match` keyword creates a model-level constraint: `match compCmdSend with compCmdReg`. This ensures that the dispatcher's output ports for sending commands are one-to-one with the input ports for registering them. If a topology connects a component's registration to index `N`, the dispatcher is guaranteed to send commands for those registered opcodes back through index `N`. | `Svc/CmdDispatcher/CmdDispatcher.fpp` |
| 441 | Implementation | In `Svc::Ccsds::ApidManager`, how is the onboard sequence count handled when a mismatch is detected during validation? | When `validateApidSeqCountIn_handler` detects that a received `receivedSeqCount` does not match the `expectedSequenceCount`, it issues a `UnexpectedSequenceCount` warning. Crucially, it then calls `setNextSeqCount(apid, receivedSeqCount + 1)` to synchronize the onboard tracker with the ground system, allowing the system to continue tracking future packets despite the detected gap. | `Svc/Ccsds/ApidManager/ApidManager.cpp` |
| 442 | Testing | How do the `CmdSequencer` unit tests verify file I/O error handling without depending on the actual filesystem? | The tests use a **PosixFileInterceptor** delegate. This class inherits from `Os::Posix::File` and intercepts `open()` and `read()` calls. By setting the `interceptor.fileStatus` or `interceptor.errorType` (e.g., `ErrorType::SIZE`), the test can force the component to encounter artificial "No Space," "Permission Denied," or "Truncated Read" errors and verify that the corresponding events are emitted. | `Svc/CmdSequencer/test/ut/CmdSequencerTester.cpp` |
| 443 | Framework Services | Describe the purpose and operation of the `CS_JOIN_WAIT` command in `Svc::CmdSequencer`. | `CS_JOIN_WAIT` allows a ground operator or script to wait for a currently running non-blocking sequence to finish. When issued, the component sets `m_join_waiting = true`. The sequencer will not return a success status for the `CS_JOIN_WAIT` command until the active sequence either completes normally or encounters an error, enabling synchronization between multiple sequence files. | `Svc/CmdSequencer/CmdSequencerImpl.cpp` |
| 444 | Implementation | Why does `Svc::Ccsds::TmFramer` utilize a `BufferOwnershipState` state machine? | Since `TmFramer` uses a fixed-size internal member buffer (`m_frameBuffer`) to avoid dynamic allocation, it must ensure that only one component uses this buffer at a time. The `BufferOwnershipState` (OWNED/NOT_OWNED) prevents the component from starting a new framing operation until the previous frame has been returned via the `dataReturnIn` port, protecting against data corruption. | `Svc/Ccsds/TmFramer/TmFramer.hpp` |
| 445 | Modeling | Explain the difference between `RELATIVE` and `ABSOLUTE` command descriptors in an F´ binary sequence file. | `ABSOLUTE` (0x00) records contain a specific GMT/UTC timestamp; the sequencer executes the command only when the system clock reaches or exceeds that time. `RELATIVE` (0x01) records contain a delay (seconds/microseconds); the sequencer adds this delay to the completion time of the *previous* command to determine the next execution time. | `Svc/CmdSequencer/docs/sdd.md` |
| 446 | Implementation | How does `Svc::ChronoTime` provide platform-independent time without using `Svc::PosixTime`? | `ChronoTime` leverages C++11 language features via the `<chrono>` library. It calls `std::chrono::system_clock::now()` and uses `duration_cast` to convert the time since epoch into seconds and microseconds. This removes the dependency on POSIX headers, allowing the same time-get logic to run on any system with a C++11-compliant compiler. | `Svc/ChronoTime/ChronoTime.cpp` |
| 447 | Architecture | How does `Svc::Ccsds::TcDeframer` handle the CCSDS Frame Length field, which is defined as "Length - 1"? | The component correctly implements the protocol by extracting the 10-bit length token from the header and adding 1: `total_frame_length = (header.get_vcIdAndLength() & FrameLengthMask) + 1`. It then uses this result to verify that the received `Fw::Buffer` contains enough bytes before attempting to validate the CRC trailer. | `Svc/Ccsds/TcDeframer/TcDeframer.cpp` |
| 448 | Error Handling | What happens in `Svc::CmdSplitter` if an incoming `Fw::Com` packet fails to deserialize into a valid `CmdPacket`? | If `cmdPkt.deserializeFrom(data)` returns a status other than `FW_SERIALIZE_OK`, the splitter does not attempt opcode-based routing. Instead, it routes the raw buffer to the `LocalCmd` port. This ensures that the local `CommandDispatcher` handles the malformed packet, where it can issue a `MalformedCommand` event and a `VALIDATION_ERROR` status to the ground. | `Svc/CmdSplitter/CmdSplitter.cpp` |
| 449 | Modeling | In FPP, what is the role of the `hook` keyword on the `seqCmdBuff` port in `CommandDispatcher`? | The `hook` keyword tells the autocoder to generate a virtual function `seqCmdBuff_overflowHook`. This hook is called if the component's input queue overflows. `CommandDispatcher` implements this hook to increment the `m_numCmdsDropped` counter and issue a `CommandDroppedQueueOverflow` event, providing observability into command-loss during load spikes. | `Svc/CmdDispatcher/CmdDispatcher.fpp` |
| 450 | Implementation | Explain the opcode extension logic in `Svc::AMPCSSequence` when translating 16-bit opcodes to F´ format. | AMPCS sequence files use 16-bit opcodes. F´ uses a configurable `FwOpcodeType` (usually 32-bit). In `translateCommand`, the component zero-extends the 2-byte opcode by padding it with `sizeof(FwOpcodeType) - 2` zero bytes before serializing it into the `Fw::ComBuffer`, ensuring compatibility with the standard F´ dispatcher. | `Svc/CmdSequencer/formats/AMPCSSequence.cpp` |
| 451 | Framework Services | How does `Svc::CommandDispatcher` handle command registration opcode collisions? | During `compCmdReg_handler`, the dispatcher iterates through its `m_entryTable`. If it finds the opcode already exists but is mapped to a *different* port, it triggers an `FW_ASSERT`. If it is mapped to the *same* port, it logs an `OpCodeReregistered` diagnostic. This ensures that every opcode in the system is uniquely routed to exactly one component handler. | `Svc/CmdDispatcher/CommandDispatcherImpl.cpp` |
| 452 | Architecture | Describe the structure of the `globalVcId` field in a CCSDS Telemetry Transfer Frame as constructed by `TmFramer`. | The 16-bit `globalVcId` (GVCID) is a bit-field concatenation: 2 bits for Version Number (0), 10 bits for the `SpacecraftId` (configured in `ComCfg.fpp`), 3 bits for the `VirtualChannelId` (provided in the `FrameContext`), and 1 bit for the Operational Control Field flag (0). | `Svc/Ccsds/TmFramer/TmFramer.cpp` |
| 453 | Implementation | How does `Svc::CmdSequencer` validate the CRC of an F´ sequence file? | It uses the `Utils/Hash/libcrc` library. It initializes a 32-bit CRC, updates it with the entire buffer content (Header + Records), and finalizes it. It then compares this `m_computed` value against the `m_stored` value extracted from the last 4 bytes of the file. A mismatch triggers a `CS_FileCrcFailure` event and aborts the load. | `Svc/CmdSequencer/FPrimeSequence.cpp` |
| 454 | Testing | What is the purpose of "Rule-Based Testing" in the `ApidManager` test suite? | It uses the `STest` library to define atomic "Rules" (e.g., `GetNewSeqCountOk`, `ValidateSeqCountFailure`). Each rule has a `precondition` and an `action`. A `RandomScenario` then picks and applies these rules in random orders, allowing the test to explore complex state-space combinations and race conditions that manual unit tests might miss. | `Svc/Ccsds/ApidManager/test/ut/ApidManagerTester.hpp` |
| 455 | Architecture | Why does `Svc::CmdSequencer` define an abstract `Sequence` class? | The abstract `Sequence` class provides a generic interface (`loadFile`, `nextRecord`, `reset`) that decouples the sequencer's execution logic from the binary file format. This allows developers to plug in different format implementations, like the provided `FPrimeSequence` or `AMPCSSequence`, or even project-specific custom formats, without modifying the core sequencer component. | `Svc/CmdSequencer/CmdSequencerImpl.hpp` |
| 456 | Modeling | Describe the `FrameError` enum used in the CCSDS service. | It is a central enumeration in `Types.fpp` that defines standardized error codes for the entire CCSDS stack: `SP_INVALID_LENGTH`, `TC_INVALID_SCID`, `TC_INVALID_LENGTH`, `TC_INVALID_VCID`, and `TC_INVALID_CRC`. These are passed through `errorNotify` ports to provide ground visibility into deframing failures. | `Svc/Ccsds/Types/Types.fpp` |
| 457 | Implementation | How is the `m_seq` counter used within `Svc::CommandDispatcher`? | `m_seq` is a monotonically increasing 32-bit counter. Every time a command is dispatched, it is assigned the current `m_seq` value as its sequence number. This number is used as the key in the `m_sequenceTracker` to uniquely identify the command and route its eventual response back to the correct source sequencer. | `Svc/CmdDispatcher/CommandDispatcherImpl.cpp` |
| 458 | Architecture | In `Svc::Ccsds::SpacePacketDeframer`, how is the `FrameContext` updated for downstream components? | The deframer takes the incoming `context`, makes a copy, and populates it with the extracted `apid` and `sequenceCount` from the Space Packet Primary Header using `set_apid()` and `set_sequenceCount()`. This "Contextual metadata" travels with the data buffer, allowing the Command Dispatcher to know the source characteristics of the command. | `Svc/Ccsds/SpacePacketDeframer/SpacePacketDeframer.cpp` |
| 459 | Implementation | Explain the "Double CRC" check in `Svc::AMPCSSequence` loading. | `AMPCSSequence` requires a companion `.CRC32` file. During `loadFile`, it first reads and validates the CRC of the `.CRC32` file itself (via `readCRCFile`). Then, it reads the sequence file, computes its internal CRC, and compares it against the value just validated from the CRC file. This ensures both the command data and its verification hash are untampered. | `Svc/CmdSequencer/formats/AMPCSSequence.cpp` |
| 460 | Modeling | Contrast the `STEP` and `AUTO` sequence modes in `Svc::CmdSequencer`. | In `AUTO` mode, the sequencer executes the next command immediately after receiving a successful `CmdResponse` for the current one. In `STEP` mode, the sequencer loads the next command but enters a `STOPPED` state, waiting for an explicit `CS_STEP` or `CS_START` command from the ground before dispatching it. | `Svc/CmdSequencer/CmdSequencer.fpp` |
| 461 | Implementation | How does the `m_allow_timeout` flag in `Svc::ComAggregator` mitigate queue overflow risks in high-latency port chains? | In `ComAggregator`, the `timeout_handler` only queues a timeout signal to the internal state machine if `m_allow_timeout` is true (typically when in the `FILL` state). This prevents the component's queue from becoming saturated with irrelevant timeout messages while waiting for a synchronous downstream radio or retry component to complete a transmission, which could otherwise lead to a drop of valid payload data. | `Svc/ComAggregator/ComAggregator.cpp` |
| 462 | Framework Services | Describe the rotation logic used by `Svc::ComQueue` to implement round-robin balancing between ports of the same priority. | After a message is dequeued and sent from a specific prioritized queue, `ComQueue::processQueue` enters a rotation loop. It identifies the range of entries in `m_prioritizedList` that share the same priority as the one just dispatched. It then performs a linear swap (shift) such that the recently serviced queue index is moved to the end of that specific priority group, ensuring the next available buffer from a different port of the same priority is selected next. | `Svc/ComQueue/ComQueue.cpp` |
| 463 | Architecture | Explain the role of the `m_reinitialize` flag in `Svc::ComStub` and its impact on the `comStatusOut` port. | The `m_reinitialize` flag tracks whether the component needs to signal readiness to an attached `Svc::ComQueue` upon driver reconnection. It is set to `true` on initialization or after a driver error (e.g., `OTHER_ERROR`). When the `drvConnected` port is invoked, if `m_reinitialize` is true, the component sends `Fw::Success::SUCCESS` via `comStatusOut` and resets the flag to `false`, effectively starting the downlink process. | `Svc/ComStub/ComStub.cpp` |
| 464 | Architecture | Detail the sorting precedence used by `Svc::DpCatalog` when building its data product catalog. | `DpCatalog` implements a strict priority-based sort using an unbalanced binary tree. Precedence is given first to the **Data Product Priority** (lower value = higher priority), then to the **Generation Time** (older products = higher priority), and finally to the **Data Product ID** (lower ID = higher priority). This ensures that critical and aged science data is prioritized for downlink over newer or less important products. | `Svc/DpCatalog/DpCatalog.cpp` |
| 465 | Implementation | Why does `Svc::ComLogger` avoid issuing events or using the `comStatusOut` port within its destructor? | To prevent "virtual method called" segmentation faults during object destruction. In C++, calling virtual methods (which includes F´ output port calls) from a destructor when the object's base class is already partially destroyed is unsafe. Instead, the destructor manually closes the file handle and writes the hash file, skipping any logic that would trigger an F´ event or port invocation. | `Svc/ComLogger/ComLogger.cpp` |
| 466 | Framework Services | Contrast the buffer ownership transfer behavior of `productGetIn` vs. `productRequestIn` in `Svc::DpManager`. | `productGetIn` is a synchronous port; it requests a buffer from the manager, fills the provided reference, and returns a status immediately, with the caller retaining control. `productRequestIn` is asynchronous; it requests a buffer and sends the resulting `Fw::Buffer` (valid or invalid) out via the `productResponseOut` port. In both cases, the `DpManager` itself acts as a broker, delegating the actual allocation to a `BufferManager` via the `bufferGetOut` port. | `Svc/DpManager/DpManager.cpp` |
| 467 | Implementation | How does `Svc::ComQueue` distinguish between `Fw::ComBuffer` and `Fw::Buffer` storage in its monolithic memory allocation? | During `configure`, `ComQueue` calculates the required memory size based on the port index. Port indices lower than `COM_PORT_COUNT` are mapped to `Fw::ComBuffer` (fixed size), while indices greater or equal are mapped to `Fw::Buffer` tracking objects. The component partitions the single provided memory block into sub-regions sized `(depth * msgSize)` for each port, where `msgSize` is determined by this index-based type check. | `Svc/ComQueue/ComQueue.cpp` |
| 468 | Implementation | Explain the "Held Data" mechanism in `Svc::ComAggregator`'s state machine. | When `ComAggregator` is in the `WAIT_STATUS` state (waiting for ground/radio acknowledgement) and receives a `fill` signal that would exceed the current buffer's capacity, it performs a `doHold` action. This stores the incoming data in `m_held`. Once the status returns and the aggregate is cleared (`doClear`), the state machine automatically triggers a `doFill` using the `m_held` data, ensuring no data is lost during the send-and-wait cycle. | `Svc/ComAggregator/ComAggregator.cpp` |
| 469 | Architecture | Describe the memory partitioning strategy used by `Svc::DpCatalog` to accommodate tree nodes, traversal stacks, and state file data in one block. | `DpCatalog` receives a single monolithic memory pointer. It partitions this block into three contiguous arrays: 1. `m_freeListHead` (array of `DpBtreeNode`), 2. `m_traverseStack` (array of node pointers for non-recursive traversal), and 3. `m_stateFileData` (array of `DpDstateFileEntry` for persistence). The sizes are proportional to `DP_MAX_FILES`, and the component re-calculates capacity if the provided block is smaller than expected, enabling graceful degradation. | `Svc/DpCatalog/DpCatalog.cpp` |
| 470 | Implementation | Compare the implementation of `RETRY_LIMIT` for synchronous vs. asynchronous sends in `Svc::ComStub`. | For **synchronous** sends (`drvSendOut`), `ComStub` uses a local `for` loop inside `handleSynchronousSend` to retry up to the limit immediately. For **asynchronous** sends (`drvAsyncSendOut`), it uses a persistent member `m_retry_count`. If the driver callback (`drvAsyncSendReturnIn`) reports `SEND_RETRY`, the component increments the count and re-invokes the async send port, only failing once the count exceeds the limit. | `Svc/ComStub/ComStub.cpp` |
| 471 | Implementation | How does `Svc::ComLogger` ensure unique filenames for log files created during the same second? | The filename format includes the Time Base, Seconds, and **Microseconds**: `%s_%d_%d_%06d.com`. By including the microsecond field from the component's `getTime()` call, `ComLogger` ensures that even if a log file is rotated rapidly (e.g., due to a small `maxFileSize` and high data rate), each new file receives a unique name based on the precise system time of its creation. | `Svc/ComLogger/ComLogger.cpp` |
| 472 | Modeling | What is the purpose of the `bufferQueueIn_overflowHook` in `Svc::ComQueue` and why is it mandatory? | The `hook` keyword in FPP for `bufferQueueIn` requires the developer to implement `bufferQueueIn_overflowHook`. This is mandatory for memory safety; when the asynchronous input queue is full, the component must have a mechanism to handle the `Fw::Buffer` ownership. `ComQueue` implements this by immediately returning the overflowed buffer via the `bufferReturnOut` port, preventing memory leaks in the producer component. | `Svc/ComQueue/ComQueue.cpp` |
| 473 | Framework Services | Describe the structure of the `DpCatalog` state file and its pruning logic. | The state file stores serialized `DpStateEntry` objects, which include the directory index and the `DpRecord` metadata (ID, Time, Priority, State). During a `BUILD_CATALOG` command, the component reads this file into `m_stateFileData`. It only writes entries back to the file (`pruneAndWriteStateFile`) if they were "visited" (matched to a real file on disk) during the current scan, effectively removing metadata for science data products that have been deleted. | `Svc/DpCatalog/DpCatalog.cpp` |
| 474 | Modeling | Explain the state transition from the `IS_FULL_THEN_SEND` choice in the `ComAggregator` state machine. | When a `fill` signal arrives, the `isFull` guard checks if the incoming data fits in the remaining aggregate space. If `true`, the machine executes `doHold` (storing the new data) and `doSend` (transmitting the current aggregate), then transitions to `WAIT_STATUS`. If `false`, it proceeds to the `WILL_FILL_THEN_SEND` choice to determine if the data can be serialized into the current aggregate before potentially sending. | `Svc/ComAggregator/ComAggregator.fpp` |
| 475 | Implementation | How does `Svc::ComQueue` handle priority values that are repeated across multiple source ports? | If multiple source ports share the same priority, `ComQueue` treats them as a peer group. The `processQueue` method will service the first one it finds in the `m_prioritizedList` that has data. After sending, it rotates that port's metadata to the end of the subset of entries with that same priority value, ensuring that the next send from that priority level will come from a different port, facilitating balanced bandwidth distribution. | `Svc/ComQueue/ComQueue.cpp` |
| 476 | Framework Services | How does `Svc::DpManager` facilitate ground-clearing of data product allocation throttles? | The component implements the `CLEAR_EVENT_THROTTLE` command. When received, it invokes the autogenerated `log_WARNING_HI_BufferAllocationFailed_ThrottleClear()` method. This resets the internal event counter for the `BufferAllocationFailed` warning, allowing the ground to resume receiving these events if scientific data product allocation failures persist after a troubleshooting session. | `Svc/DpManager/DpManager.cpp` |
| 477 | Implementation | Why does `Svc::ComStub` return an empty `FrameContext` in its `drvReceiveIn_handler`? | `ComStub` acts as a generic byte-stream bridge. When it receives raw bytes from an underlying driver (like a UART), it has no protocol-level knowledge of the data's source, APID, or sequence count. It therefore forwards the `Fw::Buffer` with a default-initialized `FrameContext`. It is the responsibility of a downstream component, such as `SpacePacketDeframer`, to parse the headers and populate the context correctly. | `Svc/ComStub/ComStub.cpp` |
| 478 | Framework Services | Discuss the trade-offs of the `storeBufferLength` configuration in `Svc::ComLogger`. | If `storeBufferLength` is true, the component serializes a 16-bit size field before every `Fw::Com` buffer in the file. This allows ground tools to parse files containing variable-length packets. If false, only the raw buffers are stored, saving 2 bytes per entry and reducing disk I/O, but requiring the ground system to have external knowledge of the packet sizes (e.g., all packets are fixed size) to correctly de-frame the log. | `Svc/ComLogger/ComLogger.hpp` |
| 479 | Architecture | Explain the traversal algorithm used by `Svc::DpCatalog` to downlink data products in priority order. | To avoid the risks of stack overflow in safety-critical systems, `DpCatalog` implements a **non-recursive in-order traversal** of its binary tree. It uses an internal `m_traverseStack` (allocated during `configure`) to manually manage the tree nodes. The `findNextTreeNode` method uses this stack to find the leftmost (highest priority) un-transmitted node, yielding one product per `fileDone` callback until the entire tree is processed. | `Svc/DpCatalog/DpCatalog.cpp` |
| 480 | Implementation | In `ComAggregator`, what occurs when the `doClear` action is executed if data is currently being "held"? | The `doClear` action resets the frame serializer and sets the aggregate size back to its maximum capacity. It then checks if `m_held` contains a valid buffer (data that overflowed the *previous* aggregate). If so, it immediately calls `doFill` with the held data, ensuring that the buffer that triggered the send is the first one placed into the new aggregation cycle. | `Svc/ComAggregator/ComAggregator.cpp` |
| 481 | Framework Services | Describe the behavior of the `Svc::ComQueue` state machine when receiving a `Fw::Success::SUCCESS` status while in the `WAITING` state. | Upon receiving a successful status, the component transitions to the `READY` state. It immediately calls `processQueue()`, which scans all source ports in priority order. If any queue has data, it dequeues the highest priority message, sends it out via `dataOut`, and transitions *back* to the `WAITING` state. If all queues are empty, it remains in `READY`, waiting to send the next incoming buffer immediately upon receipt. | `Svc/ComQueue/ComQueue.cpp` |
| 482 | Framework Services | How does `Svc::DpManager` provide visibility into scientific data volume via telemetry? | The component maintains a `numBytes` member variable (of type `U64`). Every time a filled data product is received via the `productSendIn` port, the manager adds the buffer's size to this counter. The total accumulated volume is then reported to the ground via the `NumBytes` telemetry channel during the `schedIn` handler, allowing operators to monitor science data production rates. | `Svc/DpManager/DpManager.cpp` |
| 483 | Implementation | Describe how `Svc::ComStub` handles a `Drv::ByteStreamStatus::SEND_RETRY` response from an asynchronous driver callback. | When the `drvAsyncSendReturnIn_handler` receives a `SEND_RETRY` status, it checks its internal `m_retry_count`. If the count is below the `RETRY_LIMIT`, it increments the count and re-sends the *same* `Fw::Buffer` back to the driver via `drvAsyncSendOut`. If the limit is reached, it gives up, returns the buffer to the original sender via `dataReturnOut`, and signals a failure to the queue via `comStatusOut`. | `Svc/ComStub/ComStub.cpp` |
| 484 | Implementation | How does `Svc::ComLogger` ensure the cryptographic integrity of its log files? | When a log file is closed (either by command or by reaching `maxFileSize`), the component invokes `writeHashFile`. This method calls `Os::ValidateFile::createValidation`, which computes a hash (typically SHA-256 or CRC32 depending on project config) of the entire log file and writes the result to a companion `.CRC32` or `.hash` file. This allows ground systems to verify that the binary data has not been corrupted or tampered with on disk. | `Svc/ComLogger/ComLogger.cpp` |
| 485 | Framework Services | Contrast the `drop` behavior of `comPacketQueueIn` with the `hook` behavior of `bufferQueueIn` in `Svc::ComQueue`. | `comPacketQueueIn` is defined with the `drop` keyword in FPP, meaning if the component's internal message queue is full, the incoming command packet is silently discarded by the OSAL layer. `bufferQueueIn` is defined with a `hook`, allowing the component to provide a custom `overflowHook` implementation. `ComQueue` uses this hook to ensure that any `Fw::Buffer` that cannot be queued is returned to its sender via `bufferReturnOut`, preventing a loss of memory ownership. | `Svc/ComQueue/ComQueue.fpp` & `ComQueue.cpp` |
| 486 | Implementation | How does `Drv::LinuxUartDriver` handle non-canonical input processing? | The driver explicitly sets `newtio.c_lflag = 0` and `newtio.c_oflag = 0` to disable canonical mode, echo, and output processing. This ensures that the UART interface provides raw, unaltered byte streams between the hardware and the F´ software stack, which is critical for protocol framing and scientific data integrity. | `Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 487 | Architecture | Describe the "Passthrough" nature of `Drv::ComStub` for uplink data. | When `ComStub` receives data from a driver via `drvReceiveIn`, it does not perform any processing or validation. It simply wraps the raw bytes in an `Fw::Buffer` and forwards it via `dataOut` with an empty `FrameContext`. This delegates all de-framing and protocol logic to downstream components, keeping the stub highly generic. | `Drv/ComStub/docs/sdd.md` |
| 488 | Implementation | How is the `globalVcId` field formatted in `Drv::Ccsds::TmFramer`? | The 16-bit `globalVcId` is constructed by bitwise-ORing the Transfer Frame Version (0), the `SpacecraftId` (shifted left by 4), the `VirtualChannelId` (shifted left by 1), and the Operational Control Field flag (0). This follows the CCSDS TM standard for identifying the spacecraft and logical channel of the telemetry stream. | `Svc/Ccsds/TmFramer/TmFramer.cpp` |
| 489 | Implementation | Explain the use of `tcgetattr` and 1 second timeout in `LinuxUartDriver`. | During initialization, the driver calls `tcgetattr` to fetch current device settings, then sets `VMIN=0` and `VTIME=10` (1.0 second). This configures a non-blocking read with a 1-second timeout. If no bytes arrive within that window, the `read()` call returns 0, allowing the driver thread to stay responsive to `m_quitReadThread` signals. | `Drv/LinuxUartDriver/LinuxUartDriver.cpp` |
| 490 | Modeling | Describe the `Fw::ComPacketType` usage in `Svc::ComQueue` serialization. | When `ComQueue` sends an `Fw::ComBuffer`, it first serializes an `FwPacketDescriptorType` (derived from the buffer type) into the `FrameContext` APID field. This allows ground systems or downstream deframers to identify the content type (e.g., Command, Telemetry, Log) without inspecting the payload itself. | `Svc/ComQueue/ComQueue.cpp` |
| 491 | Implementation | How does `Drv::LinuxSpiDriver` perform a full-duplex transaction? | It uses the `spi_ioc_transfer` structure. The F´ write buffer pointer is assigned to `tr.tx_buf` and the read buffer pointer to `tr.rx_buf`. Both buffers must be the same size. Calling `ioctl(m_fd, SPI_IOC_MESSAGE(1), &tr)` executes the hardware clock cycles required to simultaneously send and receive data. | `Drv/LinuxSpiDriver/LinuxSpiDriverComponentImpl.cpp` |
| 492 | Architecture | Explain the separation of `configureSend` and `configureRecv` in `Drv::UdpSocket`. | Unlike TCP, UDP is connectionless. `UdpSocket` provides separate configuration methods to allow for unidirectional data flows. `configureSend` sets the target IP/port for outgoing packets, while `configureRecv` binds the socket to a local IP/port for incoming datagrams. This enables flexible topologies like broadcast beacons or command-only uplinks. | `Drv/Ip/UdpSocket.cpp` |
| 493 | Implementation | Describe the `handleZeroReturn()` logic in `Drv::UdpSocket`. | In the base `IpSocket`, a 0-byte read indicates a TCP disconnection. `UdpSocket` overrides `handleZeroReturn()` to return `SOCK_SUCCESS`. Since UDP is datagram-based, a 0-byte read is a valid empty packet (success), not a signal that the remote peer has closed a persistent connection. | `Drv/Ip/UdpSocket.cpp` |
| 494 | Implementation | How does `Svc::ComAggregator` ensure it only sends valid buffers? | The `doSend` action in the `AggregationMachine` state machine explicitly checks `m_frameSerializer.getSize() > 0` before invoking the `dataOut` port. This prevents the component from emitting empty or uninitialized aggregates when a timeout signal arrives or an overflow is detected on an empty buffer. | `Svc/ComAggregator/ComAggregator.cpp` |
| 495 | Implementation | Why does `Svc::ComQueue` reset high-water marks in its `run` handler? | To provide real-time performance monitoring. The `run` handler writes the current `get_high_water_mark()` for every queue to telemetry and then calls `clear_high_water_mark()`. This allows ground systems to see the peak queue depth reached *during each reporting interval*, rather than a cumulative peak since startup. | `Svc/ComQueue/ComQueue.cpp` |
| 496 | Modeling | Describe the `ChoiceToChoice` transition pattern in FPP State Machines. | It is a sequence where a branch from one `choice` block targets another `choice` block. The state machine evaluates the guards of the first choice, potentially executes an action, then immediately evaluates the guards of the second choice. This allows for complex, hierarchical decision logic to be expressed linearly. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/ChoiceToChoice.cpp` |
| 497 | Implementation | What is the `Fw::InternalInterfaceString` type and its default capacity? | It is a fixed-size string implementation used for arguments in internal interface calls. By default, it has a capacity of **40 characters**. This ensures that messages sent to the component's own queue have a deterministic memory footprint, following F´'s avoidance of dynamic allocation. | `fprime/FppTestProject/FppTest/component/active/ActiveTest.hpp` |
| 498 | Implementation | How does `Svc::Ccsds::ApidManager` handle sequence count wrap-around? | The CCSDS Space Packet standard uses 14-bit sequence counts. `ApidManager` implements this using modulo math: `(seqCount + 1) % (1 << SpacePacketSubfields::SeqCountWidth)`. This ensures that counts wrap correctly from 16383 to 0, maintaining protocol compliance for long-duration science data streams. | `Svc/Ccsds/ApidManager/ApidManager.cpp` |
| 499 | Testing | Explain the usage of `SmHarness::Pick` in FPP state machine unit tests. | `SmHarness::Pick` is a utility namespace that provides helper functions (like `stateMachineId()`, `testAbsType()`, `testEnum()`) to generate random but valid data values. This allows tests to explore wide ranges of input data while ensuring the state machine's deterministic response logic remains correct. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/Pick.hpp` |
| 500 | Architecture | Describe the `configConstants` and `configObjects` phases in `Fpp::ToCpp::Phases`. | These are the earliest stages of F´ system initialization. `configConstants` sets system-wide numeric values, while `configObjects` instantiates global resources like the component registry and memory managers. These must complete before component-specific `initComponents` can safely access shared framework resources. | `fprime/Fpp/ToCpp.fpp` |
| 501 | Implementation | How does `Drv::LinuxGpioDriver` translate `EBADF` errors? | The driver uses a helper function `errno_to_gpio_status()` to map the standard Linux `EBADF` (Bad File Descriptor) error code to the F´ `Drv::GpioStatus::NOT_OPENED` enum. This provides hardware-neutral error reporting to high-level mission components. | `Drv/LinuxGpioDriver/LinuxGpioDriver.cpp` |
| 502 | Modeling | What is an "Internal Transition" in an FPP State Machine? | Defined as `on signal do { action }`, it allows a state to handle a signal and execute logic without triggering an exit/entry cycle for the state itself. This is useful for responding to periodic heartbeats or configuration updates while maintaining the same operational mode. | `fprime/FppTestProject/FppTest/state_machine/internal/state/include/BasicInternal.fppi` |
| 503 | Testing | How is `ASSERT_PRODUCT_SEND` used in Data Product unit tests? | It is a macro that verifies a science data transmission. It checks the global ID (Base + local ID), priority, timestamp, and user data, then automatically sets up a deserializer for the packet payload, allowing the test to verify the individual science records within the buffer. | `fprime/FppTestProject/FppTest/dp/test/ut/DpTestTester.hpp` |
| 504 | Implementation | Describe the `Fw::LinearBufferBase::copyRaw()` method. | It is a low-level memory utility that copies a specified number of bytes from another linear buffer into the current one. It is used in FPP tests to simulate the transfer of serialized data units between component ports without triggering high-level type-specific serialization logic. | `fprime/FppTestProject/FppTest/component/tests/TesterHandlers.cpp` |
| 505 | Modeling | Explain the significance of "choice" state exhaustiveness in FPP. | FPP requires all paths through a `choice` block to be covered. If an `if` guard is used, an `else` branch **must** be provided. This prevents the state machine from entering an undefined state if none of the explicit conditions are met, ensuring system stability. | `fprime/FppTestProject/FppTest/state_machine/internal/choice/include/Basic.fppi` |
| 506 | Implementation | What is the `m_allow_timeout` flag used for in `Svc::ComAggregator`? | It acts as a gate for timeout signals. It is set to `true` when the component enters the `FILL` state and `false` when it transitions to `WAIT_STATUS`. This ensures that ground-link timeout messages are only processed when the component is actively aggregating data, preventing queue saturation during periods of high link latency. | `Svc/ComAggregator/ComAggregator.cpp` |
| 507 | Implementation | How does `Drv::UdpSocket` implement "Ephemeral Port" selection? | By passing `0` as the port number to `bind()`. The operating system assigns a random available port, which the driver then retrieves using `getsockname()` and logs. This allows F´ components to dynamically allocate network interfaces for receiving responses from remote ground systems. | `Drv/Ip/UdpSocket.cpp` |
| 508 | Testing | Describe the `SmHarness::History` template. | It is a history structure used in state machine tests to record both the signal ID and its associated data value (for typed signals). It allows unit tests to verify that the state machine not only transitioned correctly but also received and processed the correct data payload. | `fprime/FppTestProject/FppTest/state_machine/internal/harness/SignalValueHistory.hpp` |
| 509 | Modeling | How are FPP format strings used in array definitions? | They customize the `toString()` output. For example, `array A = [3] U32 format "0x{x}"` instructs the autocoder to render each element in hexadecimal format with a "0x" prefix, facilitating human-readable debugging of binary data structures. | `fprime/FppTestProject/FppTest/array/format.fpp` |
| 510 | Implementation | Explain the "Double CRC" check in `AMPCSSequence` file loading. | The component first reads a `.CRC32` file and validates its internal hash. It then reads the actual sequence binary, computes its 32-bit CRC, and compares it against the previously verified hash. This ensures both the command sequence and its cryptographic fingerprint are consistent and untampered. | `Svc/CmdSequencer/formats/AMPCSSequence.cpp` |
| 511 | Architecture | Describe the primary role of the `Components::SampleComponent` template. | It serves as a foundational boilerplate for active components, pre-integrated with standard F´ ports for Time (`timeCaller`), Commands (`Fw.Command`), Events (`Fw.Event`), Telemetry (`Fw.Channel`), and Parameters (`prmGetOut`/`prmSetOut`). It includes a required placeholder `async command TODO` to ensure the component is immediately compatible with the framework's ReAct loop. | `Components/SampleComponent/SampleComponent.fpp` |
| 512 | Deployment | Explain how the `TopologyState` struct is used to shuttle CLI arguments. | In a standard deployment, `Main.cpp` parses command-line flags (e.g., `-a` for hostname, `-p` for port) and populates a `Deployments::TopologyState` instance. This object is then passed to `setupTopology`, allowing the topology configuration logic to remain decoupled from the specific OS-level argument parsing implementation. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopologyDefs.hpp` |
| 513 | Implementation | How does the `Main.cpp` entry point handle clean application shutdown? | It registers a `signalHandler` for `SIGINT` and `SIGTERM`. Upon receiving a signal (like Ctrl-C), the handler calls `Deployments::stopRateGroups()`, which invokes `timer.quit()`. This breaks the execution loop in `Main.cpp`, allowing `teardownTopology` to execute and cleanly join all active component threads. | `Deployments/SampleTcpClientDeployment/Main.cpp` |
| 514 | Architecture | Describe the "Base ID Convention" `0xDSSCCxxx` used in F´ deployments. | This convention ensures unique global identifiers for every port, command, and telemetry channel. `D` is the deployment digit, `SS` identifies the subtopology (e.g., `01` for `CdhCore`), `CC` identifies the specific component instance, and `xxx` is reserved for internal component indices. This allows for collision-free routing in multi-deployment systems. | `Deployments/SampleTcpClientDeployment/Top/instances.fpp` |
| 515 | Implementation | Why is `Fw::MallocAllocator` used in `Deployments::configureTopology`? | The `Svc::CmdSequencer` component requires a buffer to store the binary content of command sequences loaded from disk. Since this memory is typically larger than standard port buffers and its size may vary by mission, the topology manually allocates this space using a `MallocAllocator` during the configuration phase. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 516 | Modeling | Explain the purpose of `Svc::RateGroupDriver::DividerSet` configuration. | It defines the fundamental timing sub-cycles of the system. For example, a set of `{{1, 0}, {2, 0}, {4, 0}}` coupled with a 1Hz clock signal will drive three rate groups at 1Hz, 0.5Hz, and 0.25Hz respectively, all with zero phase offset relative to the master tick. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 517 | Implementation | How are rate group context tokens used in the reference topology? | `ActiveRateGroup` components take a context array (e.g., `rateGroup1Context`) where each element corresponds to a connected child component. While the reference topology initializes these to zero, missions use these tokens to pass task-specific identifiers (like a hardware instance ID) to generic components shared across multiple rate groups. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 518 | Architecture | Contrast the `comDriver` configuration in TCP Client vs. TCP Server deployments. | In `SampleTcpClientDeployment`, `setupTopology` calls `comDriver.configure` using the remote hostname/port and starts a receive thread. In `SampleTcpServerDeployment`, the same call initiates a listening socket on the local host, and the driver thread blocks on `accept()` rather than `connect()`. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 519 | Implementation | What is the significance of `COMM_PRIORITY = 34` in the deployment? | It assigns a specific scheduling priority to the communication driver thread. In F´, communication tasks are typically assigned a priority high enough to ensure timely data processing but lower than critical flight control rate groups (`priority 41-43`), preventing telemetry processing from starving real-time control loops. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 520 | Architecture | Describe the `ComCcsds_CdhCore` connection graph. | It bridges the command/telemetry core to the CCSDS stack. It connects `CdhCore.events.PktSend` and `CdhCore.tlmSend.PktSend` to the `ComCcsds.comQueue`, and routes `ComCcsds.fprimeRouter.commandOut` to `CdhCore.cmdDisp.seqCmdBuff`, forming the primary data loop between the spacecraft and the ground. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 521 | Implementation | How is "Flow Control" signaled from the ground to the application core? | It is implemented via the `seqCmdStatus` and `cmdResponseIn` ports. When the `cmdDisp` finishes a command, it sends a status back through the `fprimeRouter` to the `ComStub`. This signals the ground system (or sequence queue) that the dispatcher is ready for the next uplinked command packet. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 522 | Modeling | Explain the `import` usage in `topology.fpp`. | F´ uses modular subtopologies (e.g., `CdhCore.Subtopology`). The `import` statement allows a deployment to include entire graphs of pre-wired components as a single unit. This promotes architectural reuse and ensures that core services like health monitoring and file handling are consistent across different deployments. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 523 | Implementation | Describe the teardown sequence for the `comDriver` in `Main.cpp`. | Teardown follows a strict `stop()` -> `join()` pattern. `comDriver.stop()` sets an internal flag to break the receive loop, and `comDriver.join()` blocks the main thread until the OS confirms the driver task has exited. This prevents use-after-free errors when the topology is subsequently deallocated. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 524 | Architecture | What is the role of `chronoTime` in the deployment topology? | `chronoTime` (an instance of `Svc::ChronoTime`) acts as the system's central time authority. It provides a standard `timeGet` port that is wired to all other components via a `time connections` pattern, ensuring that every event and telemetry channel in the system uses a synchronized timestamp. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 525 | Implementation | How does the `SampleUartDeployment` handle a failed device open? | If `comDriver.open` returns false (e.g., due to an invalid `/dev/tty` path or insufficient permissions), the deployment prints an error message to `stdout` and skips calling `comDriver.start()`. This prevents the application from crashing while signaling to the operator that the communication link is inactive. | `Deployments/SampleUartDeployment/Top/SampleUartDeploymentTopology.cpp` |
| 526 | Modeling | Describe the purpose of the `Ports_RateGroups` enum in FPP. | It provides symbolic names for the output ports of the `RateGroupDriver`. By connecting `rateGroupDriver.CycleOut[Ports_RateGroups.rateGroup1]` instead of using a raw index, the topology becomes more readable and resistant to errors when rate group assignments are reordered. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 527 | Implementation | Why does `Svc::ActiveRateGroup` use a queue size of 10? | This is a safety margin defined in `Default.QUEUE_SIZE`. Since rate groups are triggered by a master timer, a small queue allows the system to absorb minor jitter in task execution without dropping cycles, while keeping the memory footprint small and deterministic. | `Deployments/SampleTcpClientDeployment/Top/instances.fpp` |
| 528 | Architecture | Explain the connection between `comDriver` and `commsBufferManager`. | The `comDriver` (e.g., `TcpClient`) invokes `allocate` to request memory from the `commsBufferManager` before receiving a packet from the socket. After the packet is processed by the `ComStub` and downstream components, it is returned via the `deallocate` port, ensuring memory is recycled within a fixed-size pool. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 529 | Implementation | How are "Ping Entries" used for system health monitoring? | Every component instance that supports the health interface must have an entry in the `PingEntries` namespace (e.g., `Deployments_rateGroup1`). These entries define `WARN` and `FATAL` thresholds (e.g., 3 and 5 missed pings). The `Svc::Health` component uses these values to determine when to emit an event or trigger a system-level reset. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopologyDefs.hpp` |
| 530 | Implementation | Describe the `timer.startTimer(interval)` behavior in `Main.cpp`. | This is a blocking call. It enters a loop that uses `Os::Task::delay` to wait for the specified interval (e.g., 1 second for 1Hz) and then invokes the `CycleOut` port. This tick drives the entire `RateGroupDriver` and sub-rate cycles, effectively serving as the "heartbeat" of the deployment. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentTopology.cpp` |
| 531 | Modeling | Explain the `omit` keyword usage in `telemetry packets` definitions. | The `omit` keyword (e.g., `omit { CdhCore.cmdDisp.CommandErrors }`) instructs the packetizer to exclude specific telemetry channels from an aggregated packet. This is useful when a channel is already handled by a high-priority "Error" packet or when bandwidth constraints require excluding redundant data from science packets. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentPackets.fppi` |
| 532 | Implementation | How does `Svc::ActiveRateGroup` report cycle slips? | During its `run` handler, the component compares the time since its last execution against the expected period. If the delta exceeds a threshold, it increments the `RgCycleSlips` telemetry channel. This provides ground operators with critical visibility into CPU over-utilization or scheduling conflicts. | `Deployments/SampleTcpClientDeployment/Top/SampleTcpClientDeploymentPackets.fppi` |
| 533 | Architecture | Describe the data flow from `DataProducts` to ground downlink. | The `dpCat` (Catalog) component emits a `fileOut` signal when a science data product is ready. This is wired to `FileHandling.fileDownlink.SendFile`. Once the file is successfully transmitted, `fileDownlink` returns a `FileComplete` signal to `dpCat.fileDone`, allowing the catalog to mark the product as downlinked. | `Deployments/SampleTcpClientDeployment/Top/topology.fpp` |
| 534 | Testing | Why are `UT_AUTO_HELPERS` enabled in the `SampleComponent` build? | This flag instructs the FPP autocoder to generate `GTestBase` and `TesterBase` classes. These generated helpers provide automated port connection logic and history buffers, significantly reducing the boilerplate code required to write unit tests for the component's behavioral logic. | `Components/SampleComponent/CMakeLists.txt` |
| 535 | Implementation | How does `Main.cpp` convert the `-p` flag to a port number? | It uses `atoi(optarg)` and then performs a `static_cast<U16>`. This handles the conversion from the command-line string to the 16-bit integer required by the `TcpClient::configure` method, while the `TopologyState` handles the transfer of this value into the F´ topology setup logic. | `Deployments/SampleTcpClientDeployment/Main.cpp` |
| 536 | Build System | Describe the "Contextual Search" logic used by `fprime-util` to locate a build cache. | `fprime-util` determines the build cache by recurring upward from the current working directory (CWD) until it finds a directory containing an F´ build cache matching the specified platform. If the `--ut` flag is present, it exclusively searches for unit test caches (`build-fprime-automatic-*-ut`). This allows developers to run build commands from any subdirectory within a component or deployment. | `fprime-util --help` |
| 537 | Build System | What is the implication of running `fprime-util check` without the `--ut` flag? | The `--ut` flag is **implied** when running the `check` command. `fprime-util` will automatically search for and utilize a unit test build cache to execute the component's test suite, even if the user does not explicitly specify the testing context on the command line. | `fprime-util --help` |
| 538 | Build System | How does `fprime-util` translate a directory path into a CMake build target? | It maps the relative path from the project root to a target name by replacing directory separators with underscores and appending the command. For example, running `fprime-util impl` inside `Ref/SignalGen` translates to the CMake target `Ref_SignalGen_impl`. This abstraction allows developers to focus on component paths rather than internal build system naming conventions. | `fprime-util --help` |
| 539 | Tooling | Explain the primary function of the `fprime-util impl` command. | The `impl` command invokes the FPP autocoder to generate implementation templates (`-Impl.cpp` and `-Impl.hpp`) based on the component's FPP model. It identifies missing port handlers and command implementations, providing a "fill-in-the-blanks" starting point for developers, which ensures that the C++ implementation matches the modeled interface. | `fprime-util --help` |
| 540 | Tooling | Describe the utility of the `fprime-util hash-to-file` command in flight software debugging. | F´ uses a space-saving optimization where `FW_ASSERT` calls log a numeric file ID hash instead of a full string path. `hash-to-file` takes this numeric hash and cross-references it against the build's hash map to return the absolute file path and line number where the assertion failed, enabling rapid root-cause analysis from compact flight logs. | `fprime-util --help` |
| 541 | Build System | Contrast the behavior of `fprime-util build` when run in a component directory versus a deployment directory. | When run in a **component** directory, it builds only that specific module. When run in a **deployment** directory, it builds the deployment binary and recursively builds every component instance and library linked into that deployment's topology. This "Top-Down" build ensures consistency across the entire flight application. | `fprime-util --help` |
| 542 | Build System | How can a developer force `fprime-util` to use a non-standard build cache directory? | By using the `--build-cache` flag. This flag overrides the standard recursive search logic and forces the tool to use the provided path as the CMake build directory, which is useful for specialized CI/CD environments or side-by-side comparisons of different build configurations. | `fprime-util --help` |
| 543 | Modeling | What does the `fprime-util visualize` command provide to the developer? | It launches a local web server and GUI that renders the FPP model as an interactive graph. It visualizes component connections, port types, and topology layouts, allowing engineers to verify complex system wirings and identify potential integration issues visually before compilation. | `fprime-util --help` |
| 544 | Configuration | How does `fprime-util` determine the default toolchain if none is specified? | It first checks for a `default_toolchain` entry in the project's `settings.ini` file. If that setting is missing, it defaults to the `native` toolchain, which uses the host system's compiler (e.g., `gcc` or `clang`) to build binaries for the local operating system. | `fprime-util --help` |
| 545 | Tooling | Explain the difference between `fprime-util purge` and `fprime-util generate`. | `purge` completely removes the `build-fprime-automatic-*` directories and all cached CMake artifacts, effectively "resetting" the build environment. `generate` creates or updates these directories by running the CMake configuration step, which is required whenever the project structure, `CMakeLists.txt`, or `settings.ini` is modified. | `fprime-util --help` |
| 546 | Build System | Describe the effect of the `--all` flag when combined with `fprime-util build`. | The `--all` flag instructs `fprime-util` to ignore the current deployment context and build every component, library, and tool defined within the entire F´ project tree. This is used to ensure global project health and catch syntax errors in components that might not yet be included in a deployment topology. | `fprime-util --help` |
| 547 | Tooling | What is the role of `fpp-check` in the F´ development workflow? | `fpp-check` is a static analysis tool for FPP models. It performs semantic validation, checking for issues like port type mismatches, missing connection endpoints, and ID collisions. Running this via `fprime-util fpp-check` allows developers to catch modeling errors early, before triggering the more expensive C++ compilation phase. | `fprime-util --help` |
| 548 | Implementation | How does `fprime-util format` maintain codebase consistency? | It wraps `clang-format` and applies the project's `.clang-format` rules to all C++ and header files in the current path. This ensures that all flight code adheres to the NASA/JPL JPL coding standards for indentation, bracing, and line-wrapping, regardless of which developer authored the code. | `fprime-util --help` |
| 549 | Build System | Explain the usage of the `-r/--root` flag in `fprime-util`. | The `--root` flag specifies an alternate directory to begin the upward search for a build cache. This is used when a developer is working in a library (like `Svc` or `Drv`) but wants to build it against the specific configuration and compiler settings of a distant deployment (like `Ref`). | `fprime-util --help` |
| 550 | Modeling | Describe the output of `fprime-util fpp-to-dict`. | This command translates FPP models into a machine-readable JSON dictionary format. These dictionaries are consumed by the F´ Ground Data System (GDS) to dynamically populate the UI with command names, telemetry channels, and event types without requiring the GDS to have deep knowledge of the C++ source code. | `fprime-util --help` |
| 551 | Tooling | What information is provided by the `fprime-util info` command? | `info` prints diagnostic data about the current execution context, including the identified build cache path, the platform being targeted, the specific CMake target that would be run for the current directory, and the location of the project root. It is the primary tool for troubleshooting "cache not found" errors. | `fprime-util --help` |
| 552 | Tooling | How does `fprime-util new` assist in project expansion? | It provides a wizard-based interface to generate new F´ objects, such as components, ports, or deployments. It creates the necessary directory structure, boilerplate FPP/C++ files, and `CMakeLists.txt` entries, ensuring that new modules are immediately compliant with the project's build patterns. | `fprime-util --help` |
| 553 | Implementation | Explain the significance of `FPRIME_SKIP_TOOLS_VERSION_CHECK` in `settings.ini`. | When set to `ON`, it prevents the build system from failing if the installed versions of tools (like `cmake` or `python`) do not exactly match the "recommended" versions. This allows developers to use newer toolchains while acknowledging the potential risk of minor build inconsistencies. | `settings.ini` |
| 554 | Architecture | Describe how `fprime-util` handles `fprime-venv` discovery. | While `fprime-util` itself is a Python package, the bash wrapper scripts (like `fprime-tui`) are designed to locate the project's virtual environment (`fprime-venv`), activate it, and then execute the utility. This ensures that the autocoders and build scripts always run with the correct dependency versions. | `fprime-tui` wrapper logic |
| 555 | Build System | What is a "Platform" in the context of `fprime-util generate`? | A platform (e.g., `native`, `raspberrypi`, `linux`) defines the specific toolchain file and cross-compiler settings used by CMake. Specifying a platform during generation creates a dedicated build cache for that target architecture, allowing developers to switch between local testing and flight hardware builds seamlessly. | `fprime-util --help` |
| 556 | Testing | Describe how `STest` integration works within `fprime-util check`. | `fprime-util check` identifies test targets that utilize `STest` (a rules-based testing library). It executes these binaries and captures their output, providing a unified interface for both simple unit tests and complex, randomized, state-space exploration tests. | `fprime-util --help` |
| 557 | Tooling | How can a developer debug the toolchain selection process? | By using `fprime-util version-check`. This command prints the versions and paths of the compilers, autocoders, and framework libraries currently being used. It helps identify if the build system is accidentally picking up a system-wide compiler instead of a cross-compiler from the `fprime-venv`. | `fprime-util --help` |
| 558 | Build System | Explain the relationship between `CMakePresets.json` and `fprime-util`. | `CMakePresets.json` (introduced in modern F´) provides a standard way to define build configurations. `fprime-util` leverages these presets to simplify the `generate` command, allowing developers to use named configurations (like `debug` or `release`) that encapsulate multiple complex CMake flags. | `fprime-util --help` & `CMakePresets.json` |
| 559 | Implementation | What happens when `fprime-util build` is interrupted (e.g., Ctrl-C)? | `fprime-util` passes the interrupt signal to the underlying `cmake --build` process. The build will stop at the current compilation unit. Because F´ uses `ninja` or `make` as the underlying generator, subsequent builds will resume from the point of interruption, leveraging the object file cache. | `fprime-util` process management |
| 560 | Implementation | Describe the `impl` command's behavior when a component has no FPP file. | If `fprime-util impl` is run in a directory without a valid FPP model, it will fail with an error indicating that no autocoder input was found. Implementation templates can *only* be generated from modeled interfaces, reinforcing the "Model-First" engineering philosophy of the F´ framework. | `fprime-util --help` |
| 561 | GDS | Describe the three-layer architecture of the F´ Ground Data System (GDS). | The GDS consists of: 1) the **UI Layer** (an HTML5/JavaScript dashboard for operators), 2) the **Middleware Layer** (a Python-based REST API and data processor that handles dictionary mapping and logging), and 3) the **Comm Layer** (transports like TCP or UART that bridge the GDS to the flight hardware). | `fprime-gds` architecture |
| 562 | GDS | What is the primary purpose of the `--no-app` flag in `fprime-gds`? | The `--no-app` flag instructs the GDS to launch its internal servers (API and GUI) and communication bridges, but skip executing the flight software binary. This is useful when the flight software is already running on target hardware, in a debugger, or within a containerized environment. | `fprime-gds --help` |
| 563 | GDS | How does the GDS utilize the JSON dictionaries generated by `fpp-to-dict`? | The GDS Middleware loads these dictionaries at startup to map raw telemetry IDs and command opcodes to human-readable names, types, and formatting strings. This allows the GDS to remain generic; it does not need to be recompiled when the flight software model changes, as all interface definitions are provided by the dictionaries. | `fprime-gds` dictionary logic |
| 564 | GDS | Explain the role of the "Comm Adapter" in the GDS environment. | Comm Adapters are modular Python classes that implement specific transport protocols. The default `ip` adapter handles TCP/IP socket communication, while the `uart` adapter handles serial links. This modularity allows the GDS to support diverse hardware interfaces without changing the core data processing logic. | `fprime-gds --help` |
| 565 | GDS | Describe the data flow of a command uplinked via the GDS GUI. | 1) The operator clicks "Send" in the GUI. 2) The browser sends a REST request to the Middleware. 3) The Middleware serializes the command into an `Fw::Com` packet using the dictionary. 4) The Comm Layer wraps the packet in a transport frame (e.g., framing bytes for TCP) and writes it to the socket/serial port. | `fprime-gds` command flow |
| 566 | GDS | What is the significance of the `fprime-gds.yml` configuration file? | This YAML file allows developers to store persistent CLI options for a specific deployment, such as the default IP address (`ip-client`), port number, or log directory. The `fprime-gds` tool automatically loads this file from the current directory, ensuring that the GDS launches with the correct mission-specific settings. | `Deployments/SampleTcpClientDeployment/fprime-gds.yml` |
| 567 | GDS | How does the GDS handle "Orderly Shutdown" of the flight application? | When the GDS is closed (or sends a termination signal), it attempts to cleanly stop the comm bridges and logs. If it launched the flight application, it sends a `SIGINT`/`SIGTERM` to the process, allowing the flight software's signal handlers to execute `teardownTopology` before the process exits. | `fprime-gds` process management |
| 568 | GDS | Explain how the GDS visualizes telemetry "Limits" (Red/Yellow/Orange). | The GDS UI reads limit definitions from the FPP model via the dictionary. If a telemetry value crosses a defined threshold (e.g., `low { red 0, orange 1, yellow 2 }`), the UI dynamically changes the channel's display color, providing immediate visual alerts to operators regarding system health. | `fprime-gds` UI logic |
| 569 | GDS | What information is stored in the GDS "Command Log"? | The command log records every command dispatched from the GDS, including the timestamp, the opcode, the human-readable arguments, and the status of the uplink (e.g., "Sent" or "Failed"). This provides a critical audit trail for reconstructing mission events. | `fprime-gds` logging |
| 570 | GDS | Describe the function of the `ip-address` and `ip-port` flags. | These flags specify the binding for the GDS internal web server and REST API. By default, it binds to `127.0.0.1:5000`. Adjusting these allows developers to host the GDS on a remote server while accessing the UI from a local browser. | `fprime-gds --help` |
| 571 | GDS | How does the GDS distinguish between different deployment binaries? | It uses the `--app` flag to specify the path to the flight software binary. This path is often relative to the `build-artifacts` directory. By changing this flag, the same GDS installation can be used to test different mission configurations or architecture prototypes. | `fprime-gds --help` |
| 572 | GDS | Explain the utility of the GDS "Raw Data" view. | The "Raw Data" or "Packets" view shows the hex-encoded stream of bytes arriving from the comm link. This is an essential debugging tool for verifying framing protocol integrity, checking for sync byte alignment, or troubleshooting corrupted dictionaries. | `fprime-gds` UI features |
| 573 | GDS | How are "Events" archived by the GDS? | Every event received by the GDS is logged to a CSV or text file in the deployment's `logs/` directory. These logs include the event name, severity, timestamp, and formatted message, enabling post-test analysis and long-term trend monitoring. | `fprime-gds` archiving |
| 574 | GDS | Describe the integration between the GDS and the `Svc::TlmPacketizer`. | When the flight software uses `TlmPacketizer`, it sends aggregated packets instead of individual channels. The GDS uses "Packet Dictionaries" to de-aggregate these frames, mapping the relative offsets within the packet back to the individual telemetry channels defined in FPP. | `fprime-gds` packet logic |
| 575 | GDS | What is the purpose of the `--logs` CLI flag? | It specifies the root directory where all GDS-generated artifacts (command logs, telemetry logs, event CSVs, and raw packet captures) will be stored. Organizing logs by timestamped subdirectories helps maintain data isolation between different test runs. | `fprime-gds --help` |
| 576 | GDS | How can a developer access GDS data programmatically? | The GDS Middleware exposes a RESTful API and a WebSocket interface. External scripts (e.g., Python automation scripts) can subscribe to these interfaces to receive real-time telemetry updates or inject commands, enabling automated regression testing. | `fprime-gds` API documentation |
| 577 | GDS | Explain the "Search" and "Filter" capabilities in the GDS Telemetry view. | Operators can filter telemetry channels by name, component instance, or update status. This is critical for missions with thousands of channels, allowing engineers to focus on specific subsystems (e.g., "Power" or "Thermal") during critical maneuvers. | `fprime-gds` UI features |
| 578 | GDS | What does the GDS "GDS Server" process handle? | The GDS Server is the central hub. It manages the connection to the comm adapter, performs dictionary lookups, routes data to the web GUI, and maintains the persistent log files. It acts as the bridge between the flight protocol and the web protocol. | `fprime-gds` internals |
| 579 | GDS | How does the GDS handle UART flow control? | When using the `uart` comm adapter, the GDS allows configuration of hardware flow control (RTS/CTS) via CLI flags. This must match the `LinuxUartDriver` settings in the flight software to prevent buffer overruns on high-speed serial links. | `fprime-gds --help` |
| 580 | GDS | Describe the "Dictionary Loading" error in the GDS. | This error occurs if the GDS cannot find or parse the `*_Dictionary.json` file. Without a dictionary, the GDS cannot decode incoming packets or display names, resulting in a "Raw Data Only" mode that is difficult for humans to interpret. | `fprime-gds` troubleshooting |
| 581 | GDS | What is the default port for flight application communication? | By convention, F´ applications default to port **50000** for TCP socket communication. The GDS Comm Layer targets this port unless overridden by the `-p` or `--port` flag, ensuring immediate out-of-the-box connectivity. | `fprime-gds` defaults |
| 582 | GDS | Explain the "Clear History" function in the GDS UI. | This function flushes the current display buffers for events and telemetry in the browser. It does *not* delete the persistent log files on disk, providing a way for operators to reset their view during a test without losing the historical data record. | `fprime-gds` UI logic |
| 583 | GDS | How does the GDS visualize "Event Throttling"? | If a flight component throttles an event (e.g., `throttle 5`), the GDS will only display the first 5 occurrences. Subsequent occurrences are suppressed by the flight software and thus never reach the GDS, reflecting the framework's bandwidth preservation logic in the operator view. | `fprime-gds` event logic |
| 584 | GDS | What is the role of the `fprime-gds` Python package? | It is the entry point for the ground system toolset. It orchestrates the lifecycle of the GDS server, the comm bridge, and the optional flight application process, providing a single-command interface for starting an F´ development session. | `fprime-gds` package role |
| 585 | GDS | Describe how the GDS handles "Time" display. | The GDS can display time in "Application Time" (using the timestamps from `chronoTime`) or "GDS Time" (the arrival time at the ground station). Synchronizing these views is essential for calculating one-way light time and troubleshooting link latency. | `fprime-gds` UI features |
| 586 | Tooling | Describe the primary function of the `fprime-util new` subcommand. | `fprime-util new` is an interactive, wizard-based tool for scaffolding new F´ objects such as components, ports, and deployments. it ensures that new modules follow the project's directory structure, naming conventions, and C++ namespaces while automatically updating the build system registration. | `fprime-util new --help` |
| 587 | Modeling | Explain the distinction between "Active", "Passive", and "Queued" components in the `new --component` wizard. | **Active** components have their own execution thread and message queue. **Queued** components have a message queue but share a thread (typically from a rate group). **Passive** components have neither; they execute logic synchronously on the caller's thread. The wizard uses this selection to generate the appropriate FPP keywords and C++ base classes. | `fprime-util new` wizard logic |
| 588 | Build System | How does `fprime-util new` automate build system integration? | After generating the new module's files, the tool attempts to locate the parent directory's `CMakeLists.txt` and automatically appends an `add_fprime_subdirectory()` call. This ensures that the new component or library is immediately recognized by the F´ build system and included in the next `fprime-util generate` run. | `fprime-util new` implementation |
| 589 | Implementation | What boilerplate files are created when generating a new component? | The tool creates a `[ComponentName].fpp` (model), `[ComponentName].cpp` and `[ComponentName].hpp` (implementation templates), a `CMakeLists.txt` (module definition), and a `docs/sdd.md` (Software Design Document placeholder). This enforces the framework's documentation-and-model-first engineering standard. | `fprime-util new --component` output |
| 590 | Configuration | Why does the `new --component` wizard require a C++ namespace? | F´ uses namespaces to prevent symbol collisions between different libraries and components. The wizard uses the provided namespace to wrap the C++ implementation classes and ensures the FPP `module` definition matches, maintaining strict alignment between the model and the code. | `fprime-util new` wizard logic |
| 591 | Deployment | Describe the components of a new deployment generated via `fprime-util new --deployment`. | It scaffolds a full flight application environment, including a `Top/` directory (for topology models), a `Main.cpp` (entry point), and a `Topology.cpp` (initialization logic). It provides the necessary boilerplate to bridge OS-level signals to the F´ topology lifecycle. | `fprime-util new --deployment` output |
| 592 | Modeling | What is the significance of the "Base ID" prompted during deployment creation? | The Base ID is the starting numeric offset for all telemetry, commands, and events in that deployment. The wizard helps developers set this to ensure that multiple deployments within a larger mission (e.g., an orbiter and a lander) have non-overlapping ID spaces for ground system routing. | `fprime-util new` deployment wizard |
| 593 | Implementation | How does `fprime-util new` handle component "Special Ports"? | By default, the generated `.fpp` template for a new component includes imports for `Fw.Command`, `Fw.Event`, `Fw.Channel` (Telemetry), and `Fw.Param`. It also includes a `time get port`, ensuring that any new component is "flight-ready" with the standard framework services pre-integrated. | `Components/SampleComponent/SampleComponent.fpp` |
| 594 | Tooling | Can `fprime-util new` be run outside of an F´ project? | No. The tool relies on detecting the project root (identified by `settings.ini`) to determine where to place files and how to update parent `CMakeLists.txt` files. Running it outside a project will result in an error indicating that the F´ environment could not be initialized. | `fprime-util` project detection |
| 595 | Modeling | Describe the purpose of the `new --port` subcommand. | It generates a new FPP port definition (`.fpp`) containing a signature of arguments and optional return types. Scaffolding ports via the utility ensures that the file is placed in the correct `Ports/` directory and registered as an F´ module for reuse across components. | `fprime-util new --port` |
| 596 | Build System | Explain how `fprime-util new` ensures C++ class naming consistency. | The tool forces the component class name to match the directory name and the FPP component name. If a developer provides a name with spaces or invalid characters, the wizard validates and sanitizes the input, preventing downstream compilation errors caused by illegal C++ identifiers. | `fprime-util new` validation logic |
| 597 | Implementation | What is the "TODO" command found in a newly generated component? | F´ active components require at least one asynchronous input (command or port) to trigger the message queue handler. The `new` utility provides a placeholder `async command TODO` so the component can immediately compile and participate in the topology's ReAct loop before custom logic is added. | `Components/SampleComponent/SampleComponent.fpp` |
| 598 | Architecture | How does `fprime-util new` support modular "Subtopologies"? | The `new --subtopology` option creates a modular FPP graph that can be imported by multiple deployments. This promotes architectural reuse by allowing common groups of components (like a radio stack) to be defined once and instanced across different flight applications. | `fprime-util new --subtopology` |
| 599 | Tooling | Explain the "Path Detection" logic when running `fprime-util new`. | The tool uses the current working directory to suggest a default location for the new module. For example, running `fprime-util new --component` inside a `Components/` folder will automatically target that directory for the new component's home, streamlining the developer's workflow. | `fprime-util` context logic |
| 600 | Implementation | Describe the default Software Design Document (SDD) template generated. | The generated `docs/sdd.md` includes sections for Class Diagrams, Port Descriptions, State Machines, and Requirements. This encourages developers to document the component's behavior and interfaces as they are built, following the framework's rigorous systems engineering process. | `Components/SampleComponent/docs/sdd.md` |
| 601 | Build System | How are "Module Dependencies" handled during component creation? | The generated `CMakeLists.txt` includes a `MOD_DEPS` section. While the wizard adds standard framework dependencies (like `Fw_Port`), it provides a commented-out section for developers to add mission-specific library dependencies, guiding them toward correct build system patterns. | `Components/SampleComponent/CMakeLists.txt` |
| 602 | Modeling | What is the impact of choosing a "Passive" kind for a component that needs telemetry? | The `new` utility will still include the `telemetry` port imports, but the developer must ensure that the component's logic is triggered by an external `schedIn` port or a synchronous call from another component, as a passive component lacks an internal thread to drive periodic reporting. | `fprime-util new` kind selection |
| 603 | Tooling | Describe the "Abortion" behavior of the `fprime-util new` wizard. | If a developer exits the wizard (e.g., Ctrl-C) before completion, the tool performs a cleanup, ensuring that no partially written files or corrupted `CMakeLists.txt` entries are left in the project tree, maintaining the integrity of the build system. | `fprime-util` error handling |
| 604 | Implementation | How does the generated `.hpp` file handle header guards? | The utility generates unique header guards based on the component's path and name (e.g., `NAMESPACE_COMPONENTNAME_HPP`). This prevents naming collisions in large projects where multiple components might have similar names but reside in different namespaces. | `Components/SampleComponent/SampleComponent.hpp` |
| 605 | Architecture | Why does the `new --deployment` wizard generate a `Top/` directory? | In F´, the "Topology" (wiring) is separated from the "Application" (Main.cpp). The `Top/` directory contains the FPP models that define the component instances and their connections, allowing the system's structural design to be managed independently of its execution environment. | `Deployments/SampleTcpClientDeployment/Top/` |
| 606 | Implementation | Explain the use of `register_fprime_library` in a new component's `CMakeLists.txt`. | This CMake function (generated by the utility) tells F´ to treat the component as a library module. It handles the invocation of the autocoders for the `.fpp` files and links the resulting C++ code, simplifying the complex build process into a single declarative call. | `Components/SampleComponent/CMakeLists.txt` |
| 607 | Modeling | How does `fprime-util new` handle multi-word component names? | The wizard typically recommends CamelCase (e.g., `PowerManager`). It uses this name for the C++ class, the FPP component, and the filenames, ensuring that the codebase remains consistent and follows the framework's stylistic conventions. | `fprime-util new` naming standards |
| 608 | Build System | What is the purpose of the `SOURCES` list in a generated component? | It explicitly lists the hand-coded C++ files (e.g., `Component.cpp`). The F´ build system uses this list to distinguish between developer-written logic and autocoder-generated base classes, ensuring that only the necessary files are compiled into the module. | `Components/SampleComponent/CMakeLists.txt` |
| 609 | Implementation | Describe the `Main.cpp` boilerplate for a new deployment. | It includes a basic `main()` function that initializes the OSAL (`Os::init`), parses standard flags (`-a`, `-p`), and orchestrates the `setupTopology`, `startRateGroups`, and `teardownTopology` calls, providing a production-grade entry point out of the box. | `Deployments/SampleTcpClientDeployment/Main.cpp` |
| 610 | Tooling | Explain the "Model-First" verification step after using `fprime-util new`. | After generating a new component, developers are encouraged to run `fprime-util fpp-check` immediately. This verifies that the newly scaffolded FPP model is semantically correct within the project's global context before any C++ code is written, adhering to the "Fail-Fast" modeling paradigm. | `fprime-util` development lifecycle |
| 611 | Tooling | Describe the core function of the `fprime-util format` command. | `fprime-util format` is a wrapper for the `clang-format` utility. It scans the specified directory (or current working directory) for C++ source and header files and automatically reformats them to comply with the project's standardized coding style rules. | `fprime-util format --help` |
| 612 | Implementation | Where are the specific formatting rules defined for an F´ project? | The formatting rules are defined in a `.clang-format` file, typically located at the root of the F´ framework or project directory. `fprime-util format` reads this file to determine properties like indentation width, brace placement, and line length limits. | `fprime-util format` implementation |
| 613 | Implementation | Which file extensions are targeted by the `fprime-util format` command? | The tool primarily targets C and C++ files, including **.cpp**, **.hpp**, **.c**, and **.h**. It does not format FPP model files or Python scripts, as those require different specialized formatting tools. | `fprime-util format` logic |
| 614 | Architecture | Why is automated formatting considered critical for flight software development? | In safety-critical systems, consistent style reduces cognitive load during code reviews and prevents "style noise" in git diffs. Automated formatting ensures that all contributors, regardless of their personal editor settings, produce code that meets NASA/JPL's rigorous readability standards. | F´ Engineering Standards |
| 615 | Tooling | How does `fprime-util format` handle recursive directory structures? | By default, the command operates recursively starting from the directory where it is invoked. If run at the project root, it will attempt to format every C++ file in every component and deployment, ensuring project-wide stylistic synchronization. | `fprime-util format` behavior |
| 616 | Implementation | Does `fprime-util format` create backup files? | No. The command modifies files **in-place**. Developers are strongly encouraged to ensure their workspace is managed by a version control system (like Git) before running the command, allowing them to review and revert changes if necessary. | `fprime-util format` usage |
| 617 | Tooling | Can formatting be restricted to a single component? | Yes. By navigating into a specific component directory and executing `fprime-util format`, the tool's scope is limited to the files within that component's path, allowing for surgical style updates without affecting the rest of the codebase. | `fprime-util format` context |
| 618 | Modeling | Explain why `fprime-util format` does not affect autocoded files. | Autocoded files (e.g., `*Ac.cpp`) are generated by the FPP translators using their own internal formatting logic. `fprime-util format` is intended for **hand-coded** implementation files, where developer-introduced style inconsistencies are most likely to occur. | F´ Development Workflow |
| 619 | Implementation | How can a developer exclude a specific block of code from `fprime-util format`? | Since the tool uses `clang-format`, a developer can use the comments `// clang-format off` and `// clang-format on` to wrap code sections (like complex manual data tables) that should remain untouched by the automated formatter. | `clang-format` integration |
| 620 | Tooling | Describe the relationship between `fprime-util format` and the F´ CI/CD pipeline. | Automated formatting is often used as a "gate" in CI/CD. The pipeline may run a check to ensure that all submitted code is already formatted; if `fprime-util format` would produce changes, the build is failed, enforcing style compliance at the repository level. | F´ CI/CD patterns |
| 621 | Implementation | What is the default indentation style used by the F´ framework? | The F´ framework generally follows the **JPL/C++** coding standard, which specifies 4-space indentation and the use of spaces instead of tabs. The `.clang-format` file ensures this is applied consistently across all framework-compliant components. | `.clang-format` configuration |
| 622 | Tooling | How does `fprime-util format` interact with the `-p/--path` flag? | The `-p` flag allows a developer to specify a target path for formatting without changing their current directory. This is useful for formatting a distant component or library from a centralized deployment terminal. | `fprime-util` global flags |
| 623 | Architecture | Explain how formatting aids in "diff readability" during mission-critical bug fixes. | Consistent formatting ensures that a git diff only highlights **functional logic changes**. Without it, a functional fix might be buried among hundreds of "bracing" or "spacing" changes, making it difficult for reviewers to verify the safety of the patch. | F´ Review Process |
| 624 | Implementation | Does `fprime-util format` require a build cache to run? | Unlike `build` or `check`, the `format` command does **not** strictly require a CMake build cache. It operates directly on the source files using the `.clang-format` configuration, making it one of the few utilities usable before the `generate` phase. | `fprime-util format` dependencies |
| 625 | Tooling | Describe the "Pre-Commit" hook pattern involving `fprime-util format`. | Many F´ projects use a git pre-commit hook that automatically runs `fprime-util format` on staged files. This prevents unformatted code from ever entering the repository, maintaining a "clean" history without manual intervention. | F´ DevOps best practices |
| 626 | Implementation | How does the tool handle "Include Sorting"? | The `.clang-format` rules in F´ typically specify an alphabetical and category-based sort order for `#include` directives. `fprime-util format` automatically organizes headers (e.g., placing framework headers above project-specific ones), which prevents duplicate includes and clarifies dependencies. | `.clang-format` include logic |
| 627 | Implementation | What happens if `clang-format` is not installed on the system? | `fprime-util format` will fail with an error indicating that the `clang-format` executable was not found. The tool is a wrapper, so the underlying formatting engine must be present in the user's `PATH` (typically provided by the `fprime-venv`). | `fprime-util` requirements |
| 628 | Implementation | Explain the "Column Limit" setting in F´ formatting. | The `.clang-format` file usually sets a column limit (e.g., 120 characters). If a line of code exceeds this, `fprime-util format` will automatically wrap it according to C++ operator precedence rules, ensuring the code is legible on standard-width terminal displays. | `.clang-format` line wrapping |
| 629 | Tooling | How can a developer verify what `fprime-util format` *would* change without actually modifying files? | While `fprime-util` doesn't have a built-in "dry-run" flag for formatting, a developer can run `git diff` after formatting to see the changes, or use the underlying `clang-format --dry-run --Werror` command if they need a non-zero exit code for scripting. | `fprime-util` vs raw CLI |
| 630 | Implementation | Describe the handling of "Trailing Whitespace" by the tool. | `fprime-util format` automatically strips trailing whitespace from every line. This prevents "ghost" changes in version control and adheres to the standard practice of keeping source files as compact and meaningful as possible. | `.clang-format` whitespace rules |
| 631 | Architecture | How does the "Model-First" approach impact formatting requirements? | Since the interface logic is generated from FPP, the hand-coded `-Impl.cpp` files are the only place where human error in styling can occur. Automated formatting provides a safety net, ensuring the manual logic "looks" as professional and standardized as the autocoded sections. | F´ Design Philosophy |
| 632 | Implementation | Explain the use of "Reflow Comments" in the formatting rules. | If enabled in `.clang-format`, `fprime-util format` can automatically wrap long comment blocks to fit the column limit. This ensures that technical documentation within the implementation files remains readable and properly aligned with the surrounding code. | `clang-format` comment logic |
| 633 | Tooling | What is the impact of running `fprime-util format` on a large, unformatted legacy codebase? | It will likely result in a massive number of changes. The recommended F´ practice is to format legacy code in a **dedicated style-only commit** before making functional changes, ensuring that the project's history remains clear and interpretable. | F´ Legacy Integration |
| 634 | Implementation | How does the tool handle "Brace Wrapping" for classes and functions? | The F´ `.clang-format` usually specifies `AfterFunction: true` or `AfterClass: true` for brace wrapping. `fprime-util format` will move opening braces to a new line or keep them attached to the declaration based on these rules, enforcing a consistent "Look and Feel" across the entire deployment. | `.clang-format` bracing rules |
| 635 | Implementation | Describe the significance of "Pointer Alignment" in the F´ style guide. | The style guide (enforced by the formatter) typically aligns pointers to the type (e.g., `Fw::Buffer* buffer`) rather than the variable name. This emphasizes the "Type-Safe" nature of F´ and ensures that pointer declarations are consistent throughout the codebase. | `.clang-format` alignment rules |
| 636 | Tooling | Describe the primary objective of the `fprime-util impl` command. | `fprime-util impl` is a code-generation utility that parses a component's FPP model and produces C++ implementation templates (`-Impl.cpp` and `-Impl.hpp`). These templates include the necessary class structure and empty function signatures for all input ports and commands defined in the model, ensuring the developer's code is perfectly aligned with the architecture. | `fprime-util impl --help` |
| 637 | Architecture | Contrast the roles of the `ComponentBase` class and the `ComponentImpl` class. | The **`ComponentBase`** is an autocoded, read-only class that handles framework-level concerns like message queueing, port routing, and serialization. The **`ComponentImpl`** inherits from the base class and is where the developer writes the actual mission logic. `fprime-util impl` generates the initial version of the `ComponentImpl`. | F´ Component Architecture |
| 638 | Implementation | How does `fprime-util impl` handle "Asynchronous" command definitions? | For every `async command` in the FPP model, the `impl` tool generates an `[Opcode]_cmdHandler` method in the C++ template. This method is called by the component's internal thread after the command has been dispatched from the message queue, providing a thread-safe context for executing command logic. | `fprime-util impl` command logic |
| 639 | Implementation | Explain the "TODO" comments found in the generated implementation templates. | The `impl` tool inserts `// TODO` comments inside every generated handler signature. This acts as a marker for developers, highlighting where custom implementation logic must be added. Many IDEs and static analysis tools can then be used to track these pending implementation tasks across the project. | `fprime-util impl` output |
| 640 | Implementation | Describe the handling of `portNum` arguments in generated port handlers. | Every generated port handler includes an `FwIndexType portNum` argument. This allows a single component implementation to distinguish which specific instance of a multi-indexed port (e.g., `schedIn[3]`) was invoked, enabling localized logic based on the connection source. | `fprime-util impl` port logic |
| 641 | Modeling | What happens if a developer runs `fprime-util impl` after already writing some logic? | By default, `fprime-util impl` outputs the template to `stdout` or to new files. It does **not** automatically merge changes into existing implementation files. The developer must manually copy the new handler signatures from the `impl` output into their existing `-Impl.cpp` and `-Impl.hpp` files to maintain model-code alignment. | `fprime-util impl` usage |
| 642 | Implementation | How does `fprime-util impl` generate handlers for "Guarded" input ports? | For `guarded` ports, the tool generates a standard handler signature. However, the underlying `ComponentBase` (generated by the autocoder) ensures that this handler is wrapped in a mutex lock during execution. `fprime-util impl` helps the developer by providing the correct signature that the base class expects to call within that guarded context. | `fprime-util impl` handler logic |
| 643 | Implementation | Describe the generation of "Return" port handlers. | If a port is defined with a return type (e.g., `port P -> bool`), the `impl` tool generates a C++ function that matches this signature and includes a placeholder `return` statement. This ensures that the developer implements the required synchronous response logic expected by the caller. | `fprime-util impl` signature logic |
| 644 | Tooling | Why does `fprime-util impl` require a successful `fpp-check` as a prerequisite? | Since `impl` relies on parsing the FPP model to determine the C++ interface, the model must be semantically valid. Any errors in the FPP (like undefined types or ID collisions) would prevent the tool from correctly identifying the handlers that need to be generated. | F´ MDD Workflow |
| 645 | Architecture | How are "Internal Interfaces" represented in the generated templates? | Internal interfaces are modeled as `internal port` in FPP. The `impl` tool generates an `[InterfaceName]_internalInterfaceHandler` method. This allows components to send messages to their own queue for deferred processing, and the generated handler is where that deferred logic is implemented. | `fprime-util impl` internal interface logic |
| 646 | Implementation | Explain the use of the `final` keyword in the generated `ComponentImpl` class. | The `impl` tool typically adds the `final` keyword to the implementation class declaration. This is a C++ optimization that prevents further inheritance from the implementation class and allows the compiler to de-virtualize function calls, improving execution speed in resource-constrained flight environments. | `fprime-util impl` C++ patterns |
| 647 | Implementation | How does the tool handle complex data types (structs and arrays) in handler arguments? | For FPP-defined types, the `impl` tool uses the generated C++ names (e.g., `FormalParamStruct`). It passes these by `const` reference (`const T&`) for inputs and by reference (`T&`) for `ref` arguments, adhering to F´'s performance-oriented memory management standards. | `fprime-util impl` type mapping |
| 648 | Implementation | Describe the generation of the class constructor in the `impl` template. | The tool generates a constructor that passes the `compName` to the `ComponentBase`. It also provides an empty initialization block where the developer can initialize private member variables or state machine instances before the component's thread starts. | `fprime-util impl` constructor logic |
| 649 | Modeling | Can `fprime-util impl` generate code for "Active" components without a queue? | No. The FPP semantic rules (verified before `impl` runs) require active components to have a queue. The `impl` tool assumes a queue exists and provides the class structure needed to support the component's background execution task. | F´ Modeling Constraints |
| 650 | Tooling | What is the advantage of using `fprime-util impl` over manual coding? | It eliminates the risk of "Signature Mismatch" errors. Manual coding of complex port handlers is prone to typos in argument types or ordering, which lead to difficult-to-debug link-time errors. `impl` guarantees that the implementation perfectly matches the interface defined in the model. | F´ Engineering Efficiency |
| 651 | Implementation | How does the tool represent `time get port` in the implementation? | It doesn't generate a handler for `time get port` because it is an **output** port. Instead, it provides the developer with the context that the `getTime()` method (provided by the `ComponentBase`) is available for use within any other generated handler to fetch the system time. | `fprime-util impl` service usage |
| 652 | Architecture | Explain the "Hook" handler generation for async ports. | If an async port uses the `hook` overflow behavior, the `impl` tool generates an `overflowHook` method. This allows the developer to implement custom logic (like logging a specific error or returning a buffer) when the component's message queue is unable to accept a new message. | `fprime-util impl` overflow logic |
| 653 | Implementation | Describe the generated signature for a "Text Log" port. | Like telemetry and events, text logging is an **output** capability. `fprime-util impl` does not generate handlers for it but ensures the class is structured such that the `log_TEXT_...` methods are accessible to the developer's logic. | `fprime-util impl` output ports |
| 654 | Implementation | How does the tool handle "Choice" state logic in state machine templates? | When generating code for internal state machines, the `impl` tool provides the skeleton for the action and guard functions associated with the choice points. The developer then fills in the logic that determines which branch the state machine should take. | `fprime-util impl` state machine logic |
| 655 | Architecture | Why are implementation templates generated as `.cpp` and `.hpp` files instead of a single header? | This follows the standard C++ separation of concerns. The `.hpp` defines the class interface and private state, while the `.cpp` contains the executable logic. This structure is required by the F´ build system to enable separate compilation and minimize rebuild times when only logic (and not the interface) changes. | F´ Build Architecture |
| 656 | Implementation | Explain the handling of `FwOpcodeType` in command handlers. | Every command handler receives the opcode as its first argument. While the specific handler is mapped to a specific command, receiving the opcode allows for generic processing or logging of command execution within the implementation. | `fprime-util impl` command arguments |
| 657 | Implementation | What is the `cmdSeq` argument used for in the generated templates? | The `cmdSeq` (Command Sequence) argument is a tracker passed from the `CmdDispatcher`. The developer **must** pass this same value back to the `cmdResponse_out` port at the end of the handler to ensure the ground system can correlate the response with the original uplinked command. | `fprime-util impl` command responses |
| 658 | Tooling | Describe the "Template" nature of the `impl` output. | The output is a "one-time" starting point. Once a developer has added mission-specific logic to the `-Impl.cpp` file, they should never re-run `impl` in a way that overwrites that file. Instead, they use the tool to generate snippets for *new* features added to the model. | F´ MDD Best Practices |
| 659 | Implementation | How does the tool handle "Namespace Nesting"? | If the FPP model defines a component within nested modules (e.g., `module A { module B { component C ... } }`), the `impl` tool generates the correct C++ namespace nesting (`namespace A { namespace B { class C ... } }`), ensuring logical organization of the codebase. | `fprime-util impl` namespace logic |
| 660 | Implementation | Describe the generated `~ComponentImpl()` destructor. | The tool generates a virtual destructor. While often empty by default, it provides the safe location for developers to release any resources (like file handles or manual memory buffers) that were allocated in the constructor or during component operation. | `fprime-util impl` lifecycle logic |
| 661 | Workflow | What is the recommended end-to-end process for creating and implementing a new component? | 1) Use `fprime-util new --component` to scaffold the directory and FPP model. 2) Define your ports, commands, and telemetry in the `.fpp` file. 3) Run `fprime-util impl` to generate the C++ implementation templates. 4) Fill in the logic within the generated `-Impl.cpp` handlers. 5) Run `fprime-util format` to ensure style compliance before building. | `fprime-util new` and `impl` integration |
| 662 | Workflow | Trace the path of a command from an operator's click in the GDS to the component's handler. | The GDS Middleware uses the `fpp-to-dict` JSON dictionary to serialize the command into an `Fw::Com` packet. The Comm Layer (TCP/UART) frames the packet and sends it to the flight application. `Drv::TcpClient` (or UART) receives it into a buffer from `Svc::BufferManager`. `Svc::ComStub` forwards it to the `Svc::Ccsds::SpacePacketDeframer`, which validates the APID and Sequence. Finally, `Svc::CmdDispatcher` routes it to the target component's async handler via the dispatch table. | Integration of GDS, Drv, Svc, and CmdDispatcher |
| 663 | Workflow | How do you go from a conceptual FPP model to a running binary? | 1) Run `fprime-util generate` to create the platform-specific build cache. 2) Run `fprime-util build` to invoke the autocoders and C++ compiler. 3) Navigate to the deployment's `build-artifacts` directory and execute the binary with `-a` and `-p` flags. 4) Use `fprime-gds --no-app` to connect the ground system to the already running binary. | Build system and deployment lifecycle |
| 664 | Workflow | How do you model and verify a Science Data Product (DP)? | 1) Define a `product record` and `product container` in FPP. 2) In the implementation, call `dpGet` to fetch a container and `serializeRecord` to fill it. 3) Verify the logic in a unit test using the `ASSERT_PRODUCT_SEND` macro, which checks the global ID (Base ID + Local ID), priority, and timestamp. 4) In the GDS, use the "Data Products" packet view to monitor successful aggregations. | Integration of FPP, Implementation, Testing, and GDS |
| 665 | Workflow | Describe the workflow for ensuring flight software safety and correctness. | 1) Perform semantic validation with `fprime-util fpp-check`. 2) Implement unit tests using the autocoded `TesterBase` and `GTestBase`. 3) Use `STest` within the tests to perform randomized state-space exploration. 4) Enable `FW_ASSERT` throughout the implementation; if a failure occurs in flight, use `fprime-util hash-to-file` to decode the assertion hash into a specific source line. | F´ Quality Assurance Stack |
| 666 | Workflow | How are system identifiers synchronized between the spacecraft and the ground? | FPP models define numeric IDs (via the `0xDSSCCxxx` convention). During `fprime-util build`, the `fpp-to-dict` tool extracts these into JSON dictionaries. The GDS loads these dictionaries at startup, allowing it to map raw binary packet IDs received from the `comDriver` back to the human-readable names and format strings defined in the original model. | Connection between Modeling, Build, and GDS |
| 667 | Workflow | Trace the flow of "Flow Control" status from the application core back to the ground. | When a component finishes a command, it calls `cmdResponse_out`. This goes to `Svc::CmdDispatcher`, which emits a status signal to the `ComCcsds.fprimeRouter`. The router forwards it to `Svc::ComStub`, which then signals the `comStatusOut` port. This chain informs the GDS (or an onboard sequencer) that the dispatcher is ready for the next uplink, preventing queue saturation. | Comms and Command flow integration |
| 668 | Workflow | How do you implement and verify a hierarchical state machine? | 1) Define states, choices, and transitions in FPP. 2) Use `fprime-util impl` to generate action and guard skeletons. 3) implement logic, utilizing behavioral polymorphism to override parent transitions in child states. 4) Verify the implementation using `SmHarness::History` in unit tests to record signal sequences and ensure the machine reaches the expected `State` after each `dispatchAll` call. | FPP State Machine and Testing integration |
| 669 | Workflow | Describe the timing synchronization between the hardware clock and component logic. | `Svc::LinuxTimer` (or hardware equivalent) provides a master tick to the `CycleOut` port. This is wired to the `Svc::RateGroupDriver`, which uses its `DividerSet` to trigger sub-cycles. These sub-cycles trigger `Svc::ActiveRateGroup` components, which in turn call the `schedIn` or `run` ports of various components (like `Svc::TlmSend` or `Drv::LinuxUartDriver`), ensuring all mission logic executes at deterministic intervals. | Topology and Timing Service integration |
| 670 | Workflow | Trace the persistent storage of telemetry from flight to ground. | A component writes telemetry via `tlmWrite`. `Svc::TlmSend` buffers it and optionally aggregates it via `Svc::TlmPacketizer`. The data is queued in `Svc::ComQueue` and sent via `Drv::TcpClient`. On the ground, the GDS Middleware receives the packet, decodes it using the JSON dictionary, displays the formatted string in the GUI (respecting `Limits`), and archives the value to a timestamped CSV in the `logs/` directory. | End-to-end Telemetry chain |
| 671 | Workflow | How does the system handle communication link reconnections? | If a `Drv::TcpClient` detects a disconnection, its receive thread returns `SOCK_DISCONNECTED`. The `Drv::SocketComponentHelper` then enters a `reconnectLoop`. Once hardware connectivity is restored, the driver invokes its `ready` port. This is wired to `Svc::ComStub.drvConnected`, which resets the `m_reinitialize` flag, allowing the ground system to re-synchronize with the flight software without a full application restart. | Driver and Comm service interaction |
| 672 | Workflow | Describe the process of adding a new telemetry packet to a deployment. | 1) Define the packet structure in a `.fppi` file using the `telemetry packets` keyword. 2) Include individual channel names from component instances (e.g., `CdhCore.cmdDisp.CommandsDispatched`). 3) Wire the `PktSend` port of your telemetry producer to the `comPacketQueueIn` of `Svc::ComQueue`. 4) The GDS will use the updated packet dictionary to de-aggregate the binary frame into individual UI updates. | Modeling and GDS Packet integration |
| 673 | Workflow | How are "External Parameters" managed across the flight lifecycle? | 1) Define `external param` in FPP. 2) In the deployment topology, call `registerExternalParameters` to link the component to a `Fw::ParamExternalDelegate`. 3) During `setupTopology`, call `loadParameters` to populate initial values. 4) Use GDS commands (`PARAM_SET`) to update values in memory and `PARAM_SAVE` to trigger the delegate's `serializeParam` method for persistent storage on disk. | Implementation, Topology, and Parameter service integration |
| 674 | Workflow | Explain the relationship between "Active" kind components and "Rate Groups". | Active components (like `Svc::CmdSequencer`) have their own thread but usually stay dormant. They are "driven" by periodic signals from a Rate Group connected to their `schedIn` port. This ensures that their background processing (e.g., executing the next step in a sequence) is synchronized with the system's global timing cycle while maintaining thread isolation for memory safety. | Architecture and Scheduling integration |
| 675 | Workflow | How do you trace a scientific data product from creation to file download? | 1) The component fills a `DpContainer` and calls `dpSend`. 2) `Svc::DpCatalog` prioritizes the product based on its `priority` field. 3) `Svc::DpCatalog` emits a `fileOut` signal to `Svc::FileDownlink`. 4) `FileDownlink` breaks the product into `Fw::FilePacket` frames. 5) These frames are queued in `Svc::ComQueue` and downlinked. 6) Upon completion, `fileDownlink` calls `dpCat.fileDone` to finalize the record. | Data Products and File Handling integration |
| 676 | Workflow | Describe the "Model-Code-Verification" loop for a bug fix. | 1) Reproduce the failure with an `STest` rule in a unit test. 2) If the interface is wrong, update the FPP model and run `fprime-util fpp-check`. 3) Update the C++ logic in the `-Impl.cpp` file. 4) Run `fprime-util format`. 5) Run `fprime-util check` to verify the fix passes the previously failing test. 6) Use `fprime-gds` to verify the fix in an integrated deployment context. | Development Cycle integration |
| 677 | Workflow | How are command sequences archived and validated? | 1) `Svc::ComLogger` receives command packets from `Svc::ComStub`. 2) It writes the binary data to a file. 3) When the file is closed, it calls `writeHashFile` to create a `.CRC32` checksum. 4) Operators use the GDS to download these files and verify that the hashes match, ensuring that the sequence of commands executed by the spacecraft matches the ground station's audit trail. | Logging, Comms, and File Handling integration |
| 678 | Workflow | Trace the path of an "Orderly Shutdown" signal. | 1) Operator hits Ctrl-C or GDS sends a signal. 2) `Main.cpp` signal handler calls `stopRateGroups`. 3) `Deployments::timer` calls `quit()`, breaking the tick loop. 4) `Main.cpp` calls `teardownTopology`. 5) This calls `stopTasks()` and `freeThreads()`, which signal all `ActiveComponent` threads to exit and join. 6) `comDriver.stop()` is called to close hardware handles (like file descriptors) before the process terminates. | Main, Topology, and Driver lifecycle integration |
| 679 | Workflow | How do you swap communication transports (e.g., TCP to UART) in a deployment? | 1) In `instances.fpp`, change the `comDriver` type from `Drv.TcpClient` to `Drv.LinuxUartDriver`. 2) In `Main.cpp`, update argument parsing to handle `-d` (device) and `-b` (baud) instead of `-a` and `-p`. 3) In `Topology.cpp`, call `comDriver.open` with the new UART parameters. 4) Rebuild with `fprime-util build`. 5) Launch `fprime-gds` with the `--communication-selection uart` flag. | Modeling, Implementation, and GDS selection integration |
| 680 | Workflow | Describe the interaction between `BufferManager` and Driver components. | 1) `Svc::BufferManager` is initialized with a pool of memory. 2) A driver (like `TcpClient`) invokes the `allocate` port. 3) `BufferManager` returns an `Fw::Buffer` pointing to a pre-allocated slot. 4) The driver fills the buffer with data from the hardware. 5) Downstream components (like `ComStub`) process the data and then call the `deallocate` port, which returns the slot to the `BufferManager` pool for reuse. | Resource Management and Driver integration |
| 681 | Workflow | How does the system handle "Queue Overflows" safely? | 1) An FPP `async` port is defined with `hook` behavior. 2) When the `ActiveComponent` queue is full, the framework calls the implementation's `overflowHook`. 3) The developer implements logic to emit a `WARNING_HI` event and return any contained `Fw::Buffer` to its source. 4) This prevents the component from losing track of memory ownership while alerting ground operators to CPU or scheduling issues. | Modeling, OSAL, and Health integration |
| 682 | Workflow | Trace the initialization of a "Modular Subtopology". | 1) `CdhCore::Subtopology` defines a set of instances and connections. 2) In `setupTopology`, `CdhCore::initComponents` is called first. 3) The main deployment's `connectComponents` then wires the subtopology's boundary ports (like `events.PktSend`) to the deployment's central services (like `comQueue`). 4) This ensures that modular subsystems are fully initialized and connected before `startTasks` is executed. | Subtopology and Deployment Topology integration |
| 683 | Workflow | How are "Internal Ports" used for task serialization? | 1) A component defines an `internal port`. 2) A synchronous handler (triggered by a rate group) calls the `..._internalInterfaceInvoke` method. 3) This places a message on the component's own queue. 4) The component's thread later pulls this message and executes the `..._internalInterfaceHandler`. 5) This allows a component to offload complex or long-running processing from a strict timing thread to its own background thread. | Modeling and Threading integration |
| 684 | Workflow | Explain the use of `getsockname` and `getsockopt` in driver verification. | After a `bind()` or `connect()` call in `Drv::IpSocket`, the driver uses `getsockname` to verify the actual port assigned by the OS (especially for ephemeral ports). It uses `getsockopt` with `SO_SNDTIMEO` to confirm that the requested timeouts were successfully applied to the file descriptor, ensuring the driver's behavioral timing matches its configuration. | Driver implementation and OSAL verification |
| 685 | Workflow | How do you verify "Event Throttling" logic in a unit test? | 1) Call the `log_...` method N times, where N exceeds the `throttle` limit. 2) Use the `ASSERT_EVENTS_..._SIZE` macro to verify that only the expected number of events were emitted to the history. 3) Call the `..._ThrottleClear` method. 4) Log the event again and verify that it appears in the history once more, ensuring the framework's bandwidth-preservation logic is functioning. | Implementation and Testing integration |

















