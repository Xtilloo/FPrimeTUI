/* F-Prime Mission Control - JPL/NASA Theme */

Screen {
    background: #1e1e1e;
    color: #F5F5F5;
    layout: vertical;
}

#chat-container {
    height: 1fr;
    background: #1e1e1e;
    overflow-y: scroll;
    padding: 1 2;
    scrollbar-color: #333333;
    scrollbar-color-hover: #e67e22;
    scrollbar-color-active: #e67e22;
}

#input-area {
    dock: bottom;
    height: auto;
    margin: 0;
    border-top: solid #333333;
    background: #252525;
}

#input-container {
    height: auto;
    border: none;
    padding: 0;
}

#prompt-label {
    color: #e67e22;
    padding: 0 0 0 1;
    text-style: bold;
    width: auto;
    height: 3;
}

#ai-input {
    background: #252525;
    border: none;
    min-height: 3;
    max-height: 10;
    height: auto;
    padding: 0 1;
    color: #ffffff;
    width: 1fr;
}

#ai-input:focus {
    border: none;
}

#thinking-indicator {
    height: 1;
    color: #3498db; /* NASA Blue */
    margin: 0 1;
    display: none;
}

#autocomplete-list {
    background: #252525;
    border: solid #e67e22;
    height: 10;
    display: none;
    margin-bottom: 1;
}

/* Chat turn headers - Full width bars */
.chat-header {
    background: #252525;
    color: #e67e22;
    text-style: bold;
    padding: 0 1;
    margin-top: 1;
    border-bottom: solid #333333;
    width: 100%;
}

.user-prompt {
    padding: 0 2;
    color: #f5f5f5;
    margin-bottom: 1;
}

.ai_response {
    padding: 0 2;
    margin-bottom: 1;
    background: transparent;
}

Markdown {
    background: transparent;
    padding: 0;
    margin: 0;
}

/* Prevent highlights when blocks are focused */
Markdown:focus, Markdown > *:focus, MarkdownBlock:focus {
    background: transparent;
    border: none;
}

MarkdownH1, MarkdownH2, MarkdownH3, MarkdownH4, MarkdownH5, MarkdownH6 {
    color: #e67e22 !important;
    text-style: bold;
}

/* --- Code Blocks (Restored to Grey) --- */
MarkdownFence {
    background: #2a2a2a !important;
    border-left: solid #e67e22 !important;
    margin: 1 0;
    padding: 1 2;
    height: auto;
}

MarkdownFence:focus {
    background: #2a2a2a !important;
}

MarkdownFence * {
    background: transparent !important;
    color: #f5f5f5 !important;
}

/* Inline code styling */
.markdown--code_inline, .markdown--inline-code {
    background: #333333 !important;
    color: #3498db !important; /* NASA Blue */
    text-style: bold;
}

/* Decorative elements */
MarkdownBlockQuote, MarkdownBullet, .markdown--strong, .markdown--em, .markdown--link {
    color: #e67e22 !important;
}

MarkdownBlockQuote {
    border-left: solid #e67e22 !important;
    background: #252525 !important;
    margin: 1 0;
    padding: 0 1;
}

/* Utility Classes */
.generating #ai-input {
    opacity: 0.5;
}

.hide-scrollbars {
    scrollbar-size: 0 0;
}
